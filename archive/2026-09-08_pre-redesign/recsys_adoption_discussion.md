# 오픈소스 recsys 도입 — 논의 기록 (임시, 2026-08-31)

> **문서 지위: 임시 기록이고 결정이 아니다.** 어떤 코드도 문서도 이것을 근거로 바뀌지 않았다.
> 설계 기준 문서는 `recommendation_pipeline_design.md`, 가려는 곳은 `recommendation_scoring_design.md`,
> 도구 조사는 `recsys_opensource/`가 소유한다. 이 문서는 그 셋을 읽고 나눈 대화의 결론만 모은 것이다.
>
> **표기**: `사실`(실행·소스로 확인) · `판단`(의견) · `열린 항목`(협의·측정·제품결정 대기)
>
> 검증 환경: Python 3.14 / implicit 0.7.3 / cornac 3.0.0 / scikit-surprise 1.1.5 (2026-08-31 실행)

---

## §1 논의가 도달한 곳

출발점은 "`recsys_opensource/` 문서 리뷰"였고, 도착점은 **surface가 둘이라는 사실이 도구 선택을 갈랐다**는 것이다.

```
surface 1   topic/context 쿼리 → 그 주제를 잘 아는 agent
surface 2   쿼리 없음 → 이 유저의 활동 기반으로 agent 여럿
```

**결론 한 줄**:

| surface | 질문 | 도구 | 근거 |
|---|---|---|---|
| **surface 2** | "이 사람이 좋아할 만한 사람" | **Gorse (또는 유사 올인원)** | 이게 recsys의 정확한 정의다. 직접 만들 이유가 없다 |
| **surface 1** | "이 주제를 잘 아는 사람" | **검색 정식화 (역색인 + BM25)** | CF 계열은 원리적으로 다른 것을 계산한다 (§2-3) |
| 둘 다 | — | **우리 노출 기록** | 나중에 만들 수 없는 유일한 것 (§5) |

---

## §2 사실 — 실행으로 확인한 것

### 2-1 `사실` 조사 문서의 코드 결함 2건 (논의 중 수정됨)

| 위치 | 내용 |
|---|---|
| `surprise.md` §5-1 | `was_impossible`은 known 판정이 아니다. **SVD·BaselineOnly는 unknown user에 `False`를 주면서 global mean(3.000)을 반환**한다. KNN만 `True`. 즉 문서가 막겠다고 선언한 실패를 그 필터가 통과시켰다 |
| `cornac.md` §3-1 | Cornac 3.0.0의 `rank(item_indices=…)`는 **rank는 내림차순, scores는 `item_indices` 입력 순서**로 돌려준다. `zip(rank, scores)`가 오답. 그리고 `item_indices`가 있으면 **`k`가 무시된다** |

부수 확인: `items`와 `filter_items`는 상호 배타(`ValueError`), 빈 후보 배열은 implicit·Cornac 둘 다 예외를 던진다(= "빈 allowlist면 호출하지 않는다"가 라이브러리 차원에서도 강제됨). Gorse `POST`=Value 누적 / `PUT`=덮어쓰기는 문서 서술이 맞다. 1억 규모 메모리 산수 전부 검산 통과.

### 2-2 `사실` 라이브러리 상태 — 도입에 걸림돌 없음

implicit 0.7.3 (2026-05, MIT) · cornac 3.0.0 (2026-08-27, Apache-2.0) · scikit-surprise 1.1.5 (2026-05, BSD-3). 셋 다 py3.14 wheel 존재. **다만 cornac 3.0.0은 조사 기준일 하루 전 릴리스인 메이저 버전**이라 문서의 `readthedocs/stable` 링크와 동작이 다를 수 있다 — 버전 pin 필요.

### 2-3 `사실` ★ CF 라이브러리로 "topic → agent"를 하면 반대 결과가 나온다

topic × agent 행렬을 만들어 `implicit`의 BM25 item-item에 쿼리한 실험.

```
bob         coffee, espresso
carol       coffee, roasting, drip
dave        drip                    ← drip만 파는 전문가
erin        hiking, photo           ← 무관
generalist  전부 조금씩
```

| 쿼리 | implicit item-item | BM25 문서검색 |
|---|---|---|
| `coffee` | **generalist** 1.01 / bob .69 / carol .63 / erin .40 / dave .11 | **bob** .61 / carol .52 / generalist .37 / dave 0 / erin 0 |
| `drip` | **generalist** .82 / carol .64 / erin .40 / bob .29 / **dave .20 (꼴찌)** | **dave .73** / carol .52 / generalist .37 / bob 0 / erin 0 |
| `coffee+drip+espresso` | **generalist** 2.60 / bob 1.63 / … | **bob 1.60** / generalist 1.33 / carol 1.05 / dave .73 / erin 0 |

**원인**: `implicit`의 item-item은 **동시 출현 유사도**를 계산한다. 쿼리가 실제로 묻는 것은 "이 주제 보유자들과 *비슷한* 사람"이고, `generalist`는 모두와 함께 나타나므로 항상 이긴다. 교과서적 인기도 편향.

**올바른 정식화**는 검색이다 — 문서=agent(보유 주제 묶음), 단어=topic_id, 쿼리=확정 주제들. 그러면 세 성질이 저절로 성립한다: 미보유자 정확히 0점 · IDF가 희소 주제 전문가를 올림 · 길이 정규화가 generalist를 누름.

★ **규율: 라이브러리 함수 이름이 같아도 계산이 같지 않다.** `implicit`의 BM25는 *CF 가중치*이고 정보검색의 BM25가 아니다. (fixture는 5명짜리 장난감이므로 이것은 **품질 증명이 아니라 메커니즘 대조**다.)

---

## §3 판단 — surface 둘은 서로 다른 문제다

```
surface 2   쿼리가 없다 → recsys 라이브러리의 메인 API 그 자체 (recommend(user, N))
surface 1   쿼리가 있다 → 추천 라이브러리는 쿼리를 받지 않는다
```

이 구분을 놓치면 두 방향으로 틀린다. surface 2를 surface 1에 맞추면 recsys를 못 쓰고, surface 1을 surface 2에 맞추면 쿼리가 무시된다.

`판단` **surface 1의 요구(요청별 권한 필터·임의 allowlist·쿼리 처리)를 surface 2에 적용해 왔던 것이 이 논의 이전 판단의 주된 오류다.**

---

## §4 판단 — surface 2는 Gorse가 맞다

```
이벤트 → Gorse에 user/item/feedback 적재
surface 2 → GET /api/recommend/{user}?n=20 → 본인·차단 후처리 제거 → 응답
```

우리가 만드는 것: 동기화 워커 · 얇은 클라이언트 · 후처리 필터.
우리가 **안** 만드는 것: 학습 스케줄러 · 모델 저장/교체 · 서빙 API · 캐시 · 대시보드.

### 4-1 `판단` 기존 기각 사유 셋 중 둘은 surface 2에서 성립하지 않는다

| 기각 사유 | 재평가 |
|---|---|
| 요청자별 공개 범위를 표현 못 함 | **surface 1의 요구였다.** surface 2 근거를 `public` 보유로 한정하면 요청자별 변동이 없다. 그리고 물어보지도 않은 사용자에게 친구 공개 전문성을 근거로 대는 것은 surface 2에서 오히려 부적절할 수 있다 → **제품 결정 문제이지 도구 한계가 아니다** |
| 1억 사용자 캐시 카디널리티 | **출시 전 제품에 미래 규모를 현재 제약으로 적용한 것.** scoring design §9-2가 대규모 스택을 기각할 때 쓴 논거를 반대 방향으로 잘못 적용했다 |
| 노출 스키마를 엔진이 소유 | **사실이지만 과장했다.** Gorse feedback(=Gorse의 학습 입력)과 우리 노출 스트림(=우리의 측정)은 병존 가능하다. 잃는 것은 Gorse 자신의 순서를 재현·오프폴리시 평가하는 능력이고, 이는 **분석 능력의 손실이지 출시 블로커가 아니다** |

★ **규율: 미래 규모를 현재 결정의 기각 사유로 쓰지 않는다.** 같은 문서가 다른 절에서 정확히 이 실수를 경계했는데, 반대 방향으로 반복됐다.

---

## §5 판단 — surface 1은 Gorse로 하면 원칙에 걸린다

기술적으로는 가능하다. agent `Categories`에 보유 topic id(상위 topic 포함)를 넣고 `?category=`로 부르면 된다. 계층도 인덱스에 넣으면 처리된다.

문제는 **답하는 질문이 다르다**는 것.

```
Gorse category    "이 분류 안에서 이 사용자가 좋아할 사람" → 이력 없으면 분류 내 인기순
surface 1이 원하는 것  "이 주제를 잘 아는 사람"
```

§2-3의 실험이 보여준 그대로다. 그리고 이것은 잠근 제품 원칙과 정면으로 걸린다 —
**"첫날 항으로 인기도는 쓰지 않는다. 첫날의 인기도는 실력의 대리가 아니라 우리가 누구를 먼저 보여줬는지의 대리다."**

`판단` **surface 2에서는 이 원칙이 걸리지 않는다.** surface 2는 선호 기반이 목적이므로 "이 사람이 좋아할 만한"이 정답이다. 원칙은 surface 1에만 걸린다.

### 5-1 `판단` surface 1을 검색으로 정식화하면 따라오는 것들

| 지금 | 이후 |
|---|---|
| query expansion + topic-api 카탈로그 검색 | **그대로 유지** |
| topic-api `/topics/{id}/users` 보유자 랭킹 | 사용 안 함 |
| RRF concept group 융합 | BM25 다중 쿼리어 (IDF 가중이라 RRF보다 정확 — 드문 개념에서 걸린 것이 더 강한 근거) |
| 계층 순회 · decay 0.6 · depth 3 (요청마다) | **인덱스 시점 확장** (상위 topic을 감쇠 값으로 문서에 포함) |
| 정확한 top-k 증명 (최악 2,000회 개인별 조회) | 불필요 |
| gate 후 걸러내기 | 검색 조건 (allowlist가 규율이 아니라 스키마 속성이 됨) |
| 보유 강도 | BM25의 tf 자리. **tf 포화가 이미 내장** (§3-3⒜에서 직접 설계하려던 것) |

`판단` topic-api에 남는 역할은 셋 — ① 카탈로그 검색(grounding) ② 라벨 ③ 보유 데이터(인덱스 재료).
③은 **요청마다가 아니라 배치 덤프**로 충분하므로, 실시간 변경 스트림보다 훨씬 작은 요청이다.
대가는 공개 범위가 하루 늦는 것 → 인덱스는 `public`만 담고 최종 N명은 원본 재확인.

---

## §5-2 `사실` Gorse 실측 — v0.5.11 컨테이너로 직접 검증 (2026-09-01)

`zhenghaoz/gorse-in-one` v0.5.11에 agent/topic item을 넣고 `tags` 방식 item-to-item을 설정해 실행한 결과.
fixture는 12~19개 item의 합성 데이터이므로 **품질 측정이 아니라 기능 유무·방향 확인**이다.

### 되는 것

| # | 확인된 것 | 증거 |
|---|---|---|
| G1 | **숨긴 item도 이웃이 계산되고 서빙된다** (문서에 없던 답) | `topic:coffee`(IsHidden=true)와 `topic:coffee-visible`의 이웃 결과가 완전히 동일 |
| G2 | **`category` 필터는 topic 후보를 정확히 좁힌다** | agent `Categories`에 topic id를 넣으면 `?category=coffee`가 coffee 보유자만 반환 |
| G3 | `user-id` 파라미터로 **이력 있는 후보를 제외**할 수 있다 | `?user-id=alice` → alice가 `talked`한 bob·carol이 사라짐. self-exclusion을 합성 피드백으로 구현할 여지 |
| G4 | item-to-item 레코멘더를 **이름별로 여러 개** 정의할 수 있다 | `[[recommend.item-to-item]]`이 배열. `tags`/`embedding`/`users` 세 type |

### 안 되는 것

| # | 확인된 것 | 증거 |
|---|---|---|
| **G5** | **순위를 인기도가 지배한다** | `gen`(6주제 보유·피드백 12건)이 `drip` 쿼리에서 `dave`(drip 전담·피드백 0건)를 이김. coffee 쿼리에서도 gen이 1위 |
| **G6** | **쿼리와 태그 집합이 동일한 item은 이웃에서 제외된다** | `topic:tea`({tea}) 이웃에 `twinA`/`twinB`({tea}) 부재. `twinA` 이웃에도 `twinB` 부재. → **그 주제의 가장 순수한 전문가가 체계적으로 탈락한다** |
| G7 | 이웃 결과에 **미보유자가 0.5 기본값으로 섞인다** | `topic:coffee` 이웃에 `erin`(hiking/photo) 포함. 보유/미보유가 깔끔히 갈리지 않음 |
| **G8** | **깊이를 객체로 넣을 수 없다** | `{"topics":{"coffee":5}}` → 저장은 되지만 태그로 읽히지 않음. 항상 0.5 기본값 |
| **G9** | **깊이를 반복으로 넣을 수 없다** | `["coffee"×5]`와 `["coffee"]`의 점수가 동일(0.500533) |
| **G10** | **깊이를 별도 태그로 넣으면 역방향으로 작동한다** | `topic:tea-L5` 쿼리에서 깊이 태그가 **없는** `twinA`/`twinB`(0.501376)가 `tea-L1`(0.500691)보다 위. 순서가 깊이가 아니라 태그 희소성의 부산물 |
| G11 | `POST /api/session/recommend`는 이 빌드에서 항상 `null` | 숨긴/보이는 더미 topic, 일반 agent, 복수 item, feedback type 변경 — 모든 조합에서 200 + `null` |
| G12 | 판별 폭이 매우 좁다 | 매칭 점수가 전부 `0.5 + O(10⁻³)`. 랭커가 위에 얹히면 사실상 평평 |

### 운영상 확인

- `--playground`는 **재시작마다 설정 파일을 재생성하고 114MB를 다시 임포트**한다. 설정을 바꾸려면
  playground 없이 띄우고 `/etc/gorse/config.toml`을 넣어야 한다. (첫 컨테이너가 이것 때문에 죽었다.)
- topic을 `Categories`로 쓰면 **category 카디널리티 = topic 수**이고, 개인화 결과는 (user × category)로
  미리 계산돼 캐시된다.
- 전체 오프라인 파이프라인은 소규모에서 3초 내. 재계산은 재시작으로 트리거된다.

### 판정

```
Route A  ?category=<topic>            후보 집합은 정확  →  순서가 인기도 (G2 + G5)
Route C  더미 topic item 의 이웃       착상은 맞으나     →  최고 전문가 탈락 (G6) + 미보유자 혼입 (G7)
깊이                                                    →  세 인코딩 모두 실패 (G8·G9·G10)
```

`판단` **Gorse는 "이 topic의 후보 집합"은 정확히 만들 수 있지만, 그것을 전문성으로 정렬할 수 없고
깊이를 표현할 수단이 아예 없다.** G5는 잠근 원칙("첫날 항으로 인기도를 쓰지 않는다")과 정면으로 걸리고,
G8–G10은 설계의 중심 값을 표현할 자리가 없다는 뜻이다.

`판단` 다만 **G2는 살릴 수 있다** — Gorse를 surface 1의 *후보 생성기*로 쓰고 순위는 우리가 매기는 형태.
그러면 surface 2는 Gorse가 통째로, surface 1은 Gorse가 후보만, 정렬은 우리 랭커가 된다.
이 경우 §5-1의 검색 정식화(BM25)와 비교 대상이 되는 것은 "Gorse category 후보 + 우리 정렬"이다.

★ **규율: 문서에 없는 동작은 문서가 아니라 실행이 답한다.** G1(숨긴 item의 이웃)·G6(동일 태그 제외)·
G8–G10(깊이 인코딩)은 모두 공식 문서에 서술이 없고, G6는 사양서를 읽어서는 예측할 수 없는 동작이면서
이 용도에서는 치명적이다.

---

## §5-3 `사실` 라벨별 개별 쿼리 + 가중 합산 — 실측 (2026-09-01)

전제 두 개가 확정되면서 §5-2의 판정이 바뀐다.

1. `사실` **하위 라벨 보유는 상위 라벨 보유를 함의한다** (제품 사실)
2. 쿼리 1회 오버헤드가 작다면 라벨마다 따로 쿼리하고 가중 합산할 수 있다

깨끗한 v0.5.11 인스턴스에 계층 fixture(`coffee ⊃ {drip, espresso, roasting, latte}`)를 넣고 검증했다.

### H1 `사실` G6(동일 태그 제외)이 두 가지로 해소된다

| 쿼리 | 센티넬 없음 | 센티넬 있음 |
|---|---|---|
| `coffee` (상위) | `parentonly` **누락** | 누락 없음 |
| `drip` (하위) | 누락 없음 | 누락 없음 |

- **하위 라벨 쿼리는 원래 안 터진다.** 함의 때문에 `drip` 보유자는 항상 `{coffee, drip}` 이상이라
  단일 라벨 쿼리 item과 태그 집합이 같아질 수 없다.
- **상위 라벨 쿼리는 터지는데, 쿼리 item에 아무 agent도 갖지 않는 센티넬 태그(`__QUERY__`) 하나를
  넣으면 완전히 막힌다.** 센티넬은 어떤 후보와도 매칭되지 않으므로 분자에 기여가 0이고, 분모는
  후보별 상수라 순서에 영향이 없다.

### H2 `사실` 라벨별 원점수는 "매칭 여부 × 1/‖d‖" 구조다

0.5 기준 초과분, ×10⁻³:

| | coffee | drip | espresso | roasting | latte |
|---|---|---|---|---|---|
| broad (5개 보유) | 0.606 | 0.693 | 0.789 | 0.898 | 0.898 |
| partial (3개) | 0.843 | 0.964 | 1.098 | — | — |
| narrow (2개) | 1.081 | 1.236 | — | — | — |
| parentonly (1개) | **1.601** | — | — | — | — |
| generalist (12개) | 0.302 | 0.345 | 0.393 | 0.448 | 0.448 |
| unrelated | — | — | — | — | — |

**한 라벨만 보면 항상 보유 폭이 좁은 쪽이 높다**(길이 정규화). 변별은 *몇 개의 라벨에서 매칭되는가*에서
나오고, 그래서 개별 쿼리를 합산해야 의미가 생긴다.

### H3 `사실` 가중치가 순서를 실제로 통제한다

| 가중 | 결과 순서 |
|---|---|
| ① 균등 (상위1·하위1×4) | **broad 3.88** > partial 2.90 > narrow 2.32 > generalist 1.94 > parentonly 1.60 |
| ② 하위 강조 (상위1·하위3) | broad 10.44 > partial 7.03 > **generalist 5.21** > narrow 4.79 |
| ③ 상위 강조 (상위4·하위1) | **parentonly 6.40** > broad 5.70 > narrow 5.56 > partial 5.43 |
| ④ 좁은 쿼리 (coffee1·drip5) | **narrow 7.26** > partial 5.66 > broad 4.07 > generalist 2.03 |

①이 §5-2의 단일 결합 쿼리와 정면 대조된다 — 결합 쿼리는 `broad`를 제외하고 `generalist`를 1등으로 냈다.
**개별 쿼리 + 균등 가중은 `broad`를 1등, `generalist`를 4등으로 낸다.**

④는 좁은 쿼리에서 좁고 깊은 전문가가 1등이 됨을 보인다 — 쿼리 구체성이 보존된다.

### H4 `사실` 오버헤드는 무시할 만하다

호출당 **약 1.1 ms** (순차 10회 = 10.7 ms). 병렬화하면 더 짧다. 확장이 라벨 10개를 내도 문제없다.

### H5 `판단` ★ 상위 라벨은 랭킹 신호가 아니라 recall 장치다

③이 근거다. 상위 가중을 올리면 **라벨 하나만 달린 가장 얕은 보유자가 1등**이 된다.

이유는 전제 1이다 — 하위가 상위를 함의하므로 **subtree 안 모든 사람이 상위 라벨을 갖는다.** 그러면
상위 라벨의 IDF가 낮아 변별력이 없고, 남는 것은 `1/‖d‖`뿐이라 "보유가 적은 사람"을 고르게 된다.

```
상위 라벨 쿼리   →  subtree 전체를 긁어오는 recall 장치.  랭킹 가중은 낮게(≈0)
하위 라벨 쿼리   →  실제 변별.  가중은 여기에
```

### H6 `판단` 남는 제약 셋

1. **신선도.** 이웃은 오프라인 계산·캐시다. 새 보유·깊이 변경은 다음 파이프라인 실행까지 안 보인다.
   이벤트를 실시간으로 받아도 결과에 즉시 반영되지 않는다 — 이 구조의 핵심 한계이고 해소되지 않는다.
2. **깊이는 여전히 못 들어간다**(§5-2 G8–G10). 이 방식이 주는 것은 **coverage**이고, 깊이는 rerank
   단계에서 우리 데이터로 넣어야 한다.
3. **focus 항이 없다.** ②에서 `generalist`가 `narrow`를 앞선다. "그 사람 보유 전체 중 이 subtree의
   비중"은 Gorse가 주지 않으므로 rerank에서 계산한다.

### H7 `판단` 그래서 성립하는 형태

```
grounding (우리)  →  확정 topic + 그 하위 라벨들
      ↓
라벨마다 Gorse item-to-item 쿼리 (센티넬 포함 더미 topic item)   ~1ms × N
      ↓
가중 합산 — 상위≈0, 하위에 가중                    ← 가중치가 우리 것이 된다
      ↓
후보 집합 + coverage 점수
      ↓
rerank (우리):  coverage + depth + focus + 언어 + 활동성 + 권한        ← 깊이가 여기 들어간다
```

`판단` 이 형태에서 Gorse는 **IDF·길이정규화를 갖춘 topic별 역색인**으로 쓰이는 것이고, 검색 엔진을
따로 도입하지 않고 §5-1의 정식화에 도달하는 경로다. 얻는 것은 인덱스 운영(적재·학습·캐시·API)을 안
만들어도 되는 것이고, 대가는 H6-1의 신선도다.

★ **규율: 도구의 결함은 입력의 모양을 바꿔서 우회될 수 있다.** G6(동일 태그 제외)은 §5-2에서 치명적
결함으로 판정했지만, 쿼리를 라벨 단위로 쪼개고 센티넬을 넣자 사라졌다. **결함을 발견한 시점의 사용
방식이 유일한 사용 방식이 아니다.**

---

## §5-4 `사실` 신선도 — 실측 (2026-09-01)

§5-3 H6-1에서 "우회로가 없는 유일한 제약"으로 남긴 항목을 실제로 쟀다.

### F1 `사실` 파이프라인 주기와 이웃 갱신 주기는 다른 것이다

`fit_period = "1m"`로 두면 오프라인 파이프라인은 **60초마다 실행된다**(작업 StartTime이 03:32:51,
03:33:51, 03:34:51 … 로 정확히 1분 간격). 그런데:

| 시도 | 결과 |
|---|---|
| 신규 agent 삽입 후 파이프라인 6회 실행 대기 (340s) | 기존 쿼리 item의 이웃 목록에 **등장하지 않음** |
| 쿼리 item을 다시 POST 해서 "변경"으로 만듦 (touch) | **갱신되지 않음** (200s 대기) |
| 컨테이너 재시작 | **전면 재계산.** 신규 item 전부 등장 |
| `[recommend] cache_expire = "2m"` 로 낮춤 | **120.2초 만에 반영** |

`사실` **이웃 목록의 갱신 주기를 지배하는 것은 `[recommend] cache_expire`다.** 기본값은 `72h`이고,
파이프라인이 아무리 자주 돌아도 이 값이 만료되기 전에는 기존 item의 이웃 목록이 다시 계산되지 않는다.
신규 item은 *자기 자신의* 이웃 목록만 즉시 얻는다.

★ **이것이 §5-3에서 "우회로가 없다"고 쓴 제약의 정체다. 우회로는 있다 — 설정값 하나다.**

### F2 `사실` 중복 태그 집합은 접히지 않는다 — 이전 가설 철회

`a:fresh1`이 `a:narrow`와 태그가 같아서 결과에 안 나오는 것으로 보였고, "Gorse가 동일 라벨 item을
중복으로 접는다"고 추정했다. **틀렸다.** 완전히 같은 태그 집합을 가진 `a:dupA`·`a:dupB`를 새로 넣고
재시작하니 **둘 다 등장한다.** 안 보였던 진짜 이유는 F1(캐시 미갱신)이었다.

★ **규율: 같은 증상에 두 원인 후보가 있으면 값싼 쪽(캐시)을 먼저 배제한다.** 알고리즘의 성질이라고
결론내면 설계를 바꾸게 되고, 캐시 설정이면 값 하나를 바꾸면 된다. 나는 반대 순서로 추정했다.

### F3 `사실` 재계산 비용 ≈ item당 10 ms, 2,000개까지 선형

`item-to-item` 작업 소요를 카탈로그 크기별로 측정. n_jobs=4, 단일 컨테이너.

| 태그 분포 | 21 | 500 | 2,000 |
|---|---|---|---|
| 희소 (토픽 2,000개 중 2~8개) | 1.7s | 6.1s | 20.2s |
| **계층 편중** (상위 8개 중 1개 + 하위 1~4개, 함의 구조) | 1.7s | 5.5s | 20.7s |

한계 비용은 양쪽 모두 **약 10 ms/item**이고 이 구간에서 **선형**이다. 상위 라벨을 다수가 공유하는
편중 구조가 비용을 키우지 않았다.

`열린 항목` **2,000개 너머는 미검증이다.** 태그 유사도의 후보 생성은 흔한 태그 하나 안에서
보유자 수에 이차적일 수 있고, 이 실험은 상위 라벨당 보유자가 250명인 지점까지만 갔다.
실제 규모에서 다시 재야 한다.

### F4 `판단` 신선도 공식과 그 함의

```
반영 지연 = max( cache_expire,  전면 재계산 시간 )
전면 재계산 시간 ≈ 10 ms × 전체 item 수     (이 하드웨어 기준, 2k까지 선형)
```

선형이 유지된다는 가정 하의 대략치 — **1만 = 약 100초 · 10만 = 약 17분 · 100만 = 약 3시간.**
증분 갱신은 없다. 매번 전면 재계산이다.

| 무엇의 신선도인가 | 판정 |
|---|---|
| **agent가 새 topic을 배움 / 깊이가 바뀜** | 감당 가능. 대화로 관심사가 쌓이는 제품에서 분~십수분 지연은 문제가 아니다 |
| **보유를 비공개로 전환 / 차단 / 삭제** | **여기에 의존하면 안 된다.** 이것은 보안 freshness이고, §5-3의 형태대로 최종 N명을 서빙 시점에 원본으로 재확인해서 해결한다 |

`판단` **신선도는 채택을 막는 요인이 아니다.** 대가는 "전면 재계산이라 카탈로그 크기에 비례한다"는
것이고, 이는 `cache_expire`를 규모에 맞춰 정하는 운영 파라미터 문제로 환원된다. 다만 F3의 `열린 항목`
때문에 **목표 규모에서 재계산 시간을 실측하기 전에는 `cache_expire` 하한을 약속할 수 없다.**

---

## §5-5 `판단` 이 구조가 요구하는 이벤트 계약

> ⚠ **이 절의 `E1`–`E6`은 이 논의 시점의 스냅샷이고 현재 계약이 아니다**(2026-09-03 표시).
> 살아 있는 레지스터는 [`inbound_event_contract.md` §6](inbound_event_contract.md)의 **`EVT-E1`–`EVT-E6`**이고
> 내용이 다르다 — 여기 `E1`은 `request_id`, 그쪽 `EVT-E1`은 `decision_id`다.
> 결정은 [`decisions.md`](decisions.md)가 소유한다.

§5-3의 형태로 간다고 할 때 **받아야 할 이벤트와 그 페이로드**. 아직 어느 팀과도 협의하지 않았다.

### 원칙 넷

**⓵ 델타가 아니라 현재 전체 집합을 받는다.** "topic 추가됨/삭제됨"이 아니라 "이 agent의 현재 공개
topic 집합은 이것"이다. 우리는 복제본을 유지하므로 델타는 완벽한 순서 보장을 요구하고, 한 건만 유실돼도
영구히 어긋난다. 전체 집합 + `revision`이면 **순서는 최대값 채택으로 끝나고 중복은 무해**하다.

**⓶ 공개 프로젝션만 받는다.** `friends`·`private` 보유는 애초에 받지 않는다. 그러면 "공개 → 비공개 전환"이
**집합에서 빠지는 것**으로 도착하고, 우리 쪽에 민감 데이터가 없으므로 유출 표면 자체가 없다.
받지 않는 것이 가장 강한 보호다.

**⓷ 보안 이벤트와 품질 이벤트의 지연 허용치를 분리한다.** 둘을 같은 파이프라인 지연에 묶으면
가장 느슨한 쪽이 기준이 된다.

**⓸ 귀속 키를 처음부터 넣는다.** 피드백에 `request_id`가 없으면 "무엇을 보여줬기 때문에 이 대화가
생겼는가"를 영구히 알 수 없다. §6의 노출 로그와 잇는 유일한 키이고, **나중에 소급 불가**하다.

### A. 카탈로그 이벤트 (topic-api / producer)

우리가 topic별 더미 쿼리 item을 유지해야 하므로 필요하다.

```json
{
  "event_id": "…", "event_type": "topic.changed", "occurred_at": "…",
  "topic_id": "Q12345", "revision": 8,
  "labels": {"ko": "커피", "en": "Coffee"},
  "parent_topic_ids": ["Q999"],
  "status": "active | merged | deleted",
  "merged_into_topic_id": null
}
```

| 필드 | 왜 필요한가 |
|---|---|
| `labels` | 응답 표시 + 사용자가 보는 라벨 (이 구조에서 topic-api의 주 역할) |
| `parent_topic_ids` | 상위 쿼리를 recall 장치로 쓰려면 계층을 알아야 한다(§5-3 H5) |
| `status` / `merged_into` | **topic은 병합·분할·개명된다.** 더미 쿼리 item과 우리 인덱스의 topic_id가 함께 이동해야 한다 |

`열린 항목` 병합 시 과거 노출 로그의 `topic_id`를 재작성할지, 원본 유지 + 매핑 테이블로 둘지.
후자가 맞아 보인다 — 로그는 그때의 사실이다.

### B. 보유 이벤트 (agent가 배운 것) — **이 구조의 본체**

```json
{
  "event_id": "…", "event_type": "agent.public_topics.changed", "occurred_at": "…",
  "personal_agent_id": "…", "owner_user_id": "…",
  "revision": 47,
  "public_topics": [
    {"topic_id": "Q12345", "depth": 3.2,
     "first_seen_at": "…", "last_reinforced_at": "…"}
  ]
}
```

| 필드 | 어디로 | 왜 |
|---|---|---|
| `public_topics[].topic_id` | **Gorse Item `Labels.topics`** | 후보 생성의 전부 |
| `public_topics[].depth` | **우리 store** | Gorse가 못 읽는다(§5-2 G8–G10). rerank 입력 |
| `last_reinforced_at` | 우리 store | 최근성 감쇠 |
| `owner_user_id` | 우리 store | self-exclusion · 응답 hydration |
| `revision` | — | 순서·멱등 |

`판단` **`depth`의 형식은 우리가 정하지 않는다.** 상류가 쓰는 값을 그대로 받고, 정규화·포화는 우리
feature 추출 함수에서 한다. 상류의 척도가 바뀌면 `depth_scale_version`이 필요하다.

### C. 자격·상태 이벤트

```json
{
  "event_id": "…", "event_type": "agent.status.changed", "occurred_at": "…",
  "personal_agent_id": "…", "owner_user_id": "…", "revision": 12,
  "status": "active | suspended | deleted",
  "recommendable": true,
  "languages": ["ko", "en"],
  "last_active_at": "…"
}
```

| 필드 | 비고 |
|---|---|
| `status=deleted` | **Gorse `IsHidden=true` + 우리 인덱스에서 제거.** 보안·법적 지연 허용치 |
| `recommendable` | §9 열린 항목이었던 **"추천 대상이 되는 것에 대한 동의"**. topic 공개와 다른 축이다 |
| `languages` · `last_active_at` | rerank 신호 |

그리고 사용자 축:

```json
{"event_type": "user.created", "user_id": "…", "occurred_at": "…"}
{"event_type": "user.deleted", "user_id": "…", "occurred_at": "…"}
{"event_type": "user.blocked", "requester_user_id": "…", "blocked_user_id": "…", "active": true}
```

`user.deleted`는 **Gorse User·Feedback·우리 인덱스·이벤트 스트림 네 곳**에 전파되어야 한다.

### D. 행동 이벤트 (피드백 퍼널)

```json
{
  "event_id": "…", "event_type": "conversation.started", "occurred_at": "…",
  "request_id": "…",                    // ★ 우리 노출 로그와 잇는 유일한 키
  "requester_user_id": "…", "personal_agent_id": "…",
  "surface": "topic_search | home",
  "topic_id": "Q12345"                  // 어떤 맥락에서 시작됐나
}
```

퍼널 단계별로: `card.selected` · `conversation.started` · `conversation.depth_reached` ·
`resolved` · `requery.other_agent`.

| 어디로 | 무엇 |
|---|---|
| **Gorse Feedback** | `(requester_user_id, personal_agent_id, type, timestamp)` — surface 2의 CF 학습 |
| **우리 이벤트 스트림** | 위 전부 + `request_id` + `surface` + `topic_id` |

`판단` **Gorse에 보내는 것은 프로젝션이고 원본이 아니다.** Gorse 스키마에는 `request_id`·`position`·
`propensity`를 담을 자리가 없다(§4-1). 원본은 우리가 append-only로 갖고, Gorse에는 학습용 프로젝션만 보낸다.

### 지연 허용치 — 두 등급

| 등급 | 이벤트 | 허용 지연 | 근거 |
|---|---|---|---|
| **보안** | `status=deleted` · `recommendable=false` · `user.blocked` · 공개 집합에서 빠짐 | **초 단위, 그리고 서빙 시점 재확인이 필수** | §5-4 F4. Gorse 캐시(`cache_expire`)에 의존하면 안 된다 |
| **품질** | 새 topic 학습 · `depth` 변경 · `last_active_at` | 분~십수분 | §5-4 F4. `cache_expire`로 조절 |

★ 이 분리가 §5-4의 실측을 실제 계약으로 바꾸는 지점이다. 보안 등급을 캐시 갱신에 태우면
`cache_expire`가 곧 유출 창(window)이 된다.

### 우리가 받지 않고 만드는 것

| 무엇 | 왜 우리 것인가 |
|---|---|
| **노출 이벤트** (후보 전체·위치·노출확률·잘린 이유·surface·모델 버전·`request_id`) | 서빙이 만들어내는 값이다. 아무도 우리에게 줄 수 없다 |
| coverage / focus 점수 | 쿼리 시점 계산 |
| 최종 권한 판정 | §7-5 |

### 열린 항목

> ⚠ **논의 기록이지 레지스터가 아니다**(`decisions.md` §5 규칙 ④). 아래 `E1`–`E6`은 이 논의가
> 그때 세운 목록이고, **살아 있는 협의 레지스터는 `inbound_event_contract.md` §6의 `EVT-E1`–`EVT-E9`**다.
> 번호가 겹치되 뜻이 다르다 — 여기 `E1`은 `request_id`, `EVT-E1`은 `decision_id`다.

| # | 항목 | 상대 |
|---|---|---|
| E1 | `request_id`를 대화 이벤트에 실을 수 있는가 — **가장 중요하고 소급 불가** | bourbon-agent |
| E2 | 전체 집합(⓵) 방식 수용 가능한가, 아니면 델타만 나오는가 | 발행 측 |
| E3 | `depth`의 실제 형식·척도와 그 안정성 | agent 팀 |
| E4 | `recommendable` 동의 축이 존재하는가, 없다면 누가 만드나 | 기획 |
| E5 | 보안 등급 이벤트의 전달 보장 수준 (at-least-once + 서빙 재확인으로 충분한가) | 3자 |
| E6 | topic 병합·분할 시 과거 로그의 topic_id 처리 | topic-api |

---

## §6 판단 — 되돌릴 수 있게 만드는 장치 하나

**노출 기록만은 처음부터 우리 형식으로 남긴다.**

모델도 인덱스도 언제든 다시 만들 수 있지만, "지난달에 누구를 몇 번째로 왜 보여줬는지"는 그때 안 적으면 영구히 복구 불가다. 그리고 이것을 남겨 두면 **Gorse 채택이 되돌릴 수 있는 결정**이 된다.

비용은 몇십 줄. 이것이 "일단 사서 쓰자"를 안전하게 만드는 유일한 보험이다.

반대로 **지금 안 만들어도 되는 것**: 공유 feature 함수 · 탐색 슬롯 · 랭커 · Cornac · off-policy. 데이터가 생기고 구체적 불만이 나온 뒤에 만든다.

★ **규율: 성숙형을 먼저 그리면 출시가 막힌다.** 공유 feature 함수·노출 스키마·탐색 슬롯·오프폴리시·랭커가 모두 갖춰져야 뭔가 나오는 그림은, 피드백이 0인 제품에는 과한 비계다. 측정 야심은 **연구 능력**이고 출시 조건이 아니다.

---

## §7 조사 문서(`recsys_opensource/`)에 대한 판단

`판단` **위치를 옮길 필요는 없다.** 그 문서는 "기존 recsys를 이해하기 위한" 조사이고 그 역할로 잘 쓰였다. 실제 결함은 위치가 아니라 **연결**이다 — scoring design §9-2가 Gorse를 세 줄로 기각하는데 그 근거인 22KB가 옆 디렉토리에 링크 없이 있고, §9-1이 Cornac의 프로토콜 적합성을 `열린 항목`(Q6)으로 남겼는데 `cornac.md` §6이 그 질문을 이미 절반 답해 놓았다. 양방향 포인터가 답이다.

`판단` **새 아키텍처 문서를 쓰지 않는다.** 그 역할은 `recommendation_scoring_design.md`가 이미 한다. 새로 쓰면 세 번째 병렬 planning 문서가 된다.

`판단` **새로 쓸 값이 있는 자리는 §9가 채택한 도구 중 조사가 없는 것들**이다 — off-policy 추정기(로그 스키마를 강제하므로 N1의 선결), LR→GBDT 랭커(단조 제약을 어떤 구현이 어떻게 주는가).

### 7-1 조사 문서에 없고 어느 문서에도 없는 것

| 항목 | 내용 |
|---|---|
| **피드백 위조** | 이벤트 수신 라우트는 우리 서비스 최초의 inbound write다. internal prefix는 edge-auth를 건너뛰므로 body의 신원은 "주장"인데, 추천 요청에서는 그 주장이 self-exclusion에만 쓰이는 반면 **피드백에서는 학습 데이터가 된다.** 위협 모델 재검토 필요 — 지금은 배포 규모 질문(Q10)으로만 다뤄진다 |
| **공급 측 상한** | 추천 대상이 사람인데 대화 수용량이 무한하다고 가정하고 있다. §7-3 집중도는 이것을 *재지만* 상한으로 쓰지 않는다 |
| **추천 대상 동의** | gate는 privacy·safety·self 셋. "topic이 public"과 "모르는 사람에게 추천돼도 됨"은 다른 동의다 |
| **첫 라벨 부트스트랩** | Cornac도 off-policy도 라벨이 있어야 돈다. Q1 미협의 상태에서 D2·D5가 열리는 날짜가 없다. 파이프라인에 LLM이 이미 있으므로 LLM-judge relevance set이 후보 |
| **아티팩트 안의 삭제** | Q4는 이벤트 스트림 보존만 다룬다. 주간 재학습이면 삭제 요청이 모델 안에서 일주일 산다 |
| **재방문 구조** | S_engage·S_cohort는 (요청자, 후보) 쌍에 이력이 쌓인다고 전제한다. 사람 대 사람 상담에서 그 쌍이 재발생하지 않으면 구조적으로 희박하다. D5는 "positive 라벨 수"로 게이트하는데 필요한 것은 **쌍당 재방문율** |
| **목표가 한쪽인가** | §5 퍼널은 요청자의 해소만 본다. 추천된 사람의 만족은 목표인가 |

---

## §8 버린 논지 — 논의 중 철회한 것

| 논지 | 왜 버렸나 |
|---|---|
| "조사 문서가 쇼핑 목록을 문제 형태에서 유도하지 않았다" | 그 문서는 **선정서가 아니라 이해를 위한 조사**다. CF 계열을 CF 계열로 읽는 것이 그 역할에 맞다 |
| "LTR·off-policy·exploration이 빠졌다" | repo 수준에서는 **이미 결정돼 있었다** — scoring design §9-3(LR→GBDT) · §9-1(off-policy) · N1(탐색+propensity). survey만 보고 repo를 판단했다 |
| "가중합이 RRF의 논거와 충돌하므로 rank fusion을 다시 써야 한다" | 비교 불가능성이라는 **사실**은 남지만 답이 틀렸다. 올바른 답은 **점수를 항으로 더하지 말고 feature로 넣는 것**이고, §4의 Σwᵢ는 §9-3의 LR→GBDT와 같은 말이라 이미 그 방향이다 |
| "임베딩 two-tower로 통째로 갈아타자" | 이미 평가셋이 있는 grounding 자산(query expansion + topic-api 검색)을 버린다. 그 자산을 유지하면서 "topic → agent"만 교체하는 쪽이 낫다 |
| "Gorse는 기각" | **surface 2에서는 성립하지 않는다.** §4-1 |

---

## §9 열린 항목

> ⚠ **논의 기록이지 레지스터가 아니다**(`decisions.md` §5 규칙 ④). 아래 `R1`–`R7`은 이 논의의
> 목록이고 **`recsys_opensource/README.md` §13의 `SURV-R1`–`R11`과 번호가 겹치되 뜻이 다르다**
> (여기 R3=자손 분포, R4=BM25 품질 / SURV-R3·R4는 다른 것). 살아 있는 조사 레지스터는 그쪽이다.

| # | 항목 | 성격 | 상태 |
|---|---|---|---|
| **R1** | **surface 2가 `friends` 공개 보유까지 근거로 삼는가** | **제품 결정** | **미정. 이것이 Gorse 채택 가능 여부를 정한다** — `public`만이면 채택 가능, `friends`까지면 구조적으로 막힌다(전역 item 라벨에 요청자별 근거를 넣을 자리가 없고, 넘치게 받아 거르는 방법은 빼는 것만 되고 더하는 건 안 되므로 친구 공개 후보가 누구에게도 안 나온다) |
| R2 | surface 1이 `friends` 보유까지 포함하는가 | 제품 결정 | 미정. `public`만이면 배치 인덱스로 충분 |
| R3 | 확정 topic의 자손 유무 분포 | 측정 | 계측 없음. surface 1의 현 경로가 topic-api의 싼 갈래인지 비싼 갈래인지를 정한다 |
| R4 | 실제 보유 데이터에서 BM25 정식화의 품질 | 측정 | §2-3은 5명 fixture. 현 grounding 평가셋에 정답 agent를 붙이면 며칠 |
| R5 | 노출 기록 스키마 | 설계 | **가장 급함.** off-policy 추정기 선택이 이 스키마를 강제하므로 도구 조사 중 이것 하나만 지금 필요 |
| R6 | 이벤트 수신 라우트의 신뢰 모델 | 보안 | §7-1. 미착수 |
| R7 | surface 1·2의 피드백이 한 곳으로 모이는가 | 설계 | 두 surface가 다른 엔진을 쓰면 학습 루프가 갈라질 수 있다 |

---

## §10 이 문서가 하지 않은 것

- **코드 변경 없음.** 어떤 repo도 이것을 근거로 바뀌지 않았다.
- **문서 변경 없음.** `recsys_opensource/`의 코드 결함 2건은 논의 중 사용자가 직접 수정했다.
- **도구를 도입하지 않았다.** §4·§5는 판단이고 선택이 아니다.
- **어느 팀에도 발신하지 않았다.**
- **실제 데이터로 재보지 않았다.** §2-3은 합성 fixture 5명이고 메커니즘 대조까지만 유효하다.
- **R1을 답하지 않았다.** 그것이 §4의 전제이며, 답이 없으면 §4는 조건부 판단이다.
