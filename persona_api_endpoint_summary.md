# Persona API에 필요한 endpoint 요약 (agent-recommendation 팀 → persona 팀, 첫 공유)

우리가 하는 일: 유저가 주제를 지정하거나 대화 컨텍스트를 보내면, **그 주제를 잘 아는 / 직접 경험한 / 찬성·반대 의견을 가진 다른 유저의 agent를 추천**합니다. 그 재료가 persona 팀이 대화에서 추출하는 topic·statement·성격 데이터입니다.

아래는 필요한 endpoint 목록입니다. **route 경로·필드명은 전부 예시**이고 기존 규약에 맞춰 바꾸셔도 됩니다 — 우리가 지키고 싶은 것은 각 값의 *의미*입니다.

## 전체 흐름 — 한 번의 추천 요청이 이 API들을 어떻게 쓰나

유저가 "재택근무에 반대하는 사람과 얘기해보고 싶어"라고 요청했다고 하면:

```
[1] 후보 찾기 (병렬)
    ├─ topic 검색   POST /topics/search                     ...................... ①
    │   "재택근무를 아는 유저" — 우리가 LLM으로 검색어를 여러 개 만들어 검색
    └─ 발언 검색    POST /evidence/search                   ...................... ③
        "재택근무에 대해 주장을 말한 적 있는 유저" — 주제 지식 순위가 낮아도
        의견이 강한 유저를 놓치지 않기 위한 별도 경로 (본문 없이 좌표만)
    (후보가 너무 적으면: topic 관계로 인접 주제까지 확장    ...................... ⑤)

[2] 후보 합치기     POST /topics/batch                      ...................... ②
    각 경로의 후보를 합치고, 좌표만 있는 후보에 랭킹용 값
    (depth·evidence_count·경험 등)을 한 번의 호출로 채움

[3] 거르고 줄세우기  (우리 내부 — persona 호출 없음)
    근거량·일관성·최신성·경험으로 자격 미달 제거 후 순위 결정

[4] 찬반 판정       POST /statements/batch + /statements/search .................. ④
    살아남은 후보들의 실제 발언 본문을 가져와 LLM이
    "이 유저는 이 명제에 찬성인가 반대인가"를 그 자리에서 판정

[5] 응답            추천 agent 목록 + 추천 이유 (요약·부연만 — 발언 원문 인용 없음)

(추후) 성격 매칭    GET /owners/{id}/hexaco + batch          ...................... ⑦
    주제 없이 "대화가 잘 맞을 상대" 추천 — 별도 기능으로 나중에

(요청 밖) 상태 조회  GET /status (+ owner별 상태는 진단용)     ...................... ⑥
    추천 요청마다 부르는 것이 아니라, 배포 점검·문제 진단 용도
```

단계마다 성격이 다릅니다: **[1]은 넓고 얕게**(전체 유저에서 회수, 본문 없음), **[4]는 좁고 깊게**(확정된 소수 후보만, 본문 포함). 이 분리가 3번/4번이 별도 endpoint인 이유이고, 노출 표면과 비용을 함께 줄입니다. 각 단계는 **그 시점의 최신 값**을 읽습니다 — persona 데이터가 유저별로 상시 갱신되는 구조이므로 시점을 고정해 달라고 요청하지 않습니다. 대신 [1]에서 찾은 항목이 [2]/[4] 시점에 사라졌으면 그 항목만 `not_found`로 알려주시면 되고, 우리는 그 후보만 빼고 계속 갑니다 (아래 공통 규약 1).

---

## 1. Topic 검색 — `POST /topics/search`

**왜 필요한가.** 추천의 첫 단계 = "이 주제를 아는 유저 찾기". topic 이름이 유저마다 다르게 지어지므로("머신러닝"/"ML"/"기계학습"), 우리가 LLM으로 query를 여러 개 만들어 검색합니다. **전체 유저 대상(cross-user) 검색**이 필요합니다 — 유저별 조회만 있으면 유저 수만큼 호출해야 합니다. 검색 대상은 유저가 **공개로 설정한 topic만**입니다 (공통 규약 2).

**Request**

| 값 | 의미 |
|---|---|
| `queries[]` | 우리가 확장 생성한 검색어 여러 개. 하나라도 걸리면 hit (ANY-match) |
| `limit`, `per_owner_limit` | 전체 상한 / 한 유저가 결과를 도배하지 않게 하는 유저당 상한 |
| `owner_ids` (선택) | 지정하면 그 유저들로 한정, 없으면 전체 |
| `filters` (선택) | 아래 response 값들에 대한 **원시 필드 기준 필터** — 예: `min_evidence_count`, `min_depth`, `min_consistency`, `experience: "firsthand"`(직접 경험 있는 유저만), `evidence_last_seen_after`. **문턱값은 우리가 요청마다 정해서 보냅니다** — persona 쪽에 판정 정책을 요구하는 게 아니라 거르는 기능만 요청합니다 |

`filters`가 필요한 이유: 추천 목적별로 최소 요건이 다릅니다("직접 해본 사람", "근거가 n개 이상인 사람"). 필터 없이 top-100을 받으면 그중 요건 미달을 우리가 버린 뒤 실제 쓸 후보가 줄고, 101등 이하의 요건 충족 후보는 회수할 방법이 없습니다.

**Response** — 검색 hit 하나가 곧 "추천 후보"가 되도록, 랭킹에 필요한 값을 함께 실어주세요 (안 그러면 100건 검색 후 100번 재조회가 됩니다):

| 값 | 의미 |
|---|---|
| `owner_id`, `topic_id` | 후보의 식별자. **topic_id는 조용히 재발급되면 안 됩니다** — 병합·개명 시 id가 어떻게 되는지(승계/redirect 등)를 정해서 알려주세요. 우리가 추천 기록에 영구 저장합니다 |
| `name`, `description`, `lang` | 이름·설명·표기 언어. 화면 표시와 LLM 판정에 씁니다 — **타 유저에게 노출 가능하게 정제된 값만** |
| `depth` (0–1) | 이 유저가 이 주제를 얼마나 깊이 아는가 (발화의 구체성·정교함) |
| `consistency` (0–1) | **지금 유효한 주장들끼리 서로 모순이 없는가.** ⚠ "과거에 생각을 바꾼 적 있는가"가 아니어야 합니다 — 후자면 의견을 바꾼 유저가 추천에서 부당하게 배제됩니다 |
| `evidence_count` | 현재 유효한 근거의 수. **statement 개수가 아니라 원 발화 단위로 중복 제거한 수** — 추출기가 한 발화를 몇 조각으로 자르느냐에 값이 흔들리면 안 됩니다 |
| `evidence_last_seen` | **현재 유효한 근거들 중 가장 최근 발화 시각** = max(발화 시각). 근거가 삭제되면 값이 뒤로 가고, 전부 없어지면 null. 재추출 실패나 저장 시각으로 갱신되면 안 됩니다 |
| `experience` | 경험 신호. `firsthand`(직접 해봄)/`secondhand`(배워서 앎) **각각**에 대해 근거 수·구체성·최신 시각 — 한 유저가 둘 다 가질 수 있으므로 단일 라벨이 아니라 둘 다 |
| depth·consistency의 **신뢰도** | 위 점수를 얼마나 믿어도 되는지 — confidence 수치든 "판정 가능/근거 부족" 상태든 형태는 제안해 주세요. 그리고 **근거가 부족한 값은 `filters`에서 자동 탈락시키지 말고** 상태와 함께 내려주세요 — 대화가 적은 유저가 검색 단계에서 조용히 사라지면 안 됩니다 (제외가 필요하면 우리가 명시 옵션으로 요청) |
| `extraction_run_id`, `as_of` | 위 값들이 나온 추출 실행의 식별자·기준 시각 — depth는 지난주 값, count는 오늘 값이 섞이면 안 되므로 |

**검색 대상과 다국어.** 검색은 name만이 아니라 **name + alias + description 전체**를 대상으로 해주세요. 그리고 **다국어 검색 alias를 생성**해 주시길 요청합니다: 유저가 영어로만 대화해서 topic이 "coffee"로 저장됐어도, "커피"로 검색하면 찾아져야 합니다. 번역 생성은 name 단독이 아니라 **설명문까지 문맥으로** 해주세요 — "Java"처럼 이름만으로 뜻이 갈리는 topic이 잘못 번역됩니다. 생성된 alias에는 언어와 생성 모델 버전을 남겨주시고, 유저가 실제로 말한 표현(alias)과 시스템이 생성한 번역(search alias)은 구분해 주세요 — 노출 정책이 다릅니다.

## 2. Topic 일괄 조회 — `POST /topics/batch` (+ 단건 `GET .../topics/{topic_id}`)

**왜.** 아래 3번(evidence 검색)으로 찾은 후보는 좌표(owner, topic)만 있습니다. 그 후보들을 **한 번의 호출로** 1번과 같은 형태로 채워야 랭킹에 넣을 수 있습니다.

- Request: `keys: [{owner_id, topic_id}, ...]`
- Response: 1번과 **같은 필드**(최신 값). key별 결과 상태를 구분해 주세요: `ready` / `not_publishable`(비공개 전환됨) / `not_found`(그 사이 재추출 등으로 사라짐) / `not_extracted`(아직 추출 안 된 유저). 어느 경우든 우리는 **그 항목만 빼고 계속** 진행하니, 하나가 실패해도 전체가 죽지 않게만 해주세요.
- 단건 GET은 디버깅·상세 표시용 (aliases 등 포함한 상세형).

## 3. Evidence(발언) 후보 검색 — `POST /evidence/search`

**왜.** "이 주장에 찬성/반대하는 유저"를 찾으려면 topic 이름 검색만으로는 부족합니다 — 주제 지식 순위는 낮아도 그 명제에 강한 의견을 가진 유저가 있습니다. 그래서 **발언 자체를 검색해 후보를 찾는 경로**가 따로 필요합니다. 이 단계에서는 **발언 본문이 필요 없습니다** — 누가·어느 topic에서·어떤 종류의 발언을 했는지만.

- Request: `queries[]`(의견 어휘들), `kinds[]`(주장/경험담 등 종류 필터), `limit`, `per_owner_limit`
- Response: `[{ statement_id, owner_id, topic_id, kind, said_at }]` — **본문 없음**
- 검색 대상은 **공개된 topic에 속한 발언만**입니다 — topic 검색과 같은 경계 (공통 규약 2).

| 값 | 의미 |
|---|---|
| `statement_id` | **불변·비가역 식별자** (원 대화 message id가 아니고, 역산도 불가). 이것으로 4번에서 본문을 정확히 가져오고, 추천 기록에 "어떤 근거로 판정했나"를 남깁니다. 추천 기록 용도 때문에 **재추출 후에도 같은 발언은 같은 id를 유지**해 주시길 요청합니다 (어렵다면 감사용 논리 id를 따로 — 함께 정하고 싶은 것 5) |
| `kind` | 발언의 종류 (주장/선호/경험담 등 — enum 값과 의미를 문서로) |
| `said_at` | **유저가 실제로 말한 시각** (저장·재저장 시각 아님) |

## 4. 발언 본문 조회 — `POST /statements/batch` + `POST /statements/search`

**왜.** 찬성/반대 판정은 LLM이 실제 발언을 읽고 합니다. 3번에서 찾은 발언을 **id로 정확히** 가져오는 batch와, 확정된 후보 유저들에 대해 추가 발언을 찾는 scoped 검색 둘 다 필요합니다.

- `statements/batch`: Request `statement_ids[]` → 본문 포함 응답. 사라진 id는 항목별 `not_found`로 주시면 우리가 그 항목만 빼고 진행합니다. 다만 **재추출마다 id가 전부 바뀌면 우리 추천 기록의 근거 참조가 무의미해지므로**, 같은 발언은 재추출 후에도 같은 id이길 요청합니다 (3번 표 참조).
- `statements/search`: Request `scopes: [{owner_id, topic_ids[]}]`(**이 범위 밖 검색 없음** — 후보 유저로 한정) + `queries[]` + `per_owner_limit` → owner별 발언 목록.

**본문(`text`)의 의미.** 발언 본문은 저희 내부 판정(LLM)에만 쓰이고 **최종 유저에게는 노출되지 않습니다** — 추천 이유는 요약 수준이고 원문 인용은 하지 않습니다. 그래서 노출용 가공까지는 필요 없고 세 가지만 부탁드립니다: ① 원 대화 전문이 아니라 **추출된 근거문**이면 충분합니다 — 원 대화의 message id·위치 정보는 저희에게 필요 없는 데이터이니 응답에 넣지 말아 주세요. ② 추출·요약 시 **주장의 방향과 강도는 보존**돼야 합니다 — 중립화로 찬반이 뭉개지면 판정 자체가 불가능합니다. ③ 화면에 안 나가도 서비스 경계를 넘고 LLM 입력이 되는 데이터이므로, **제3자 실명·연락처 같은 명백한 개인정보·비밀정보는 추출 단계에서 빼 주세요** — 저희도 발언 본문을 로그나 추천 기록에 저장하지 않습니다(기록은 id와 판정 결과만).

## 5. Topic 관계 조회

**왜.** 두 용도: ① "이 주제의 여러 갈래를 고루 아는 사람들" 구성(하위 주제 관계), ② 해당 주제를 아는 유저가 너무 적을 때 인접 주제로 후보 확장(연관 관계).

- 최소 관계 2종: `subtopic_of`(하위 주제) / `related`(인접 주제) + 관계별 confidence.
- "인접 topic을 아는 다른 유저"를 구할 수 있으면 형태는 맡기겠습니다 (1번 검색에 얹어도, 별도 endpoint여도 좋습니다).

## 6. 상태 조회 — `GET /status` (+ owner별 상태는 진단용)

**왜.** 겪었던 실제 사고: 상류 데이터가 아직 없는 상태가 "빈 검색 결과"로 내려와서, 호출자에게 "당신 요청이 잘못됐다(422)"로 잘못 귀속됐습니다. 서비스 수준의 **"결과가 0개"와 "아직 준비가 안 됨"이 응답에서 구분**돼야 합니다. 둘 다 추천 요청마다 부르는 것이 아닙니다 — `GET /status`는 배포 점검·운영 관측용, owner별 상태는 지원·디버깅용입니다.

- `GET /status`: 추출 파이프라인 상태, 데이터 종류별(topic/statement/성격) 준비 상태, 커버된 유저 수와 **공개 topic 수**(기본 비공개 모델에서 추천 pool 크기의 관측치), 어느 시점 대화까지 반영됐는지.
- owner별 상태(**진단용 — 추천 처리에서는 부르지 않습니다**): `topics/statements/hexaco 각각에 대해 none | pending | ready | failed`. "이 유저는 왜 추천이 안 되나"를 확인하는 지원·디버깅 용도입니다. 상태값 의미 두 가지만 지켜주세요: "topic 0개(ready, 정상)"와 "아직 추출 전(pending)"의 구분, 그리고 `pending`은 **최초 추출 전에만** — 이미 데이터가 있는 유저의 주기적 갱신 중에는 `ready`를 유지하고 기존 값을 계속 서빙 (공통 규약 1).

## 7. 성격 데이터 — `GET /owners/{id}/hexaco` + batch (추후 소비, 계약만 먼저)

**왜.** "주제 없이 성격이 잘 맞는 상대 추천"(추후 기능). 매칭은 요청 유저 ↔ 후보 여러 명 비교라 **batch 조회**가 필요합니다.

**무엇을 해보려는가.** 단순 성격 유사도만으로 특정 두 사람의 궁합을 예측하기는 어렵다는 연구가 많아, 단계적 매칭을 **가설로** 실험하려 합니다: ① 상대의 원만성(A)·성실성(C)처럼 "누구와도 대화가 잘 되는" 신호를 먼저 보고 → ② 대화 스타일이 맞는지 보고 → ③ 동률에서만 축별 유사/상보를 봅니다. (이 순서는 연구 결론이 아니라 저희 가설이고, 결과 데이터로 조정합니다.) 이를 위해 필요한 값:

- 축별(가능하면 하위 척도별) 점수 + **점수를 얼마나 믿어도 되는지**(confidence 또는 근거량) — 대화 3턴 유저와 3천턴 유저의 점수는 신뢰도가 다르고, 적은 대화로 추정된 낮은 점수로 유저를 상시 하향시키면 안 되므로 ①을 안전하게 쓰는 데 필수입니다.
- `ready | insufficient | none` 구분 + 추출 모델 버전.
- HEXACO 외에 **대화 스타일**(직설성·장황함·격식·논쟁 수용도 등)도 추출 가능하다면 함께 — ②에 직접 쓰입니다. 실제 대화 중의 스타일 동조가 관계 결과와 연관된다는 연구가 있는데, 저장된 스타일끼리의 사전 비교가 같은 효과를 내는지는 저희가 데이터로 검증할 가설입니다.

---

## 공통 규약 (모든 endpoint)

1. **읽기 모델 — 각 호출은 최신 값**: persona 데이터가 유저별로 상시 갱신된다고 들었고, 그 전제에서 시점 고정(snapshot)은 요청하지 않습니다. 대신 두 가지만 지켜주세요. ① **갱신이 진행되는 동안에도 그 유저의 기존 값은 계속 조회 가능**해야 합니다 — 갱신 중 데이터가 잠깐 사라지면 그 유저가 주기적으로 추천에서 빠집니다. ② 조회 사이에 사라진 항목은 항목별 `not_found`/`not_publishable`로 알려주세요 — 우리가 그 후보만 빼고 계속 갑니다. 비공개 전환은 항상 즉시 반영해 주세요. (새 topic이 절반만 처리된 값으로 보이는 문제는 기본 비공개 모델이 좁혀주지만, 공개 전환 시점의 보장이 따로 필요합니다 — 아래 규약 2.)
2. **공개 topic만**: 유저가 topic별로 공개 여부를 결정하고 기본은 비공개라고 들었습니다. persona API는 **명시적으로 공개된 topic과 그 소속 발언만** 반환해 주세요 — 발언의 공개 여부는 소속 topic을 따르고, 발언 단위 별도 제어는 요청하지 않습니다. 공개 전환의 타이밍이 중요합니다: **공개는 topic의 집계값과 소속 발언 색인이 모두 준비된 뒤에만 검색에 나타나야 하고(그 전에는 공개 의사만 저장), 비공개는 즉시 숨겨져야 합니다.** 한쪽만 반영된 상태 — topic은 검색되는데 발언은 아직 없거나, 절반만 처리된 값으로 필터·판정되거나, 숨긴 topic의 발언이 발언 검색(③)에 남는 것 — 가 없어야 합니다.
3. **관련도 점수**: 결과 **순서**는 항상 주시고, 점수를 주신다면 의미·범위·모델 버전을 함께 — 우리는 순서에만 의존하고 점수는 참고용으로만 씁니다. 검색 모델을 바꾸셔도 우리가 깨지지 않게 하기 위함입니다.
4. **버전**: 응답에 추출 모델 버전을 실어주세요. 모델이 바뀌면 전 유저 점수가 함께 움직이는데, 버전이 없으면 유저가 변한 것과 구분할 수 없습니다. 검색 관련도 점수를 주신다면(규약 3) 검색 모델 버전도 같은 이유로 함께.
5. **평가 재료**: 계약 fixture(대표 응답 JSON)와 고정 corpus를 주시면, 검색 품질(예: "커피"→"coffee" 회수)을 우리가 독립적으로 측정하겠습니다.

## 함께 정하고 싶은 것

1. topic_id의 좌표계 — (owner_id, topic_id) 쌍인지 전역 유일인지, 병합·개명 시 id가 어떻게 되는지.
2. 추출 주기 — 유저별 상시 갱신으로 확인됨. 남는 질문: 처리 window 크기와 대화 반영 지연의 기대 범위.
3. 다국어 search alias의 지원 언어 셋(한/영/일 …)과 생성 시점.
4. 접근 통제 — 이 API는 전 유저 데이터의 bulk 조회 표면이라, 호출 서비스 제한·감사 로그 등을 어느 층에서 할지.
5. 발언 id의 재추출 간 유지 — 같은 발언 = 같은 id가 가능한지, 어렵다면 조회용 id와 감사(추천 기록)용 논리 id를 분리할 수 있는지.
6. topic 공개 동의의 범위 — 유저의 "공개"가 그 topic의 추출 발언을 추천 판정(LLM 입력 포함)에 쓰는 것까지 포함하는 동의인지.
