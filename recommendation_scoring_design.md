# 추천 스코어링 설계 — 하나의 랭커, 표면별 정책 (rev 3, 2026-08-31)

> **문서 지위: 제안이고 결정이 아니다.** 어떤 코드도 이 문서를 근거로 바뀌지 않았고, 어느 팀에도
> 발신하지 않았다. **설계 정본은 `recommendation_pipeline_design.md`**이며, 이 문서가 정본과 어긋나면
> 정본이 맞다 — 정본은 *현재 가동 중인* 파이프라인을 서술하고, 이 문서는 *가려는 곳*을 서술한다.
> 결정이 생기면 정본 §S5·§10과 §9(열린 결정)로 올린다.
>
> **표기**: `사실`(소스에서 `file:line`으로 확인) · `판단`(이 문서의 의견) · `열린 항목`(협의·측정 대기).
>
> **전제 (2026-08-27 지시)**: 기존에 잠근 계약은 이 그림의 제약이 아니다. 정본의 규율은 *지금까지의*
> 가정 집합(topic-api가 보유자 랭킹을 소유·피드백 없음·facets 미정)에서 나온 것이므로, 가정이 바뀌면
> 재도출 대상이다. 이 문서는 계약을 지키는 방향이 아니라 **가장 좋은 시스템**을 목표로 쓴다.
>
> **선행 기록**: `archive/recsys_serving_discussion.md` — 이 설계에 이르기까지의 논의와, `file:line`으로 확인한
> 상류 사실들. 이 문서가 그 논의의 결론이고, 그쪽은 과정의 기록으로 남는다.
>
> **도구 조사 근거**: [`recsys_opensource/`](recsys_opensource/README.md) — §9의 채택·기각 판단을
> 검증하는 survey다. 요구사항과 결정은 이 문서가 소유하고, survey는 구현 후보의 사실과 비용을 소유한다.
>
> 기준 커밋: `bourbon-agent-discovery-api` `d82cd04` · `bourbon-topic-api` `9ee67f3` ·
> `bourbon-agent` `1029b45a`.

---

## §1 한 문장

**신호를 여러 개 두고 하나의 ranking function으로 합치되, 자격 판정은 점수 밖에 두고, 무엇을 왜
보여줬는지를 처음부터 기록한다.** 초기 ranking function은 LR의 선형식이고, 데이터가 충분하면 같은 feature
계약 위에서 GBDT로 바뀔 수 있다. 표면이 달라져도 코드 분기가 아니라 `surface` 입력과 학습된 정책이
달라진다(§4·§9-3).

이 문서가 답하는 질문은 다섯이다.

1. 무엇을 신호로 두는가 (§3)
2. 상황마다 어떻게 다르게 쓰는가 (§4)
3. 사용자 행동을 어떻게 신호로 바꾸는가 (§5)
4. 그것이 맞는지 어떻게 아는가 (§7)
5. **agent-discovery-api가 어떤 모양이 되는가 (§10)**

---

## §2 구조 — 다섯 단계

```
① 후보 생성   여러 소스의 합집합
      ↓
② gate        자격: privacy · safety · self · opt-in · hard capacity  ← 점수가 아니라 통과/탈락
      ↓
③ 점수        fθ(표면, 신호, 부재 mask)                         ← 초기 LR, 이후 GBDT 가능
      ↓
④ 탐색        소수 슬롯의 의도된 교란                          ← ⑤의 재료를 만든다
      ↓
⑤ 로그        고려 후보 전체 + 자리 + 정책확률 + 표면 + ranker/feature version
```

### 2-1 `판단` 왜 gate가 점수 밖에 있어야 하는가

가중합만 있으면 **"이 신호는 순서를 바꾸되 자격 없는 후보를 끌어올리지는 못한다"를 표현할 방법이
없다.** 가중치가 0보다 크면 충분히 큰 신호값은 언제든 승격시킨다. 자격과 선호를 같은 축에 놓는 순간
둘은 교환 가능해진다.

그래서 gate는 별도 단계다. 어차피 필요하기도 하다 — persona는 `sharable`만 쓸 수 있고(§8-2),
topic 보유에도 visibility가 있으며(§8-1), 추천 대상 opt-in(Q11)과 실제 처리 불가능한 hard capacity도
점수와 교환할 수 없다. 자격 판정을 명시적으로 두면
**"선호 신호가 순서만 바꾸게 할지, 집합도 고르게 할지"를 가중치가 아니라 구조로 정할 수 있다**
— 그리고 그 결정을 나중으로 미룰 수 있다(§11 D1).

### 2-2 `판단` 왜 탐색이 로그와 세트인가

§7-2에서 자세히 다루지만 요지는 이것이다: **우리가 노출을 통제하므로, 로그만으로는 "3번 자리가 덜
선택된 것이 순위가 맞아서인지 자리 때문인지" 알 수 없다.** 로그는 상관을 주고, 자리 효과와 실력을
분리하려면 개입이 필요하다. 탐색 슬롯이 그 개입이고, 동시에 "보여주지 않았다면 어땠을까"의 유일한
증거원이다.

---

## §3 신호

네 개를 둔다. 각각 **없을 수 있고**, 없는 것과 0인 것은 다르다 — 신호 부재는 missing mask로
표현하며 값 0으로 위장하지 않는다.

| 신호 | 무엇을 재나 | 언제부터 있나 |
|---|---|---|
| **S<sub>topic</sub>** 주제 관련도 | 지금 물은 것과 후보가 보유한 것의 일치도 | **지금** |
| **S<sub>persona</sub>** 누적 취향 | 요청자 persona와 후보 보유 토픽의 친화도 | persona 경로가 열리면 (§8-2) |
| **S<sub>engage</sub>** 상호작용 이력 | 이 요청자와 이 후보 사이의 과거 결과 | 피드백이 쌓이면 (§8-3) |
| **S<sub>cohort</sub>** 유사 사용자 | 비슷한 요청자들이 이 후보/토픽에서 얻은 결과 | 위 둘이 있으면 |

### 3-1 S<sub>topic</sub> — 주제 관련도

지금 있는 것이고, 첫날 이 시스템을 혼자 지탱한다. 자연어 요청을 카탈로그 토픽으로 확정한 뒤 그
토픽의 보유자를 읽는다. 후보마다 얻는 값: 확정 토픽과의 거리(자기 자신인가 후손인가), 보유 근거의
강도, 여러 해석에서 겹쳐 나왔는지.

### 3-2 S<sub>persona</sub> — 누적 취향

`판단` **임베딩보다 먼저 시도할 것이 있다.** persona의 렌더 텍스트는 마크다운이고 **모든 `#` 헤딩이
토픽 이름**이라는 것이 상류 계약이다(§8-2). 그러므로:

```
sharable.preferences 마크다운 → 헤딩 파싱 → 헤딩을 카탈로그 토픽으로 ground → 토픽 집합
                                → 후보 보유 토픽과의 집합 유사도
```

첫 단계에 LLM이 필요 없다. 그리고 이 경로는 **해석 가능하다** — "두 분 다 드립 커피와 등산"이라고
말할 수 있는 친화도다. 인프라도 0이다.

임베딩이 필요해지는 조건은 둘이고 **둘 다 측정으로 확인된다**: 헤딩이 카탈로그에 없는 개념일 때,
또는 헤딩 아래 본문에만 있는 정보가 필요할 때. 지표는 **헤딩의 grounding 성공률** 하나다.

쓰게 되면 요건은 다국어 · 대칭 유사도 · CPU급이고, 요청 경로가 아니라 persona 갱신 시 배치다.
**kind 분리는 우리 몫이 아니다** — 상류가 `bio`·`traits`·`preferences`를 이미 별도 문자열로 준다.

### 3-3 S<sub>engage</sub> — 상호작용 이력

세 변환을 거친다. 앞의 둘은 표준이고, 셋째가 이 제품에 특유하다.

**⒜ 깊이의 포화** — 아주 긴 대화 하나가 그 사람의 취향 전체를 지배하지 않도록 누른다. 체감 효용이
선형이 아니므로 압축한다.

**⒝ 최근성 감쇠** — 반년 전과 지난주는 무게가 달라야 한다. 하드 윈도우("최근 N일")가 아니라 서서히
옅어지게 해서 경계에서 순위가 갑자기 뒤집히지 않게 한다.

**⒞ 해소 조건** — §5-2. 깊이는 **단독으로는 부호가 정해지지 않는다.**

### 3-4 S<sub>cohort</sub> — 유사 사용자

"비슷한 사용자가 이 토픽에서 긍정적이었다"를 옮긴다. 유사도의 근거는 두 단계로 바뀐다.

- **처음**: persona 기반(§3-2의 토픽 집합 유사도). 상호작용 0건에서도 동작한다.
- **나중**: 행동 기반(같은 후보에 대한 결과의 공변). 데이터가 쌓이면 이쪽이 더 정확하다.

`판단` 그래서 **persona 임베딩은 영구 부품이 아니라 cold start 장치**다. 이것이 도입 규모를 정한다 —
cold start 장치에 벡터DB 클러스터를 세울 이유가 없다.

---

## §4 표면을 입력으로 받는 하나의 랭커

같은 시스템이 표면마다 다르게 행동하는 방법은 **ranking function에 표면을 입력하는 것**이다. 코드 분기가
아니다.

초기 LR 단계에서는 이것이 학습된 표면별 가중합으로 나타난다.

```text
f_LR(surface, x, missing) = β₀(surface) + Σ βᵢ(surface) × xᵢ + Σ γᵢ × missingᵢ
```

GBDT 단계에서는 `fθ`가 비선형이므로 `Σ wᵢ × 신호ᵢ` 표기를 문자 그대로 적용하지 않는다. 신호 이름,
부재 mask, surface와 feature version은 유지하고 함수만 교체한다(§9-3). LR/GBDT 구현 비교 근거는
[`recsys_opensource/ranking.md`](recsys_opensource/ranking.md)에 둔다.

| 표면 | S<sub>topic</sub> | S<sub>persona</sub> | S<sub>engage</sub> | S<sub>cohort</sub> | 근거 |
|---|---|---|---|---|---|
| 대화 중 | **높음** | 낮음 | 중간 | 낮음 | 지금 하는 이야기가 가장 강한 증거다 |
| 홈 | 낮음 | **높음** | **높음** | 중간 | 물어본 것이 없으니 누적된 것이 답한다 |
| 비로그인 | **단독** | — | — | — | 이력을 붙일 신원이 없다 |
| 토픽 탐색 | **높음** | 중간 | 낮음 | 낮음 | 주제가 명시적이다 |

`판단` **표의 값은 지금 채우지 않는다.** 이 표는 feature의 기대 방향과 ablation 가설이지 사람이 입력할
production 상수가 아니다. 구조만 지금 만들고 LR 계수나 GBDT split은 데이터가 정한다. 지금 정할 수 있는
것은 **어느 칸이 비어 있어야 하는가**뿐이고, 비로그인 행이 그 예다(신원이 없으므로 세 신호가 구조적으로
부재한다).

`판단` **신호 부재 필터.** 특정 신호가 없는 후보를 걸러내는 조건을 얹으면 같은 시스템에서
"주제 기반만" 또는 "개인화만" 나오는 표면도 만들 수 있다. 이것은 가중치 0과 다르다 — 가중치 0은
그 신호를 무시하고, 필터는 그 신호가 없는 후보를 제외한다.

---

## §5 피드백 — 퍼널과 라벨

### 5-1 퍼널

사용자 반응은 단일 사건이 아니라 단계다. 각 단계는 **다른 편향**을 갖는다.

```
노출 → 톡 → 대화 시작 → 대화 깊이 → 해소
 └자리 효과가 가장 큼        └여기부터는 자리와 거의 무관
```

앞쪽 단계일수록 **우리가 만든 자리 효과에 오염**되고, 뒤쪽 단계일수록 **사용자 판단에 가깝지만 희소**
하다. 그래서 하나만 쓰지 않고 단계별로 따로 기록한다.

### 5-2 `판단` 깊이의 부호는 해소와 함께 정해진다

포화 압축(§3-3 ⒜)은 **크기** 문제를 풀지 **부호** 문제를 풀지 않는다 — `log(1+x)`도 여전히
"길수록 좋다"이다. 그런데 깊이의 부호는 상호작용의 종류에 따라 갈린다:

- **지식을 물으러 갔다면** 정확한 답이 대화를 끝낸다. 길어진 것은 못 알아들었다는 뜻일 수 있다.
- **같이 이야기하러 갔다면** 길수록 좋다.

깊이 하나로는 이 둘을 구분할 수 없다. **"그 뒤에 해소되었는가"를 곱하면 구분된다.**

| | 같은 토픽 재질의 **없음** | 같은 토픽을 **다른 에이전트에게** |
|---|---|---|
| **깊은 대화** | 강한 긍정 — 몰입했고 해결됐다 | **부정** — 길었지만 답을 못 했다 |
| **짧은 대화** | 긍정 — 정확히 답했다 | 부정 — 헛걸음이었다 |

★ **재질의 신호는 파생 비용이 거의 0이다.** 같은 요청자가 같은 토픽을 다시 물었고 다른 후보를 골랐다
— 이벤트 스트림에 이미 들어 있는 두 사건의 조합이다. 그리고 이것이 **깊이를 버리지 않으면서 부호
문제를 푸는 유일한 값싼 방법**이다.

### 5-3 `판단` 자리 효과는 로그만으로 분리되지 않는다

"3번째 자리 후보가 1번째보다 덜 선택되는 것이 순위가 맞아서인가 자리 때문인가"는 정확히 물어야 할
질문이고, **관측만으로는 답이 없다.** 우리가 자리를 정했으므로 자리와 실력이 완전히 얽혀 있다.

분리하려면 **개입**이 필요하다:

- 같은 후보를 여러 자리에 노출시킨 사례가 자연 발생하도록 두거나(느리다),
- 소수 요청에서 **의도적으로 자리를 섞는다**(탐색 슬롯, §2-2).

전자는 공짜지만 몇 달이 걸리고, 후자는 첫날부터 답을 만든다. **탐색 슬롯이 실력 측정의 도구이지
다양성 장치가 아니라는 점**이 중요하다.

---

## §6 콜드 스타트

`판단` **별도 시스템을 만들지 않는다.** 콜드 스타트는 feature availability 상태이지 다른 코드가 아니다.

```
첫날      S_topic 지배 + S_persona 보조        (S_engage·S_cohort 부재 + missing mask)
        ↓  데이터가 쌓이면서
성숙     네 신호가 표면별 비중으로 섞임
```

"처음엔 규칙 기반으로 하고 나중에 갈아탄다"가 남기는 **두 번째 시스템과 전환 작업이 없다.** 이것이
이 설계를 고르는 가장 큰 이유다.

### 6-1 `판단` 첫날 항으로 인기도는 쓰지 않는다

첫날 항은 **주제 관련도 + persona 친화도**다. 인기도를 그 자리에 두지 않는 이유는 규율이 아니라
측정 문제다:

> **첫날의 인기도는 실력의 대리가 아니라 "우리가 누구를 먼저 보여줬는지"의 대리다.** 그리고 그것이
> 그대로 다음 인기도가 된다.

우리가 노출을 통제하고 후보 집합이 작으므로, 인기도로 시작하면 첫 노출 분포가 자기실현된다. persona
친화도는 이 문제가 없다 — **우리 노출과 무관한 곳에서 만들어진 값**이기 때문이다(§8-2). 그리고 그
값은 이미 존재한다.

인기도를 나중에 신호로 넣는 것은 별개 문제이고, 넣더라도 §7-3의 집중도 지표를 보면서 넣는다.

---

## §7 측정

### 7-1 노출 로그 — 가장 싸고 가장 되돌릴 수 없는 항목

**추천한 것에 대해서만 피드백이 생긴다.** 노출을 기록하지 않으면 "보여주지 않았던 후보가 더
나았을까"를 나중에 물을 방법이 없다. 그리고 이것은 **기다리면 사라지는 종류**다 — 서빙이 만들어내는
값이라, 기록 없이 서빙한 기간의 피드백은 무엇이 왜 보여졌는지를 소급 재구성할 수 없어 보정 불가능한
데이터가 된다.

남길 것:

| 항목 | 이유 |
|---|---|
| 고려된 후보 **전체** (보여준 것만이 아니라) | 보여준 것만 남기면 분모가 없다 |
| 각 후보의 자리와 **노출 확률** | 자리 효과 분리(§5-3)와 off-policy 보정의 전제 |
| 잘린 이유 (gate / 신호 부재 / 순위) | 자격 미달과 순위 미달은 다른 사건이다 |
| 표면 + logging policy·ranker·feature 버전 | 정책이나 특징 정의가 바뀌면 과거 로그의 해석도 바뀐다. LR이면 계수 아티팩트 버전까지 이 값에 포함한다 |
| 확정된 토픽 | S<sub>topic</sub> 라벨의 키 |

`판단` 이것은 **로그가 아니라 이벤트 스트림**이다. 후보별 사용자 id가 들어가므로 보존 정책이 붙는
데이터이고, 배치 잡이 읽으므로 lookup 저장소가 아니라 append-only 파티션이 맞다.

### 7-2 무엇을 답할 수 있게 되는가

- 홈 추천의 전환율이 대화 중 추천보다 높은가 → 표면 필드
- 3번 자리가 덜 선택되는 것이 자리 때문인가 → 자리 + 노출 확률 + 탐색
- 다른 순서였다면 더 나았을까 → 노출 확률(off-policy 추정)
- 어떤 신호가 실제로 기여하는가 → ranker/feature 버전 + 신호별 값·부재 mask

**검색 로직이었다면 답할 방법이 없는 질문들**이고, 이것이 추천 시스템으로 다루는 실질적 이득이다.

### 7-3 `판단` 집중도 지표를 하나 세운다

상위 소수가 노출을 얼마나 흡수하는지를 재는 지표(노출 분포의 지니 계수, long-tail coverage)를
상시 지표로 둔다.

정의와 분모는 [`recsys_opensource/concentration_metrics.md`](recsys_opensource/concentration_metrics.md)에
둔다. **집중도를 재는 것, hard capacity gate, soft redistribution은 서로 다른 장치**다.

**집중을 금지하자는 것이 아니다.** 행동 신호를 쓰면 집중은 어느 정도 필연이다. 요점은 **얼마나인지
알고 받아들이자**는 것이고, 지금은 어느 쪽도 관측되지 않는다. 정의 차용은 수십 줄이고 기존 eval
게이트에 키 하나로 붙는다.

---

## §8 입력 소스 — 지금 무엇이 실재하는가

### 8-1 `사실` topic-api — 있다

`/search/topics`(카탈로그 검색)와 `/topics/{id}/users`(토픽 보유자 랭킹). 보유 항목에 visibility가
있고(`public`/`friends`), 점수는 요청 경로에서 계산되지 않는다 — producer가 facets와 confidence에서
미리 계산해 주입한다(`topic/persona_topics/scoring.py:18`, `topic/storage/user_topics.py:6-8`).

`판단` **오프라인 계산 / 온라인 서빙 분리는 새로 만들 것이 아니라 이미 있는 구조**다.

### 8-2 `사실` persona — 있다. 우리 쪽 경로만 없다

bourbon-agent가 이미 생산·저장하고 있다(`persona_extractor/` + `bourbon_agent/user_persona/`).

> ⚠ **이름 충돌**: `bourbon_agent/agents/persona/`는 **캐릭터 에이전트**(Cathie Wood 등)로 무관하다.

**두 층**이고 아래에서 위가 렌더된다:

| 층 | 내용 |
|---|---|
| note (누적기) | `bio[]` · `traits.hexaco{}`(닫힌 25 facet) · `traits.free[]` · `preferences[]`. 항목은 `{topic, digest, updated_at}` |
| persona (렌더) | visibility 2슬롯(`sharable`/`private`) × kind 3종(`bio`/`traits`/`preferences`), 각각 마크다운 + `revision` |

**우리 설계에 직접 걸리는 네 가지:**

1. **헤딩이 곧 토픽이다.** `persona_extractor/prompt.py:69` — *"Persona texts are markdown, and every
   `#`-style heading names a topic."* 실제 형태:
   ```python
   UserPersonaSlot(preferences="## 커피\n- 산미 선호")           # 렌더
   NoteItem(topic="coffee", digest="prefers light roast", ...)  # note
   ```
   → §3-2의 파싱 경로가 성립하는 근거다.

2. **`sharable`만 쓸 수 있다.** `prompt.py:138-144` — `sharable`은 *"what the agent may say about the
   owner to OTHER people"*, `private`은 *"health, finances, relationships, work specifics"*, 그리고
   **판단이 불확실하면 private으로 간다(fail closed)**. 둘은 포함 관계가 아니라 **상호보완**이다
   (*"content on the sharable side never repeats on the private side"*).

3. **note 층은 요청하지 않는다.** 구조가 `{topic, digest}`라 우리에게 훨씬 편하지만 **visibility 구분이
   아예 없어서**(grep 0건) private 재료가 섞여 있다. 편의를 근거로 상류의 fail-closed 분류를 우회하면
   그 경계를 무너뜨리는 쪽이 우리가 된다.

4. **암호화되어 있어 테이블 조인 경로가 없다.** `doc`이 zlib + AES-GCM이고 키는 bourbon-agent 것이다
   (`user_persona/repository.py` docstring). → **수신은 그들이 소유하는 API 하나뿐이고**, 요청할 것은
   `sharable` 슬롯의 세 kind + `revision`이다(`revision`은 캐시 무효화 키).

`사실` **헤딩은 안정적 식별자가 아니다.** `prompt.py:70` — *"topics may merge, split, move under
broader headings, or be renamed as the evidence grows."* → 캐시 키·조인 키·feature 카테고리로 쓰면
드리프트한다. 안정적 키는 **ground된 `topic_id`**뿐이고, §3-2의 파이프라인이 정확히 그것을 만든다.
(예외 장치인 pinned topics는 `load_pinned_topics`가 `()`를 반환하는 stub이다.)

`사실` 갱신은 owner의 메시지마다 debounce되어 수시로 일어난다(`persona_extractor/scheduling.py`).

### 8-3 `사실` 대화 이벤트 — 없다

`rec_signal`·`rec-signal` grep 0건. 대화 시작·깊이·재질의 어느 것도 기록되지 않는다. 노출 기록도
집계 수치뿐이다(`pipelines/topic_recommend.py:68-72` — 후보별 자리와 잘린 이유는 남지 않는다).

→ §5의 퍼널 전체가 **협의와 신규 배선을 요구한다.** 이것이 이 설계의 유일한 시급 항목이다(§11).

---

## §9 도구

### 9-1 `판단` 채택

| 도구 | 용도 | 근거 |
|---|---|---|
| **Cornac** | **오프라인 모델 실험 하네스** | 모델 동물원은 쓰되 all-catalog split/metric은 쓰지 않는다. 요청마다 달라지는 eligible candidate set을 replay하고 같은 `qid` 안에서만 채점하는 evaluator는 우리가 소유한다. 남은 일은 모델별 `score(candidate_ids)` adapter 계약 테스트다. 근거: [`cornac.md` §6](recsys_opensource/cornac.md#6-우리-시스템-배치) |
| **off-policy 추정기** (IPS/DR 계열) | "다른 순서였다면" | full A/B가 어려운 구간에서, **overlap을 가진 탐색 로그가 있을 때** 쓸 수 있는 평가 수단이다. 더 중요한 가치는 로그 스키마를 강제하는 데 있다. estimator보다 estimand와 propensity 계약을 먼저 정한다. 조사: [`off_policy.md`](recsys_opensource/off_policy.md) |
| **LR → GBDT 랭커** | 후보 100~1,000개 재순위 | 서로 스케일이 다른 topic·persona·행동 점수를 더하지 않고 feature로 넣는다. 초기에는 LR, 라벨과 비선형 이득이 확인되면 LightGBM/XGBoost ranker를 challenger로 둔다. 조사: [`ranking.md`](recsys_opensource/ranking.md) |
| **집중도 지표 정의** | §7-3 | Gini 하나가 아니라 top-share·coverage·HHI와 workload saturation을 함께 본다. 정의: [`concentration_metrics.md`](recsys_opensource/concentration_metrics.md) |

### 9-2 `판단` 기각

**Gorse** — 붙이는 속도와 운영 도구가 장점이지만, 전역 feed·캐시 중심 계약을 우리 요청별
eligible pool, surface-aware ranker, propensity 이벤트 계약에 맞추려면 핵심 경로를 다시 소유해야 한다.
그 순간 올인원 도입의 이득이 줄어든다. 추천 기능만 빠르게 필요할 때는 합리적이지만 현재 query형
파이프라인의 기본 선택은 아니다. 상세 근거: [`gorse.md`](recsys_opensource/gorse.md).

**대규모 학습 스택 · ANN · feature store** — 규모가 맞지 않는다. 후보는 요청당 수백이고 라벨은 처음
수천 행이다. 이 구간에서는 **GBDT가 신경망을 이기고**, 근사 검색이 해결할 대상이 없으며, feature는
같은 요청에서 이미 읽는 값이라 feature store가 온라인/오프라인 일관성 문제를 새로 사온다.

### 9-3 `판단` GPU는 어느 지점에도 필요 없다

| 지점 | 필요? | 근거 |
|---|---|---|
| 랭커 학습 | 아니다 | 데이터 규모가 GBDT 구간이다. CPU 수초 |
| 랭커 추론 | 아니다 | 후보 수백 × 트리 앙상블 = 마이크로초 |
| persona 임베딩 | 아니다 | 요청 경로가 아니라 갱신 시 배치. CPU 수십 ms |
| ANN | 해당 없음 | §3-2의 경로에서 후보가 유계로 남는다 |

모델은 **LR로 시작해 GBDT를 challenger로 검증**한다. 초기 라벨 규모에서는 LR이 과적합에 더 안전하고,
서빙 이미지에 수치 스택을 들이지 않아도 된다. GBDT로 갈 때 얻는 것 중 하나가 **단조 제약**인데,
이것으로 "이 신호는 한 방향으로만 작용한다"를 규율이 아니라 강제로 만들 수 있다. 전환은 날짜나
행 수만으로 하지 않고 request-group 평가·calibration·집중도·latency가 LR보다 나을 때 한다. 구현 비교와
주의점은 [`ranking.md`](recsys_opensource/ranking.md)에 둔다.

### 9-4 `판단` 서빙은 얇게, 학습 잡은 같은 repo에

모델은 **파일 하나이고 서비스가 아니다.** 점수 계산은 순수 함수이므로 네트워크 홉도 상태도 없다.

★ **feature 추출 함수는 하나여야 한다.** 학습 잡과 서빙이 같은 함수를 쓴다. training/serving skew는
이 종류 시스템의 대표적 결함이고, **두 구현이 생기면 양쪽 테스트가 각자의 구현을 고정해서 그 결함은
테스트로 잡히지 않는다.** 그래서 학습 잡은 별도 서비스가 아니라 같은 repo의 CLI 서브커맨드다.

---

## §10 시스템 구조 — 도입했을 때 agent-discovery-api가 어떤 모양이 되는가

### 10-1 `판단` 원칙 — 모델은 파일이지 서비스가 아니다

학습 결과물은 **아티팩트 파일 하나**다. 서빙은 그것을 읽어 순수 함수로 점수를 낸다. 추론 서비스도,
네트워크 홉도, 공유 상태도 없다.

이 선택 하나가 아래 구조를 거의 다 결정한다.

- **요청 경로에 새 의존이 생기지 않는다.** 모델 서비스가 죽어서 추천이 죽는 일이 없다(§10-7).
- **학습이 서빙과 완전히 비동기다.** 학습 잡의 실패가 서빙에 도달하지 않는다.
- 대신 **모델 버전을 요청마다 로그에 남겨야 한다.** 안 그러면 결과를 어느 모델에 귀속시킬지 알 수 없고,
  §7-1의 로그가 시간에 걸쳐 의미를 잃는다.

### 10-2 배포 단위

| 단위 | 종류 | 주기 | 왜 분리되나 |
|---|---|---|---|
| **서빙 API** | Deployment (기존) | 요청당 | 지금 있는 것. §2의 ①–⑤가 여기서 돈다 |
| **이벤트 수신** | 라우트 추가 → (필요시) 별도 Deployment | 이벤트당 | 노출은 서빙이 쓰지만 **피드백은 bourbon-agent가 보낸다** — 들어오는 방향이 다르다 |
| **학습 잡** | CronJob | 일 또는 주 | CPU 수분. 서빙과 자원·이미지를 격리한다 |
| **persona 동기화** | CronJob | 시간 또는 일 | 상류 `revision`을 보고 바뀐 것만 갱신(§8-2) |
| **오프라인 평가** | 수동 / CronJob | 필요할 때 | Cornac·off-policy. **서빙 이미지에 들어가지 않는다**(§10-5) |

`판단` 이벤트 수신을 서빙과 합칠지는 **트래픽이 정한다.** 처음에는 같은 Deployment의 다른 라우트로
충분하고, 피드백 양이 요청 양을 넘어서면 분리한다. 지금 나눌 이유가 없고, 나중에 나누는 비용도 낮다.

### 10-3 저장소

| 저장소 | 담는 것 | 쓰는 쪽 | 읽는 쪽 |
|---|---|---|---|
| **이벤트 스트림** (append-only, 날짜 파티션) | 노출 + 피드백 (§7-1) | 서빙, 수신 | 학습, 평가 |
| **모델 아티팩트** (오브젝트 스토어) | 가중치/트리 + 메타(버전·학습 데이터 범위·생성 시각) | 학습 잡 | 서빙 (기동 시 + 갱신 감지) |
| **persona 캐시** | `sharable` 3 kind + `revision` | 동기화 잡 | 서빙 |
| 카탈로그·보유 | (상류) | topic-api | 서빙 |

`판단` 이벤트 스트림은 **lookup 저장소가 아니라 배치가 읽는 파티션**이다. 후보별 사용자 id가 들어가
보존 정책이 붙으므로, 애플리케이션 로그(structlog)가 아니라 별도 스트림이어야 한다 — 로그에 넣으면
보존 정책과 접근 통제를 로그 인프라가 결정하게 된다.

### 10-4 repo 모듈 구조

```
agent_discovery/
  domain/              (기존) 단계 산출물 타입
  stages/              (기존) + gate·탐색 단계
  pipelines/           (기존) 조립 함수. 새 추천 모양 = 여기 파일 하나
  providers/           (기존 topic-api) + persona/ + events/   ← 주입되는 포트
  ports/               (기존)
  scoring/             ★ 신규
    features.py        신호 추출 — 학습과 서빙이 공유하는 단 하나의 함수
    ranker.py          LR/GBDT 아티팩트 로딩 · 예측 · 버전 (§4·§9-3)
    policy.py          탐색·노출 확률과 surface 정책
  events/              ★ 신규 — 노출/피드백 이벤트 타입과 sink 인터페이스
api/                   (기존) HTTP wire + 이벤트 수신 라우트
cli/
  recommend.py         (기존)
  train.py             ★ 학습 잡
  evaluate.py          ★ Cornac · off-policy
  sync_persona.py      ★ persona 동기화 잡
```

★ **`scoring/features.py`가 이 구조의 핵심이다.** 서빙(`stages/`)과 학습(`cli/train.py`)이 **같은
함수**를 import한다. 두 구현이 생기면 training/serving skew가 생기고, 그 결함은 **양쪽 테스트가
각자의 구현을 고정하기 때문에 테스트로 잡히지 않는다.** 학습 잡을 별도 repo나 별도 서비스로 빼면 안
되는 이유가 이것이고, 편의가 아니라 방어다.

### 10-5 이미지 — 하나의 repo, 두 개의 설치

| 이미지 | 설치 | 담는 것 | 크기 관심사 |
|---|---|---|---|
| **서빙** | 기본 의존성만 | API + `scoring/` + `stages/` | spot에 뜨고 512Mi request다. 수치 스택을 넣지 않는 것이 기본 |
| **학습·평가** | 기본 + `train` 그룹 | 위 전부 + 학습기 + Cornac + off-policy | 크기 무관. CronJob이고 수분 산다 |

의존성 그룹(`[dependency-groups]`)으로 나누면 **코드는 하나, 설치만 둘**이 된다. §10-4의 공유 함수가
성립하려면 코드가 하나여야 하므로 이 방식이 강제된다.

★ **import 경계 테스트로 막는다.** 이 repo에는 아키텍처 import 경계를 테스트로 강제하는 장치가 이미
있다. "학습 스택은 `agent_discovery/`에서 import될 수 없다"를 같은 방식으로 넣으면, 실수로 서빙
이미지에 딸려 들어가는 일이 **리뷰가 아니라 CI에서** 막힌다.

### 10-6 요청 경로에서 실제로 바뀌는 것

| 단계 | 지금 | 도입 후 | 요청당 비용 |
|---|---|---|---|
| 후보 생성 | topic-api 읽기 | 그대로 (persona 경로면 소스 하나 추가) | 변화 없음 |
| gate | 여러 곳에 산재 | 명시적 단계로 모음 | 변화 없음 |
| 정렬 | 고정 규칙 | 공용 feature 추출 + ranker(LR, 이후 GBDT challenger) | **마이크로초** |
| 탐색 | 없음 | 소수 슬롯 교란 | 무시 가능 |
| 로그 | 집계 수치 | 후보별 이벤트 | **요청 경로 밖이어야 한다** ↓ |

★ 마지막 줄이 유일하게 조심할 지점이다. 노출 이벤트 쓰기가 응답을 기다리게 하면 안 되므로 버퍼에
넣고 배치로 흘린다. 다만 **유실이 부하와 상관되면 로그가 편향된다** — 무작위 유실은 균일 표본이라
보정이 되지만 "바쁠 때만 빠지는" 유실은 안 된다. 그래서 버퍼가 차면 **드롭하되 드롭 카운터를 남기고**,
그 카운터가 특정 시간대에 몰리는지를 본다.

### 10-7 열화 — 각 의존이 죽었을 때

| 죽은 것 | 응답 | 근거 |
|---|---|---|
| 모델 아티팩트 없음/손상 | **버전 고정된 deterministic baseline으로 계속** | 학습 모델은 보강이지 요청 성공의 전제가 아니다 |
| persona 캐시 없음 | S<sub>persona</sub> **부재** + missing mask | 0이 아니라 부재다(§3) |
| 이벤트 싱크 다운 | 버퍼 → 드롭 + 카운터. **요청은 성공** | 추천을 못 하는 것보다 측정을 잃는 편이 낫다 |
| 학습 잡 실패 | 직전 아티팩트 유지 | 서빙과 비동기다(§10-1) |
| topic-api 다운 | 지금과 같음 | 후보가 없으면 추천이 없다 |

★ 앞의 네 줄이 **모델을 서비스가 아니라 파일로 둔 결과**다. 학습 계층 전체가 죽어도 추천은 나온다.
반대로 모델을 서비스로 두면 이 표의 네 줄이 전부 "추천 실패"가 된다.

### 10-8 대조 — 올인원 엔진을 도입했다면

| | 이 설계 | 올인원 엔진 (Gorse류) |
|---|---|---|
| 점수 계산 | 우리 코드 (`scoring/`) | 엔진 내부 |
| surface별 정책 | ranker 입력·정책으로 우리가 소유 (§4) | 엔진이 지원하는 범위 안에서 |
| gate | 점수 밖 명시 단계 (§2-1) | 엔진의 필터 기능에 의존 |
| 노출 로그 스키마 | 우리가 정의 (§7-1) | 엔진 스키마 |
| 후보 생성 | topic-api + persona | 엔진에 데이터를 적재해야 함 |
| 요청 경로 의존 | 없음 (파일) | 엔진 서비스 |
| 붙이는 속도 | 느리다 | **빠르다** |
| 대시보드·A/B | 직접 | **딸려 온다** |

`판단` 오른쪽 열의 마지막 두 줄은 **실제 장점이고 과소평가하면 안 된다.** 추천 팀 없이 추천 기능만
필요하다면 합리적인 선택이다. 우리 경우에 맞지 않는 이유는 하나뿐이다 — **우리가 직접 정해야 하는
두 가지(§4의 ranker/policy 계약, §7-1의 스키마)를 엔진이 소유한다.** 그 둘이 이 설계의 본체이므로, 엔진을 쓰면
설계할 것이 남지 않는 것이 아니라 **설계할 수 없게 된다.**

---

## §11 지금 만들 것 / 나중에 정할 것

### 11-1 지금 만든다 (이음매)

| # | 항목 | 왜 지금인가 |
|---|---|---|
| **N1** | **노출·propensity 이벤트 + 탐색 슬롯** | 세트다. 기다리면 그 기간의 데이터가 영구히 보정 불가가 된다(§7-1) |
| **N2** | 퍼널 + 해소 신호 정의 (§5) | bourbon-agent와 협의가 필요하고, 협의가 배선보다 오래 걸린다 |
| **N3** | persona 조회 API 요청 (§8-2) | 첫날 개인화가 여기 걸려 있고, 암호화 때문에 대안 경로가 없다 |
| **N4** | 신호·부재 mask + surface 입력 + LR baseline의 **구조** | 계수 값은 비운다. 구조가 있어야 신호가 도착할 때 공용 extractor와 ranker를 배선할 수 있다 |
| **N5** | 집중도 지표 (§7-3) | 도구 없이 지금 닫을 수 있는 유일한 항목 |

### 11-2 나중에 정한다 (정책)

값이 없는 동안 정하면 **스탠드인에 맞춰진다.** 그래서 미룬다.

| # | 항목 | 무엇이 답을 주나 |
|---|---|---|
| **D1** | 선호 신호가 gate를 넘을 수 있는가 (§2-1) | 두 경우의 이음매가 같으므로 데이터가 쌓인 뒤 측정으로 |
| **D2** | surface별 ranker/policy 파라미터 (§4) | propensity가 있는 로그 + off-policy 추정 |
| **D3** | 포화·감쇠의 형태와 상수 (§3-3) | 같음 |
| **D4** | persona 임베딩이 필요한가 (§3-2) | 헤딩 grounding 성공률 |
| **D5** | 모델 클래스 (LR → GBDT 전환) | positive 라벨 수만이 아니라 request-group 평가·calibration·집중도·latency |
| **D6** | 인기도를 신호로 넣을 것인가 (§6-1) | 집중도 지표를 확보한 뒤 |
| **D7** | 공급자별 soft 노출 상한·부하 분산 정책 | owner capacity와 양면 만족 목표. hard availability는 gate, soft redistribution은 rank 뒤 정책으로 분리 |
| **D8** | CF/상호작용 feature를 활성화할 것인가 | requester당 distinct agent, agent당 requester, **requester-agent 쌍 재방문율**. 희박하면 CF는 popularity를 재발견하므로 열지 않는다 |

---

## §12 열린 항목

| # | 항목 | 상태 |
|---|---|---|
| Q1 | 대화 이벤트 계약 (무엇이 발생하고 누가 보내나) | bourbon-agent와 **미협의**. N2의 선결 |
| Q2 | 재질의 신호의 관측 가능성 (§5-2) | 같은 요청자·같은 토픽·다른 후보를 우리가 이어붙일 수 있는가에 달려 있다 |
| Q3 | persona 조회 API의 모양 | **미발신**. 요청 내용은 정해졌다(§8-2 4) |
| Q4 | 이벤트 저장소·보존·삭제 SLA | 저장 제품 미정. 이벤트 원본뿐 아니라 파생 feature, id map, 캐시, 현재/과거 모델 아티팩트와 백업에서 삭제가 언제 반영되는지도 정해야 한다. 주간 재학습이면 삭제된 정보가 모델에 일주일 남을 수 있다 |
| Q5 | 후보 생성 소스가 늘어날 때의 합집합 규칙 | 미설계. 지금은 topic-api 단독 |
| Q6 | Cornac 프로토콜의 적합성 (§9-1) | **방향 종결** — model zoo만 쓰고 request별 candidate replay evaluator는 우리가 소유한다. 모델별 score 순서·`k` 계약 테스트만 구현 항목으로 남는다 |
| Q7 | 표면 목록의 확정 (§4의 행) | 기획 영역 |
| Q8 | precompute 여부 | **경로별 속성**이다 — 자유 텍스트 경로는 질문을 미리 볼 수 없어 불가, persona 기반 경로는 가능. 하면 **서빙 시점 visibility 재확인**이 필수다(하루 전 계산에 그 사이 비공개로 바뀐 보유가 섞인다) |
| Q9 | 모델 아티팩트 저장소와 서빙의 갱신 감지 (§10-3) | 미정. 기동 시 로딩은 자명하지만 **가동 중 갱신을 감지할지**는 별개다 — 안 하면 배포까지 옛 모델이고, 하면 pod마다 다른 모델이 도는 구간이 생긴다. 어느 쪽이든 §10-1의 모델 버전 로깅이 전제다 |
| Q10 | 이벤트 수신을 서빙과 분리할 시점 (§10-2) | 트래픽이 정한다. 처음엔 같은 Deployment의 라우트 |
| Q11 | recommendable opt-in | public topic 보유와 "모르는 사람에게 추천 대상이 됨"은 다른 동의다. opt-in의 소유자·철회 SLA를 출시 전에 정한다 |
| Q12 | 첫 relevance 라벨 bootstrap | N1과 병렬. LLM judge는 후보 라벨 초안만 만들고 사람 검증셋으로 편향을 잰다. 전문가 라벨·사용자 설문과 비용/신뢰도를 비교하며 행동 reward를 대신하지 않는다 |
| Q13 | 양면 목표 | 요청자의 해소뿐 아니라 추천된 owner의 만족·부하를 목표에 넣을지 기획 결정 필요. D7의 목적함수를 정한다 |
| Q14 | 관점 다양성 | for/against 축을 제품 목표로 유지한다면 engagement만 최적화할 때 동의하는 agent로 쏠리는 문제와 다양성 제약을 정한다 |

---

## §13 이 문서가 하지 않은 것

- **코드 변경 없음.** 어떤 repo도 이 문서를 근거로 바뀌지 않았다.
- **정본 변경 없음.** 정본은 현재 가동 중인 파이프라인을 서술하고, 이 문서는 가려는 곳을 서술한다.
  지금 정본의 규율을 지우면 **아직 서빙 중인 시스템의 근거를 지우는 것**이 된다.
- **어느 팀에도 발신하지 않았다** — bourbon-agent(Q1·Q3)에도, producer·topic-api에도.
- **도구를 도입하지 않았다.** §9는 판정이고 선택이 아니다.
- **ranker 계수·트리와 정책 임계값을 정하지 않았다.** §4의 표는 feature 방향과 ablation 가설이지
  운영 가중치표가 아니다. 그것이 §11-2의 요지다.
- **(rev 2) 저장소 제품을 고르지 않았다.** §10-3은 각 저장소가 **무엇을 담고 누가 읽는가**를 정했을
  뿐이고, 어떤 제품을 쓸지는 Q9와 Q4다.
- **(rev 2) §10-4의 모듈 트리는 배치 제안이지 확정이 아니다.** 강제되는 것은 하나뿐이다 —
  `scoring/features.py`를 학습과 서빙이 공유한다는 것(§10-4 ★).
- **persona를 읽어 보지 않았다.** §8-2의 예제는 전부 그 repo의 테스트 fixture와 프롬프트 텍스트에서
  온 것이고, 실제 사용자 데이터는 암호화되어 있어 접근하지 않았다.

## 변경 이력

- **2026-08-28 rev 1** — 신설. `recsys_serving_discussion.md`(rev 1–4)의 논의를 **"하나의 점수식 +
  표면별 가중치"를 뼈대로 다시 쓴 것**이다. 그 문서는 "무엇을 하지 말아야 하는가" 쪽으로 기울어
  있었고, 그 구도로는 좋은 시스템이 나오지 않는다.

  **뼈대를 바꾸면서 바뀐 판단 셋**:
  ⑴ **단계별 이행 계획을 버렸다.** "지금은 규칙, 나중에 학습으로 전환"은 두 번째 시스템과 전환 작업을
  남긴다. 신호를 나란히 두고 같은 feature 계약을 유지하면 그 비용이 없고, **콜드 스타트도 별도 설계가
  아니라 feature availability 상태**가 된다(§6).
  ⑵ **최근성 감쇠를 신호 설계에 넣었다**(§3-3 ⒝). 이전 논의에 없던 축이다.
  ⑶ **Cornac을 기각에서 채택으로 옮겼다**(§9-1). 이전에 MF/CF 라이브러리로 묶어 기각했는데, **모델
  라이브러리로서와 실험 하네스로서는 다른 용도**이고 후자로는 맞다.

  **유지한 판단 둘, 근거를 다시 세워서**:
  ⑷ **깊이는 단독으로 부호가 정해지지 않는다**(§5-2). 포화 압축은 크기 문제를 풀지 부호 문제를 풀지
  않는다. 다만 "깊이를 라벨로 쓰지 말라"에서 **"깊이 × 해소로 쓰라"**로 바뀌었다 — 재질의 신호를 곱하면
  네 칸이 전부 해석 가능해지고, 깊이를 버리지 않아도 된다.
  ⑸ **첫날 항에 인기도를 쓰지 않는다**(§6-1). 근거를 규율에서 측정으로 옮겼다 — 첫날의 인기도는
  실력이 아니라 **우리 자신의 첫 노출 결정**을 재고, 대체재(persona 친화도)가 이미 존재한다.

  ★규율 하나: **설계 문서의 뼈대가 "하지 말아야 할 것"이면 그 문서로는 시스템을 만들 수 없다.**
  금지 목록은 결정을 좁히지만 구조를 주지 않는다. 같은 판단이라도 **무엇을 만드는가**를 뼈대로 다시
  놓으면 어디에 붙는지가 정해지고, 붙을 곳이 없는 판단은 그때 걸러진다.
- **2026-08-28 rev 2** — §10(시스템 구조) 신설. 오픈소스를 도입했을 때 agent-discovery-api가 어떤
  모양이 되는지를 배포 단위·저장소·모듈 트리·이미지 분리·요청 경로 변화·열화·올인원 대조 일곱 개 표로
  정리했다. 뒤 절은 §11–§13으로 밀렸다(내용 변경 없음). Q9·Q10 추가.

  구조 전체를 결정하는 선택은 **§10-1 하나**다 — 모델을 서비스가 아니라 **파일**로 둔다. 그 결과
  요청 경로에 새 의존이 생기지 않고, 학습 계층이 통째로 죽어도 추천이 나오며(§10-7), 대가는 모델
  버전을 요청마다 로그에 남겨야 한다는 것 하나다.

  ★규율 ⑵: **"라이브러리로 쓴다"는 결론은 배포 단위와 이미지까지 그려 봐야 검증된다.** §9-4에서
  "모델은 파일이고 학습 잡은 같은 repo"라고 적었지만, 그것만으로는 서빙 이미지에 학습 스택이 딸려
  들어가는 것을 막지 못한다 — 같은 repo·두 설치(§10-5)와 **import 경계 테스트**가 있어야 그 결론이
  실제로 강제된다. 결론과 강제점은 다른 것이고, 강제점은 구조를 그려야 보인다.
- **2026-08-31 rev 3** — `recsys_opensource/` 조사를 §9 판단의 근거로 연결하고, 사람이 정하는 표면별
  고정 가중치처럼 읽히던 표현을 **surface를 입력으로 받는 LR → GBDT ranker**로 정정했다. Cornac Q6은
  model zoo + 우리 request-replay evaluator로 방향을 종결했다. §11에 공급 capacity와 CF 밀도/재방문
  gate를, §12에 recommendable opt-in·첫 relevance 라벨·모델 삭제 SLA·양면 목표·관점 다양성을 추가했다.
  off-policy·랭커·집중도 조사는 각각 별도 survey 장으로 두되 요구사항과 채택 판단은 이 문서만 소유한다.
