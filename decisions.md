# 결정 레지스터 (재설계, 2026-09-08~)

> 결정은 이 문서만 갖는다. 설계 문서는 결정을 인용하되 새로 내리지 않는다. 정해지지 않은 것을 정해진 것처럼 쓰지 않는다. 이전 레지스터(D01–D26)는 `archive/2026-09-08_pre-redesign/decisions.md`에 있고 **유효하지 않다.**
>
> 출처: `오너` = 오너가 말함 · `분석` = 코드·문서에서 도출.

| # | 결정 | 출처 | 날짜 | 어디에 반영 |
|---|---|---|---|---|
| R01 | 이전 문서의 결정·제약을 전제하지 않는다. 전부 아카이브하고 새로 판단한다 | 오너 | 2026-09-08 | `archive/2026-09-08_pre-redesign/` |
| R02 | 추천은 세 타입: ① 자기 agent에게 free text topic + context로 명시 요청, ② 탐색 서브탭에서 내 topic으로 대화해 볼 타인 agent, ③ 탐색 메인에서 비슷한 사람들이 좋아한·내가 좋아할 agent | 오너 | 2026-09-08 | 재설계 §2 |
| R03 | 타입 ①은 뽑힌 topic(상한 3)을 모두 가진 agent가 먼저, 그다음 일부만 가진 agent. 1차 키 커버 수, 2차 키 점수 | 오너 | 2026-09-08 | 재설계 §2-1, 계약 §4-4 |
| R04 | 타입 ②는 topic별 섹션 목록 | 오너 | 2026-09-08 | 재설계 §2-2, 계약 §2-2 |
| R05 | 타입 ③은 상호작용 로그가 쌓이기 전까지 인기도로 시작해도 된다 | 오너 | 2026-09-08 | 재설계 §2-3 |
| R06 | topic·agent의 default는 private. 유저가 public/friends로 공개하고 다시 비공개로 되돌릴 수 있다. friends tier는 처음부터 후보에 든다 | 오너 | 2026-09-08 | 재설계 §3, 계약 §5 |
| R07 | 탐색 탭은 클라이언트가 `/api/svc/agent-discovery`를 직접 호출한다(topic-api의 `/api/svc/topic`과 같은 형태). 타입 ①은 bourbon-agent가 내부 API로 | 오너 | 2026-09-08 | 계약 §1 |
| R08 | 저장소는 MySQL, PostgreSQL, DynamoDB, Redis, OpenSearch 안에서. 다른 저장소 추가는 선호하지 않는다 | 오너 | 2026-09-08 | 재설계 §0·§5 |
| R09 | 오픈소스 중심안(A)과 직접 구현안(B)을 둘 다 만들고 10만 합성 유저로 지연 시간·비용·재계산·품질을 비교해 최종 선택한다. 섞을 수 있다. **R34(2026-09-09)로 개정** — 검증 결과가 방향을 정해 비교 없이 단일 구현으로 | 오너 | 2026-09-08, 개정 2026-09-09 | 재설계 §5·§6, `synthetic_population_spec.md` |
| R10 | 추가할 이벤트는 agent-discovery-api에서 먼저 정의·발행·테스트하고 그 뒤 발신 repo에 요청한다. 발행 repo가 그 지점에서 그 필드를 갖는지 먼저 검증한다 | 오너 | 2026-09-08 | 이벤트 정의서 §3·§4 |
| R11 | public agent와의 대화는 친구가 아니어도 시작할 수 있게 한다. 방법(방 종류)은 bourbon-api의 몫이고 DM이 유력 | 오너 | 2026-09-08 | 이벤트 정의서 §2-4 |
| R12 | 기존 `POST /recommend`의 필드명은 유지하지 않는다. 타입별로 route와 스키마를 나눈다 | 오너 | 2026-09-08 | 계약 머리말·§0 |
| R13 | 유저 규모는 20만 → 100만 → 1천만 → (1억) 단계로 대응한다. 처음부터 최대 규모를 요구하지 않는다 | 오너 | 2026-09-07 (재설계에서도 유효, 2026-09-08 재확인) | 재설계 §7 |
| R14 | topic 변경은 이미 있는 `bourbon.topics_updated`를 힌트로 받아 topic-api를 다시 조회한다. 스냅샷 이벤트를 새로 요청하지 않는다 | 분석 | 2026-09-08 | 이벤트 정의서 §2-1 |
| R15 | 대화 시작 이벤트는 bourbon-api가 방을 생성할 때 낸다(처음 안 `agent_dm_opened` → **R46(2026-09-10)으로 일반 이벤트 `room_created`로 개정**). turn은 이미 있는 `message_created`로 세고 새 turn 이벤트는 요청하지 않는다 | 분석 | 2026-09-08 | 이벤트 정의서 §2-4·§2-5 |
| R16 | 다른 유저의 agent와 대화해 배우는 것은 이 서비스의 핵심 feature다. 따라서 (a) 합성 스펙의 대화율 λ=3은 너무 작다고 보고 검증은 {3, 10, 30}을 모두 돈다, (b) CF가 배우는 신호는 "대화를 시작했다"가 아니라 turn 수·재방문으로 가중한 confidence다 | 오너 (a) · 분석 (b) | 2026-09-08 | 합성 스펙 §2·§7·§8, 계약 §7 `cf_score` |
| R17 | 요청자의 친구 집합은 `bourbon.friendship_changed`로 우리 저장소에 미러한다(요청 시 bourbon-api 조회·TTL 캐시는 쓰지 않는다). visibility 판정 `tier = public ∨ (tier = friends ∧ owner ∈ friends(요청자))`는 **후보 조회 쿼리 안에서** 한다 — 공개된 row 저장소를 읽는 소스(`topic_index`, `content_similarity`, PostgreSQL `popularity`, 이웃 테이블)는 조건을 쿼리에 넣어 `limit`만큼만 읽고, 요청자별 사전 계산은 배치 시점 친구 집합으로 후보를 제한한다. 넉넉히 가져와 뒤에서 거르는 것은 조건을 넣을 수 없는 소스(외부 엔진 등)만이다. 응답 직전 재확인(불변식 4)은 그대로 남는다 | 오너(방향) · 분석(근거: 타입 ①② 쿼리는 이미 그렇게 동작하고 친구 5,000 배열에서 p95 2.3 ms, `agent_discovery_walkthrough.md` §3-2) | 2026-09-08 | 계약 §4-2·§4-3·§5·§6, 재설계 §3-3, 이벤트 정의서 §2-7, walkthrough §1·§2-1·§5 |
| R18 | 저장소 배치. **조인·집계가 있는 집합은 PostgreSQL**(`visible_topic_rows`, `friends`, `agents`, `interactions`와 turn 카운터, `popularity`, 이웃 테이블, `population_stats`). **단건 키 읽기·쓰기만 있는 집합은 DynamoDB**(`cf_candidates` PK `user_id`, **TTL 없음** — 두면 돌아온 유저가 콜드스타트 경로로 밀린다(R19); 결정 로그 PK `recommendation_id` + 평가용 GSI `type, served_day` + TTL; `event_log` TTL 180일 — **DynamoDB key space는 R44(2026-09-10)로 개정**: 테이블 하나, prefix 키, shard). **Redis는 deferq만** 쓰고 우리 데이터는 두지 않는다(인기도 sorted set·topic 목록 캐시 없음). 처음부터 이렇게 두고 규모에 따라 옮기지 않는다. 근거: Redis는 메모리를 선구매하고 DynamoDB는 사용량 과금이라 10만~100만 구간에서 한 자릿수 배 저렴, 결정 로그는 나중에 옮기면 과거분이 갈라짐, 플랫폼에 DynamoDB 운영 절차(IAM·dynamodb-local·배포)가 이미 있음. 감수: GetItem p50 3~5 ms(Redis 1 ms 미만), 결정 로그 집계는 SQL 대신 GSI Query·스캔 코드. (Gorse의 자체 저장소는 R33으로 Gorse가 제외되어 해당 없음) | 오너 | 2026-09-08 | 계약 §4-2·§6·§8, 재설계 §5-1·§5-2·§6-1·§6-3·§7, 합성 스펙 §8-2, walkthrough §2·§5·§7 |
| R19 | 타입 ③ 사전 계산(`cf_candidates`)은 **읽혔거나 활동한 유저만, 그 뒤에** 갱신한다(2026-09-09 개정 — 처음 안은 "활성 창 안 유저 전원 매일 배치"였다). 전역 CF 모델 학습은 주기적(예 하루 1회)이고 전원의 `interactions`를 쓴다. 유저별 top-K는 워커의 **주기 스윕**(예 30분)이 PostgreSQL에서 대화가 1건 이상인 유저 중 `last_active_at > cf_candidates_computed_at`(만든 뒤 읽혔거나 활동했다, NULL은 −∞) AND (`cf_candidates_computed_at` < now − TTL(예 24시간) OR 그 뒤 새 대화가 있다)인 유저를 `ORDER BY last_active_at DESC LIMIT n`으로 모아 **배치로** 만들고(엔진 무관 — `implicit`이면 recommend 한 번, walkthrough §5-3) DynamoDB에 쓴 뒤 `agents.cf_candidates_computed_at`을 갱신한다. `last_active_at`은 우리 API 요청·`room_created`의 creator·`topics_updated`로 갱신한다. **오래된 항목은 그대로 한 번 서빙하고**, 그 요청으로 갱신 조건이 성립해 다음 스윕이 잡는다(재확인·이미 대화한 상대 제외가 안전장치). 항목이 없는 유저(대화 0건)는 콜드스타트 경로(인기도 + content, `basis` 표시, `degraded` 비움)로 완전한 목록을 받고, 첫 대화 이벤트 뒤 스윕이 첫 항목을 만든다. deferq에 유저별 task로 넣지 않는 이유: API가 예약을 몰라도 되고, 모아서 행렬곱 한 번이 유저 하나씩보다 싸고, deferq에는 재시도가 없어 실패한 task는 사라지지만 스윕 조건은 다음 사이클에도 참이라 스스로 다시 잡는다. 비용은 그날 읽혔거나 활동한 유저 수에 비례한다(전원·MAU가 아니다). 외부 엔진(Gorse)은 이 읽기 기준 갱신을 할 수 없어 전원 상시 재계산이 된다 — Gorse를 제외한 이유 중 하나(R33). **후보(소유자) 쪽은 활동 여부로 제외하지 않는다** — `recency`·`popularity`가 순위로 내린다(하드 제외는 하지 않는다 — R31) | 오너 | 2026-09-08, 개정 2026-09-09 | 계약 §2-3·§3-4·§6 `agents`·`cf_candidates`·§7, 재설계 §2-3·§7, 합성 스펙 §8-1, walkthrough §2-1·§5-3·§5-6 |
| R20 | 타입 ③ 최종 목록은 **점수 구간 안에서만 섞어** 내린다. 랭커가 `score desc`로 정렬한 뒤 구간(예 상위 5·다음 15·다음 30, 경계는 설정)을 나누고 구간 안 순서만 랜덤. seed는 `recommendation_id`라 다음 페이지가 같은 순서를 잇고 결정 로그(`shuffle.seed`, `shuffle.bands`)로 재현된다. CF pool K는 100~200으로 넉넉히 둔다. 타입 ①②는 섞지 않는다(①은 R03의 커버리지 순서). 검증(R34)의 정확도 지표는 섞기 전 순서로 재고 섞기의 효과는 결정 로그로 따로 본다. O15(탐색 항)의 첫 답이다 | 오너 | 2026-09-09 | 계약 §2-4·§4-4·§6·§8, 재설계 §2-3·§6-3, 합성 스펙 §4, walkthrough §1·§2-1·§5-5·§6·§9 |
| R21 | 저장소·필드 이름은 **내용물**로 짓는다(만든 방법이나 쓰는 화면이 아니라). 유저별 CF 후보 스냅샷은 `cf_candidates`(항목 `candidates[(owner_user_id, score)]`, `computed_at`, `model_version`), 그 시각은 `agents.cf_candidates_computed_at`, 축소 값은 `cf_candidates_stale`, 워커 작업은 `fit_cf_model`(전역 학습)·`refresh_cf_candidates`(스윕). 소스 이름(`popularity`, `content_similarity`, `cf_*`)과 같은 줄에 서고 `<source>_candidates` 패턴으로 확장된다. 공개된 row 테이블은 `visible_topic_rows`(요청자에게 보일 수 있는 tier의 row — `open`은 tier인지 상태인지 모호했다). 이전 이름 `precomputed_for_you`·`cf_computed_at`·`stale_precompute`·`open_topic_rows`는 폐기. route `/discover/for-you`와 `basis` 값은 제품 언어라 그대로 | 오너 | 2026-09-09 | 전 문서 |
| R22 | 요청자 **자신의** 프로필(타입 ②의 섹션, 타입 ③ content 입력)은 본인의 `public`·`friends`·`private` topic을 읽고 **`hidden`은 읽지 않는다.** 유저가 명시적으로 숨긴 것은 추천에도 반영하지 않는다 — hidden의 기획적 의미가 바뀌면 그때 추가한다. private을 포함하는 이유: 타인 agent 방에서 R이 한 말은 bourbon-agent가 R의 persona로 추출해 topic-api가 grounding하는데 새 topic은 default private이라, private을 빼면 그 대화에서 배운 것이 프로필에서 사라진다. topic-api 내부 route 호출은 `visibility=public&visibility=friends&visibility=private`(타인 row 재조회의 `public`·`friends`와 다르다). 어디서 읽나는 R27 | 오너 | 2026-09-09 | 계약 §2-2·§6 `requester_topics`·§9-1, 재설계 §2-3·§3-1, 이벤트 정의서 §0·§5, walkthrough §4-1 |
| R23 | agent 공개 여부는 bourbon-api personal agent의 **새 필드 `discoverable`(기본 false)**로 한다. `enabled`는 재사용하지 않는다 — `enabled=false`는 agent가 동작하지 않는다는 뜻이고 "쓰되 목록에는 안 올린다"는 다른 선택이다. 뒤에 필드 정의가 바뀌면 그때 수정한다. O1 닫힘. **2026-09-10 플랫폼 이벤트 기준으로 재검토(R46 근거) — 유지(오너)**: 필드의 소유자가 bourbon-api고 변화를 알 다른 길은 폴링뿐이라 이벤트가 맞다. 유저가 쓰는 boolean 필드의 변경이라 `friendship_changed`와 같은 부류 **R57(2026-09-13)로 개정 — 그 필드는 만들어지지 않았고, 같은 사실을 재조회가 파생한다** | 오너 | 2026-09-09, 재확인 2026-09-10, 개정 2026-09-13 | 이벤트 정의서 §0·§2-3·§3·§5·§6, 계약 §9-1, 재설계 §1-2·§4-2 |
| R24 | `recommendation_id` 전달을 **지금 요청 범위에 넣는다.** 경로: 우리 응답 → (타입 ①) bourbon-agent가 추천 카드의 meta에 실어 bourbon-api를 거쳐 클라이언트로 / (타입 ②③) 클라이언트가 우리 응답에서 직접 → 클라이언트가 방 생성 등 대화 시작 요청에 `recommendation_id`를 실어 bourbon-api에 → bourbon-api가 `agent_dm_opened`에 실어 발행. bourbon-agent·bourbon-api·클라이언트 셋에 요청한다. O3 닫힘. **R47(2026-09-10)으로 개정** — bourbon-api를 거치지 않고 클라이언트가 우리 route에 직접 보고한다 | 오너 | 2026-09-09, 개정 2026-09-10 | 이벤트 정의서 §2-4·§5·§6, 계약 §9-1 |
| R25 | **content 유사도의 정의**(O18 제안 채택; **모집단은 R58(2026-09-13)로 정밀화 — "답에 나올 수 있는 소유자"**): (a) IDF 가중 `log(전체 소유자 수 / 그 topic을 가진 소유자 수)`; (b) 카탈로그 계층 감쇠 — 요청자 topic과 소유자 topic 사이의 양방향 최단 hop에 `0.6^hop`, 최대 3 hop(상수는 topic-api 검색 설정 `SEARCH_DEFAULT_DECAY`·`SEARCH_MAX_DEPTH`에서 빌림 — topic-api는 자손 방향에만 쓴다), 경로당 가장 구체적인 일치 하나만(하위 정확 일치 1.0 > 부모만 0.6), drawer(`selectable: false`)는 통과 노드; (c) 임베딩 kNN은 넣지 않는다 — topic-api의 grounding 실패 preference 비율이 나쁠 때 다시 본다. "대화한 agent의 topic을 프로필에 더하는" 경로는 넣지 않는다(agent 발화는 evidence가 아니다). 값은 설정(`agent_discovery_settings.md`). O18 닫힘 | 오너 | 2026-09-09 | 계약 §7 `content_similarity`, 재설계 §2-3·§5-2, walkthrough §5-2, 합성 스펙 §4 |
| R26 | 카탈로그 계층은 **topic-api repo의 `data/catalog_dist/catalog.json`을 빌드 때 우리 이미지에 복사**해 edge 테이블을 채운다. 카탈로그가 바뀌면 재배포한다(구현은 복사본을 우리 repo에 커밋하는 방식 — `implementation_plan.md` §1-2, 효과는 같다). 내부 route(이벤트 정의서 §2-8)는 **지금 요청하지 않고** 보류 — 두 서비스의 카탈로그 버전이 어긋나는 일이 잦아지면 그때 요청한다. 어긋남 감지: 카탈로그 `built_at`을 결정 로그에 기록하고, topic-api 응답에 우리 카탈로그가 모르는 topic_id가 오면 그 topic은 정확 일치만 적용하고 건수를 지표로 남긴다 | 오너 | 2026-09-09 | 이벤트 정의서 §0·§2-8·§3·§4·§6, 재설계 §5-2, walkthrough §5-2 |
| R27 | 요청자 자신의 topic 목록은 **요청 시 topic-api 내부 route로 읽는다**(O2 (a)). `visibility=public&visibility=friends&visibility=private`(R22). 미러(b)는 지연 시간이 문제될 때의 대안으로만 남긴다. O2 닫힘 | 오너 | 2026-09-09 | 계약 §2-2·§6 `requester_topics`·§9-1, 재설계 §2·§3-1, 이벤트 정의서 §0·§5, walkthrough §2-1·§4-1·§6·§7 |
| R28 | 타입 ③ for-you는 **요청자가 이미 대화를 시작한 agent를 제외**한다. 다만 아직 안 보여 준 후보를 모두 소진했고 다음 갱신(스윕) 전이면, 미대화 후보 뒤에 다시 나올 수 있다. "이미 대화한 상대" 표식(`talked_before`)은 **코드와 결정 로그 `ranked[]`에는 두되 응답에는 싣지 않는다** — 클라이언트는 자기 방 목록으로 알 수 있고, 표시 기획이 생기면 그때 필드를 연다(같은 `contract_version`). 합성 정답도 같은 규칙. O19 닫힘 | 오너 | 2026-09-09 | 계약 §2-3·§3-1, 합성 스펙 §4, walkthrough §5-3·§5-4·§5-6 |
| R29 | **인기도 정의**: 최근 30일 창의 대화 시작을 반감기 7일로 시간 감쇠해 합산. 시작 1회를 1로 세지 않고 `cf_score`와 같은 confidence `1 + α·log(1 + turns) + β·reopen_count`로 가중. 신규 agent에는 작은 사전값 부스트(콜드스타트). 전체 최대로 정규화. 값은 설정. O4 닫힘 | 오너 | 2026-09-09 | 계약 §7 `popularity`, walkthrough §2-1·§5-1, 설정 레지스터 |
| R30 | **타입 ② 섹션**: 요청자 preference 점수순, `per_section` 기본 5(상한 20), `sections` 기본 3(상한 10), 둘 다 요청 파라미터. 더 보기는 섹션별 커서. O9 닫힘 | 오너 | 2026-09-09 | 계약 §2-2, walkthrough §4, 설정 레지스터 |
| R31 | 오래 활동하지 않은 소유자의 agent는 **후보에서 빼지 않는다.** `recency`·`popularity` 감쇠로 순위만 내린다(R19 유지). O17 닫힘 | 오너 | 2026-09-09 | 계약 §7 `recency`, walkthrough §5-5 |
| R32 | `tier_is_friends` 가중은 **작은 양의 값(초기 .05)**으로 시작하고 측정 뒤 조정한다. 친구에게만 공개한 topic이 겹치는 것은 의도적 신뢰 신호다. O12 닫힘 | 오너 | 2026-09-09 | 계약 §7, 설정 레지스터 |
| R33 | 타입 ③ CF는 **`implicit` ALS(라이브러리형)**. Gorse는 제외 — R08 안에서 cache store가 MySQL이 되고, 기본 설정으로는 CF가 서빙되지 않는 원인 둘이 있었고, 고친 뒤 HR@10이 `implicit`의 1/9(검증 §3·§4). O8 닫힘 | 오너 | 2026-09-09 | 재설계 §5-1·§5-2·§8, walkthrough §2-3·§5-3·§7 |
| R34 | **두 안 비교를 하지 않고 단일 구현으로 간다**(R09 개정). 구현 = PostgreSQL 역인덱스(타입 ①②) + 인기도 + content 유사도 + `implicit` ALS 배치·스윕(타입 ③). B안의 동시 출현 CF(`cf_item`)는 만들지 않는다 — 검증 §3에서 ALS의 절반. 합성 모집단(스펙)은 비교가 아니라 **이 구현의 품질(인기도만·content만·CF만·합친 랭커 ablation)·부하·CF 게이트 검증**에 쓴다. 계약 §4의 소스 인터페이스는 그대로 두어 뒤에 소스를 추가할 수 있게 한다. O11 닫힘 | 오너 | 2026-09-09 | 재설계 §0·§5·§6·§9, 계약 §4·§10, 합성 스펙 머리말, walkthrough 머리말, README |
| R35 | **CF 가중 자동 게이트**(O14 제안 채택): `w_cf`는 0에서 시작. 배치가 주기적으로 실제 로그 leave-one-out에서 ALS를 인기도와 비교하고, HR@10과 군집 다양성에서 이길 때만 가중을 올린다. λ 추정으로 시점을 정하지 않는다. O14 닫힘 | 오너 | 2026-09-09 | 계약 §7 `cf_score`, walkthrough §5-5·§8, 설정 레지스터 |
| R36 | **타입 ③ 갱신 값**(O13): TTL 24시간, 스윕 주기 30분, 사이클당 상한 5,000명, 전역 학습 하루 1회, `cf_candidates_stale` 임계 7일. 전부 설정이고 실측 뒤 조정. O13 닫힘 | 오너 | 2026-09-09 | 계약 §3-4, walkthrough §2-1·§5-3·§5-6, 설정 레지스터 |
| R37 | **파라미터 자동 보정 값**(O16 제안 채택): 하루 1회, 90일 창, 활성 유저 1,000명·대화 시작 5,000건 미만이면 기본값 유지, `event_log` 보존 180일. O16 닫힘 | 오너 | 2026-09-09 | 합성 스펙 §8, walkthrough §8, 설정 레지스터 |
| R38 | **탈퇴 뒤 돌아온 유저는 신규 가입으로 본다.** 재활성 기획이 아직 없다. 재활성이 들어오면 그때 이벤트와 처리를 추가한다. O7 닫힘 | 오너 | 2026-09-09 | 이벤트 정의서 §5, 계약 §9-1 |
| R39 | **설정값은 한곳에서 본다.** 기획 문서에서는 `agent_discovery_settings.md`(설정 레지스터)가 모든 설정의 이름·기본값·뜻·값을 올리고 내릴 때의 효과·출처 결정을 갖고, 코드에서는 설정 클래스 하나가 같은 이름과 설명을 갖는다. 다른 문서는 값을 쓰지 않고 이 레지스터를 가리킨다. **R54(2026-09-12)로 개정** — 값은 코드가 갖고 이 레지스터는 뜻을 갖는다 | 오너 | 2026-09-09, 개정 2026-09-12 | `agent_discovery_settings.md`, 계약 §7, README |
| R40 | **모든 요청은 `lang`을 받고(기본 `en`), 응답의 다국어 필드는 그 언어의 문자열 하나다.** 허용 값은 우선 **`ko`, `en`, `ja`** 세 가지(topic-api 카탈로그 라벨과 같다) — 그 밖의 값은 422. `labels: {ko, en}` 맵 대신 `label: "…"`, `owner_note`도 문자열 하나. 폴백: 요청 언어 → `en` → 있는 첫 값 → null | 오너 | 2026-09-09 | 계약 §2·§3, walkthrough §3-5·§4-3 |
| R41 | 첫 버전의 추천 카드에 **"안 맞음"(부정) 표시는 없다.** 보이는 근거는 긍정 근거(겹치는 topic 등)만이다. 표시의 축("나와 비슷해요 / 다른 관점도 살펴봐요")은 기획 중(O5) | 오너 | 2026-09-09 | 계약 §3-1 `fit`, 재설계 §8 |
| R42 | **persona(HEXACO)를 타입 ③에 쓴다 — 방향과 코드 자리만.** bourbon-agent의 persona 중 topic으로 오지 않는 것은 HEXACO 25 facet 추정치(1~5, confidence, 관측 수)·자유 성향·traits 서술형 텍스트·bio 넷이고, 기계적으로 비교 가능한 **HEXACO 벡터만** 대상이다. (a) bourbon-agent의 persona 테이블을 직접 읽지 않는다 — **복호화 키를 공유하지 않는다**(topic-api가 그렇게 읽는 선례를 따르지 않음). 받는 경로는 **이벤트**로 둔다(확정 아님, O20) — bourbon-agent에 API(내부 route)를 열 가능성은 없다(오너). (b) 뒤에 붙이기 쉽게 **코드에 자리를 먼저 잡는다**: 타입 ③ 쿼리의 요청자 성향 입력(`UserQuery.requester_traits`, 지금은 항상 None), 랭커 feature `persona_similarity`, 설정 `rank.for_you.w_persona`(시작값 0). 입력이 없으면 feature는 0이고 `present`에 없다 — **지금 응답은 바뀌지 않는다.** 구체 경로·동의 범위·가중 올리는 규칙·검증은 O20 | 오너 | 2026-09-10 | 계약 §2-3·§4-1·§4-4·§7·§11, 설정 레지스터 §2·§11, 이벤트 정의서 §0·§2-7·§6, 재설계 §1-1·§1-2·§2·§2-3·§2-4·§8·§9, 합성 스펙 §7, walkthrough §5-5·§9 |
| R43 | **구현 순서와 환경.** 인프라는 요청하면 곧 준비되므로 **로컬 Docker(PostgreSQL·dynamodb-local·valkey·rabbitmq·topic-api 목 서버)에서 먼저 구현·검증**하고, 1차 배포가 준비될 즈음 인프라 팀에 요청한다. 산출물에 **합성 데이터 시드 스크립트와 테스트 CLI**를 반드시 포함한다. 인프라 선행 조건과 외부 이벤트는 받는 서비스별(인프라·bourbon-api·클라이언트·bourbon-agent·topic-api) **요청서를 미리 써 둔다**(`requests/`). 그 외 순서는 분석 권고대로(`implementation_plan.md`) | 오너 | 2026-09-10 | `implementation_plan.md`, `requests/`, 재설계 §9, README |
| R44 | **DynamoDB는 서비스 소유 테이블 하나**(`bourbon-agent-discovery-tokyo-{env}`) — 인프라 방침(소유 서비스별 1테이블, 엔티티는 PK/SK prefix key space로). 둘 이상이 필요하면 성능·비용 근거를 요청서에 적는다(지금은 필요 없다). topic-api가 받은 키 설계 피드백 둘을 처음부터 반영: 주 읽기 패턴가 Query가 되게 날짜를 파티션 키에 넣고, 하루치 쓰기가 한 파티션에 몰리지 않게 **shard를 키 포맷에 지금 박는다**(`{…}#{shard}`, N은 설정 `dynamodb.log_shards`). key space는 계약 §6-1. dev 테이블은 방침대로 우리가 직접 만들고, prod는 terraform 코드화 시점에 포함한다 | 오너(방침) + 분석(key space) | 2026-09-10 | 계약 §6-1·§8, 설정 레지스터 §9, `requests/infra.md` §2 |
| R45 | **PostgreSQL 사용 확정.** 인프라 팀과 이야기해 agent-discovery-api가 PostgreSQL을 쓰기로 확인했고, 요청 시 추가해 주기로 했다. R18의 배치는 그대로다. `requests/infra.md` §1의 근거는 기록으로 남기고 MySQL 대안 질문은 뺀다. O21 닫힘 | 오너(인프라 확답) | 2026-09-10 | `requests/infra.md` §1·§6, `implementation_plan.md` §0, README |
| R46 | **대화 시작은 bourbon-api의 일반 이벤트 `bourbon.room_created`로 받는다** — 우리 전용 `agent_dm_opened` 요청을 대체한다(R15 개정). payload `room_id, room_type, creator_id, member_user_ids, agents[{agent_id, owner_user_id}], occurred_at`. `reopened`는 뺀다 — "전에 있었다"를 알아야 하는 상태 이벤트다. `entry`·`recommendation_id`도 뺀다(R47). 근거는 플랫폼의 이벤트 추가 기준(2026-09-10 공유): **판정 기준이 명확하고 소비자가 여럿인 것만 이벤트로, 상태를 내포하지 않게(stateless), 단일 소비자·사후분석용은 그 서비스 안이나 로그 기반으로.** 방 생성은 row insert라 판정이 명확하고 알림·분석 등 다른 소비자가 있을 수 있다. `reopened`·`entry`·`recommendation_id`는 셋 다 이 기준에 걸렸다. 우리 처리: `room_type == agent_dm`만 받아 actor = `creator_id`, 소유자 = creator가 소유하지 않은 착석 agent의 `owner_user_id`. 자기 agent 방(착석 agent가 모두 creator 소유)·`user_dm`·`group`은 무시. 재방문은 이벤트로 받지 않고 필요하면 `message_created` 간격으로 우리 안에서 계산한다(`interactions.reopened_count` 삭제, confidence의 β항 제거). **기각 시 대안**(요청서에 같이 적는다): (a) bourbon-api 내부 route로 room 구성 조회 — `message_created` 첫 건에서 1회, 모든 대화를 본다, DB 직접 접근이 아니다 (b) agent DM room id는 `uuid5(DM_NAMESPACE, "dm:user:{user}:agent:{agent}")`로 결정론적이라 추천 시점에 후보별 room id를 계산해 두고 `message_created`와 매칭 — 추천된 대화만 보이고 bourbon-api의 id 규칙에 결합되므로 마지막 수단 **R51(2026-09-11)로 개정 — bourbon-api가 기각하고 대안 (a)를 제안했다.** | 오너 | 2026-09-10, 개정 2026-09-11 | 이벤트 정의서 §0·§2-4·§3·§4·§6, 계약 §6·§8·§9-1, walkthrough §2-1·§2-2·§5-3·§6, 합성 스펙 §4·§7·§8-1, 구현 계획 1-4·1-10, 재설계 §4-2, 요청서 bourbon-api·README |
| R47 | **어트리뷰션은 클라이언트가 우리 route에 직접 보고한다**(R24 개정). 카드에서 대화를 시작할 때 `POST /api/svc/agent-discovery/attributions` `{recommendation_id, owner_user_id, entry}` — 요청자는 edge-auth `x-user-id`. bourbon-api route 파라미터(`recommendation_id`·`entry`)와 이벤트 필드 요청은 철회한다. 근거: 우리만 읽는 사후분석 값이라 플랫폼 기준으로 "그 서비스 안에서 풀 문제"고, 다른 서비스의 route·이벤트에 우리 필드를 얹지 않게 된다. 조인은 우리 안에서: `attributions`(PostgreSQL)에 두고 `room_created`(R46)가 같은 `(actor, owner)` 쌍으로 오면 `attribution.window_hours`(24) 안의 최근 보고 하나를 `interactions.entry`·`recommendation_id`에 채운다. 없으면 `entry = direct`. 보고는 best-effort(앱 종료 등으로 빠질 수 있다) — 측정용 값이라 감수한다. 타입 ①의 카드 meta 경로(bourbon-agent)는 그대로 — 클라이언트가 meta에서 읽어 같은 route로 보고한다 | 오너 | 2026-09-10 | 계약 §2-5·§6·§8·§9-1, 이벤트 정의서 §0·§2-4·§5·§6, 설정 레지스터 §9, walkthrough §2-1·§2-2·§5-3, 구현 계획 1-4·1-10, 재설계 §4-2, 요청서 client·bourbon-agent·bourbon-api·README |
| R48 | **topic-api에는 visibility 변경 신호의 "필요"만 요청하고 형태는 topic-api가 정한다.** 필요: 유저가 topic의 visibility를 바꾸면 `user_id`(가능하면 `topic_id`)가 담긴 이벤트 하나. 우리가 제안했던 새 타입 `user_topic_settings_updated`의 payload(`touched` 포함)와 `persona_revision` 변경은 요청하지 않는다 — `persona_revision`은 topic-api의 출처 추적용 필드(추출이 읽은 persona 슬롯 버전)로 지금 어느 소비자도 읽지 않고 우리도 읽지 않는다. 의견만 적는다: 플랫폼의 "타입 추가 신중" 기준에는 기존 `topics_updated`를 settings 쓰기에서도 발행하는 쪽이 가깝다. 우리 처리는 어느 형태든 같다(유저 단위 debounce → 재조회). 우리 미러는 답이 오기 전까지 `bourbon.user_topic_settings_updated` `{user_id, topic_id, topic_revision}`을 **임시 이름**으로 둔다. **R55(2026-09-13)로 답이 왔다** — 새 타입 `bourbon.topic_visibility_changed` `{user_id, topic_id}`, revision 없음 | 오너 | 2026-09-10, 답 2026-09-13 | 이벤트 정의서 §0·§2-2·§3·§6, 계약 §9-1, 설정 레지스터 §9, walkthrough §2-2, 합성 스펙 §4·§7, 구현 계획 1-4, 재설계 §4-2, 요청서 bourbon-topic-api·README |
| R49 | **`interactions`의 정의와 채우는 규칙 확정.** 대화(방) 단위 한 행 `(actor_user_id, owner_user_id, room_id)` + `started_at, turns, entry, recommendation_id`. 행 생성은 `room_created`(R46: actor = creator, 소유자 = creator가 소유하지 않은 착석 agent의 소유자), `entry`·`recommendation_id`는 `attributions` 조인(R47, 없으면 `direct`/null), `turns`는 `message_created`(`room_type == agent_dm`, `sender_type == user`)로 센다 — agent 답장은 세지 않는다. **turn 카운터는 `room_turns(room_id, turns, last_turn_at)`로 따로 두어 이벤트 순서에 의존하지 않는다**: `message_created`와 `room_created`는 다른 큐라 순서 보장이 없고, 카운터를 방 키로 독립시키면 어느 쪽이 먼저 와도 버리거나 보류할 것이 없다. 읽는 쪽(인기도·CF·평가)이 `room_id`로 조인하고, `interactions` 행이 없는 `room_turns`(우리 서비스 이전에 열린 방, 시작을 놓친 방)는 조인에서 빠지며 `room_turns.orphan_ttl_days`(7) 뒤 정리한다. `last_turn_at`은 뒤에 재방문을 우리 안에서 계산할 재료(R46). 자기 agent 방·user DM·group은 행을 만들지 않는다. 탈퇴는 owner 행 삭제, actor 행은 actor 익명화 | 오너 | 2026-09-10 | 계약 §6·§9-1, 이벤트 정의서 §2-5, walkthrough §2-1, 설정 레지스터 §9, 구현 계획 1-4 |
| R50 | **`friends` 미러는 삭제하지 않고 상태와 이벤트 시각을 남긴다.** `friends(user_low, user_high, state, changed_at)`, `state ∈ {friends, removed}`. `accepted`·`removed` 모두 upsert이고, **자기보다 새 `changed_at`이 이미 있으면 적용하지 않는다**. 읽는 쪽은 전부 `state = 'friends'`를 붙인다. 근거: deferq는 `prefetch_count = WORKER_CONCURRENCY`로 한 큐에서 메시지마다 태스크를 돌리므로 한 쌍에 대한 이벤트 둘이 **커밋 순서**로 정착하고, 재시도 backoff(1·2·4·8초)가 그 창을 초 단위로 벌린다 — `accepted`가 뒤에 커밋하면 **끊은 친구가 계속 친구로 남고**, friends tier는 후보 조회 쿼리 안의 필터라 끊은 상대에게 계속 row가 나간다. 조용하고 프라이버시에 닿는다. 지우는 설계로는 고칠 수 없다: `removed`가 row를 지우면 늦게 온 `accepted`에게 비교할 시각이 없다. 같은 문제를 `agents.discoverable`은 `visibility_changed_at`으로 고쳤고(1-4d), 그쪽은 row가 남아 있어서 가능했다. 대가 둘을 감수한다: 끊긴 쌍이 tombstone으로 남으므로 정리 정책이 필요하고(설정 `friends.tombstone_ttl_days`), R17이 정한 "값 없는 순수한 쌍 미러"가 아니게 된다. **읽는 쪽 한 곳이라도 조건을 빠뜨리면 끊긴 친구가 통과하므로**, 저장소가 `state`를 노출하지 않고 "친구 집합"만 답하는 하나의 읽기로 감싼다 | 오너 | 2026-09-11 | 계약 §6, 재설계 §4-2, 설정 레지스터 §9, 구현 계획 1-4·1-5 |
| R51 | **`bourbon.room_created` 요청을 철회하고, 대화 시작은 `message_created` + 우리가 미리 계산한 room id로 안다**(R46 개정). bourbon-api가 기각하며 대안을 줬다 — 기존 내부 route `GET /api/internal/rooms/{room_id}/agent-context`. 그런데 우리에게 필요한 건 **소유자 하나**뿐이다: `room_type`·actor는 `message_created`가 이미 싣고, 그 응답에는 방 종류도 creator도 생성 시각도 없으며, 대신 **이용자들의 메시지 본문**이 딸려 온다(`limit` 최솟값 1). 그래서 route를 부르지 않고 **추천을 낼 때 room id를 미리 계산해 둔다**: agent DM의 room id는 `uuid5(DM_NAMESPACE, "dm:user:{actor}:agent:{agent_id}")`이고(bourbon-api `rooms/dm_identity.py`, find·create 두 분기가 같은 식을 쓴다) 응답 시점에 양쪽을 다 알기 때문이다. 저장은 DynamoDB `ROOM#{room_id}`(단건 키 읽기 + TTL = R18이 DynamoDB라고 말하는 모양), 읽기는 그 방의 **첫 turn에서 GetItem 한 번**(`room_turns`의 `turns == 1`이 곧 "이 방 처음 본다"라 공짜다). `started_at`은 첫 메시지 시각 — 방을 열고 말을 걸지 않은 것은 대화가 아니고, `room_created`가 못 보던 **재방문**도 이 경로는 본다. 대가: DM 네임스페이스 상수 하나에 결합되고(그들이 바꾸면 적중률이 0이 되며 조용하다), 어트리뷰션 창이 방 생성이 아니라 첫 메시지 기준이 된다 | 오너 | 2026-09-11 | 계약 §6·§6-1·§9-1, 이벤트 정의서 §2-4·§2-5, 재설계 §4-2, 구현 계획 1-4·1-5, 설정 레지스터 §9, 요청서 bourbon-api |
| R52 | **추천으로 시작되지 않은 대화는 기록하지 않는다.** 예측 인덱스에 없는 방은 소유자를 알 방법이 없고(그 하나를 위해 route를 부르지 않기로 했다), 우리가 관리할 것은 추천에 대한 피드백이다. 비용은 미스 한 번의 GetItem이라 사실상 0. **되돌릴 수 없는 쪽은 데이터다**: 인기도·CF 학습·R28(이미 대화한 상대 제외)은 그러면 추천 전용 표본을 읽고, 인기도는 "얼마나 인기 있나"가 "우리가 얼마나 추천했나"가 되는 닫힌 루프가 된다. 셋 다 1-7이라 지금 읽는 사람은 없지만 기록하지 않은 대화는 나중에 되메울 수 없으므로, **못 찾은 방마다 카운터를 하나 올린다**(소유자도 개인정보도 필요 없다) — 1-7 전에 그 비율을 보고 다시 정한다 | 오너 | 2026-09-11 | 계약 §6, 구현 계획 1-4·1-7 |
| R53 | **어트리뷰션 출처는 둘을 다 두고, 클라이언트 보고가 이긴다**(R47 유지 + R51). 대화가 시작되면 `attribution.window_hours` 안의 최근 **보고**를 먼저 찾고, 없으면 **예측 인덱스**의 값을 쓰고, 둘 다 없으면 `direct`다. 근거: 보고는 이용자가 우리 카드를 실제로 눌렀다는 증거고, 예측은 "그들이 연 방이 우리가 예측한 방"이라는 더 약한 증거다 — 카드를 보고 지나쳤다가 나중에 검색으로 같은 사람을 찾아가도 room id는 같다(방 id가 (유저, agent)의 함수라서). 그래서 **둘을 구별해 기록한다**: `interactions.attributed_by ∈ {report, predicted, direct}`. 구별하지 않으면 R47이 재려던 "어느 화면에서 왔나"가 누르지 않은 방문으로 부풀어 측정이 망가진다 | 오너 | 2026-09-11 | 계약 §2-5·§6·§8, 이벤트 정의서 §2-4, 구현 계획 1-4·1-5 |
| R54 | **값은 코드가 갖고, 기획 문서는 뜻을 갖는다**(R39의 값 보유 부분 개정). 설정 레지스터는 `기본값` 열을 빼고 이름·뜻·올리면/내리면·출처만 갖는다 — 값은 `agent_discovery/settings.py` 하나에 있고 레지스터의 그룹마다 그 모듈의 클래스를 가리킨다. **합성 스펙 §2는 그대로 둔다** — 서비스가 튜닝하는 값이 아니라 실험 설계이고(λ의 세 값은 R16), 한 run이 자기가 쓴 값을 `params.json`에 적어 `run_id`로 삼으므로 "어떤 모집단에서 잰 숫자인가"는 이미 그 파일이 답한다. 코드 repo에서 없어지는 것은 사본과 그것을 파싱하던 테스트다. **계약 §7·§3-4의 목록은 그대로 둔다** — 튜닝 값이 아니라 답을 이루는 어휘이고, `degraded`는 OpenAPI 스키마로 나가 소비자가 읽는다. 코드 repo의 사본(`docs/planning/`)과 `scripts/sync_planning_docs.py`, 그 사본을 파싱하던 테스트 셋을 지운다. **근거**: 사본 복사가 수동이라(CI 없음 — 플랫폼 방침) 그 테스트들이 지키던 것은 "코드가 기획과 맞다"가 아니라 **"코드가 마지막으로 누가 복사해 둔 것과 맞다"**였다. 사본이 낡으면 셋 다 초록이고, `planning.source.json`의 `source_commit`은 무엇과도 비교하지 않는다. 낡음을 알아낼 유일한 방법은 형제 체크아웃을 읽는 것인데 스크립트 자신이 "누구 노트북에서 도느냐에 따라 통과가 갈린다"며 그것을 거부했으므로, 사본은 **구조적으로 자신이 최신임을 증명할 수 없다**. 없애면 사실마다 사본이 하나라 낡음이 불가능해지고 틀림만 남는데, 틀림은 리뷰가 잡는다. 덤으로 레지스터가 아직 정하지 않은 값에 기본값을 적어 정해진 것처럼 보이게 하던 일도 사라진다 — 값은 구현 시점에 코드에서 태어난다. **대가 둘**: (a) 값을 튜닝할 때 뜻과 값이 한 화면에 없다 — 그룹마다 `settings.py`의 클래스를 가리키는 링크로 줄이고, 그래도 불편하면 코드 repo가 생성하는 값 표를 두고 레지스터가 그것을 가리킨다(생성기와 대상이 같은 repo라 "최신인가"를 진짜로 검사할 수 있다 — 지금 구조로는 못 한다). (b) 두 repo의 이름이 갈라져도 아무도 말해 주지 않는다 — 대신 코드 쪽에 자족 테스트 둘을 둔다: 모든 설정 필드에 설명이 있다(이미 있다), 모든 설정 필드에 출처(R 번호 또는 문서 절)가 있다 | 오너 | 2026-09-12 | `agent_discovery_settings.md`, 합성 스펙 §2, 계약 §7·§3-4, README, 구현 계획, 재설계 §6-4, walkthrough §8 |
| R55 | **R48의 답이 왔다 — visibility 변경 신호는 새 타입 `bourbon.topic_visibility_changed`다**(R48 확정, 임시 이름 폐기). topic-api #68·#69. payload는 `{user_id, topic_id: str \| None}` 둘뿐이고 **revision도 시각도 없다** — 그들 docstring이 이유를 적는다: 삭제 경로에는 실을 revision이 남아 있지 않고, 발행 지점마다 payload 모양이 하나여야 소비자가 "어느 쓰기였나"로 분기하지 않는다. 발행은 쓰기 **뒤** best-effort(실패는 로그만, 요청은 성공하고 트랜잭션도 없다 — DynamoDB 단건 쓰기라 쓰기와 발행 사이에 죽으면 이벤트가 없다), **움직인 row마다 한 건**이고 bulk patch는 첫 발행 실패 뒤 그 요청의 나머지를 건너뛴다. 계정 삭제는 `topic_id=None` 한 건, topic 삭제는 visibility를 안 보고 무조건 한 건. 발행 지점은 `/me/topics` 단건·bulk, 내부 PATCH, topic 삭제, 계정 삭제 다섯이다. 우리 처리는 §2-1과 같다 — `user_id`만 꺼내 유저 단위 debounce → 재조회. **결과 하나**: revision이 없으므로 "재조회가 이벤트보다 낡게 왔으면 한 번 더" 절이 이 경로에는 성립하지 않는다. 그 자리를 R56이 대신한다. 우리 미러는 `topic_id`를 optional로 받는다 — 계정 삭제의 `null`이 미러에서 타입 오류가 되면 이벤트가 사라지고 payload가 로그와 Sentry에 찍힌다 | 오너 | 2026-09-13 | 이벤트 정의서 §1·§2-2·§4, 계약 §9-1, 요청서 bourbon-topic-api, 구현 계획 1-4 |
| R56 | **워커의 재조회는 `consistent=true`로 읽고, 요청 경로의 요청자 조회는 그대로 약한 읽기다**(topic-api #69). topic-api의 유저 topic은 DynamoDB에 있고 기본 읽기는 아무 복제본에서나 답하므로 **바꾸기 전 값**이 올 수 있다. 재조회는 "이 사람이 방금 visibility를 바꿨다"는 신호를 받고 도는 데다 그 결과로 row를 **통째로 교체**하므로, 약한 읽기 한 번이면 방금 숨긴 row를 우리가 되살려 저장한다 — 지연이 아니라 노출이고(불변식 1), 다음 재조회까지 남는다. 대화하지 않는 유저에게는 그 "다음"이 오지 않는다. 응답 직전 재확인(불변식 4)은 우리 저장소를 다시 보는 것이라 이것을 못 잡는다. 디바운스(수 초)가 DynamoDB의 전파 창(보통 밀리초)을 실무적으로 거의 덮지만 **상한이 없고 틀렸을 때의 대가가 비대칭**이다. 비용은 읽기 2배인데 재조회는 유저당 디바운스 뒤 1회라 절대량이 작다. **요청자 조회(R27)에는 켜지 않는다**: 남의 노출을 정하지 않고(요청자 본인 topic이 랭킹 입력일 뿐) 응답 지연에 직접 올라탄다. `client.user_topics`와 `client.requester_topics`가 이미 다른 메서드라 나누는 데 드는 것이 없다. 참고: bourbon-api의 `reconcile_public`도 같은 이유로 `consistent=true`로 읽는다 | 오너 | 2026-09-13 | 이벤트 정의서 §2-1·§2-2, 계약 §9-1, 구현 계획 1-4 |
| R57 | **`discoverable`은 재조회가 파생하는 값이고, bourbon-api에 대한 요청은 철회한다**(R23 개정). bourbon-api는 `discoverable`을 만들지 않았고 `bourbon.personal_agent_visibility_changed`도 발행하지 않는다. #325가 친구 게이트를 내리면서 둔 `agents.public`은 유저가 쓰는 필드가 아니라 **"public topic을 1개 이상 갖고 있나"에서 파생되는 상태**이고 API 응답에도 나오지 않는다 — R23이 요청한 옵트인은 플랫폼에 존재하지 않는다. 그대로 두면 우리 컬럼은 `false`로 태어나 아무도 뒤집지 않고, 후보 조회·CF 스윕·IDF가 전부 그것을 조인하므로 **운영에서 모든 추천이 조용히 빈 답이 된다**(에러도 `degraded`도 없이). **그래서 같은 사실을 우리가 직접 파생한다**: 재조회가 읽어 온 결과에 `public` tier row가 하나라도 있으면 `true`, 없으면 `false`를 **row를 교체하는 바로 그 트랜잭션에서** 쓴다. 이벤트 둘·시계 둘이던 것이 하나가 되어 플래그와 row가 어긋날 수 없고, 그래서 `visibility_changed_at`(순서 없는 이벤트 둘을 비교하려고 둔 컬럼)은 존재 이유가 사라져 **삭제한다**. **플래그는 `public` 갈래에만 붙인다**: 뜻이 "public topic이 있나"가 된 이상 그것으로 *friends* tier row까지 막으면, public topic 없이 친구에게만 공개한 소유자가 **자기 친구에게도 안 보인다** — 게이트는 그 사람을 친구 갈래로 통과시키는데. 이러면 우리 두 갈래가 게이트의 두 갈래와 일대일이 된다. **컬럼은 남긴다**: IDF의 모집단 수는 `agents`만 세고 `visible_topic_rows`를 보지 않으며 CF 스윕도 소유자 단위라 거기서는 실제로 일을 한다. `public` 갈래에서는 이제 row의 존재가 플래그를 함의해 중복인데, 빼뒀다가 옵트인이 생기는 날 다시 넣는 것이 **조용한 노출 버그가 되는 방향**이라 남기고 그 이유를 코드 주석에 적는다. **R23이 지키려던 구분은 살아 있다** — "누구나 말 걸 수 있다"와 "낯선 사람에게 홍보된다"는 다른 노출이고, 플랫폼이 전자 하나만 스위치로 만들었을 뿐이다. 후자를 따로 두는 것은 O22로 연다 | 오너 | 2026-09-13 | 계약 §5·§6·§9-1, 이벤트 정의서 §2-3·§3·§5·§6, 재설계 §1-2·§4-2, walkthrough, 구현 계획 1-4·1-5, 요청서 bourbon-api |
| R58 | **소유자 단위로 세는 집계 셋의 모집단은 "답에 나올 수 있는 소유자"다** — 공개된 row가 하나라도 있는 사람(R57 후속, R25의 IDF 정의를 정밀화). R57이 노출 판정을 **row 단위 두 갈래**로 만들었는데(public은 `discoverable`, friends는 친구 관계) 소유자 단위로 집계하는 세 곳이 여전히 `discoverable` 하나로 답하고 있었다. 셋 다 답에 나오는 사람의 부분집합만 세게 되어 **각각 다른 방식으로 틀렸다**: (a) **IDF**(계약 §7 `content_similarity`, R25) — `holders`가 `discoverable`로 걸러지고 본 쿼리에 inner join이라, friends로만 공개한 사람만 가진 topic은 holder가 0이라 **그 row가 통째로 사라진다**. 타입 ①②는 그 사람을 답하는데 타입 ③ content는 답하지 못하는, 같은 row에 대한 조용한 불일치다. 분모도 같이 옮긴다(`agents` 중 discoverable → `visible_topic_rows`의 distinct 소유자). 그러면 모든 row의 topic이 최소 자기 자신을 holder로 가지므로 **inner join이 구조적으로 total이 되어** 실패 양상 자체가 없어진다 — outer join으로 땜질하지 않는다. (b) **CF 게이트의 개인화 대리 지표**(`published_topics`) — 게이트는 *우리가 실제로 낸 추천*이 개인적이었나를 재는데 그 추천에 friends-only 소유자가 들어가기 시작했으므로, 대리 지표가 그들의 topic을 모르면 그 사람을 추천할 때마다 자동으로 "비개인적" 한 표고 `gate.min_cluster_gain` 판정이 아래로 샌다. (c) **합성 생성기의 인기 가중치** — `discoverable`로 0을 만드는데 바로 아래 `OpenTo`가 이미 tier별 per-pair 판정을 하므로 중복이고, 예외 2%가 사라진 뒤로는 틀린 중복이다. 안 고치면 friends-only 소유자가 합성 세계에서 대화 상대가 될 수 없어 **1-9가 R57의 변경을 측정할 대상을 만들지 못한다**. **비용과 그 처리**: (a)의 새 분모는 타입 ③ 쿼리마다 `visible_topic_rows` 위의 `COUNT(DISTINCT)`다. **실측(2026-09-13, 19,811 agents / 65,229 row): 9.9~14.1 ms, 예전 `count(*) FROM agents WHERE discoverable`는 0.78~0.84 ms — 12배.** 쿼리 모양을 바꾸는 것은 답이 아니다(`agents` + `EXISTS` semi-join도 9.0~9.2 ms). 문제는 모양이 아니라 **요청마다 센다는 것**이고, 그것은 R58 전에도 사실이었다. 모집단은 사람들이 공개/비공개를 바꿀 때만 움직이는 통계이고 R25가 요구하는 것은 요청자 무관이지 최신성이 아니므로, `popularity`의 주기 재계산과 같은 모양으로 `population_stats`(마이그레이션 0001, writer 없음)에 옮긴다 — 1-7은 이미 끝났으므로 진행표 **13-3**(1-9 앞). **버린 대안**: 분모를 `agents` 전체 수로 두는 것 — 싸지만 아무것도 공개하지 않는 소유자까지 세서 N이 부풀고, 그 변화는 항마다 `ln(N'/N)`의 **덧셈**이라 상대 가중치가 움직인다(로그 밑 변경이 무해하다는 R25의 논리는 곱셈 상수에만 해당한다) | 오너 | 2026-09-13 | 계약 §7, 설정 레지스터 §2, 합성 스펙 §4·§6·§7, 구현 계획 1-7·1-9, R25·R57 |
| R59 | **IDF의 모집단은 주기 스냅샷에서 읽고, 읽을 것이 없으면 그 자리에서 센다**(R58 후속, 진행표 13-3). R58이 정한 분모는 옳지만 요청마다 `visible_topic_rows` 전체를 세고, 그것은 R58 이전에도 그랬다 — 예전 분모가 싸서 안 보였을 뿐이다. 모집단은 사람들이 공개·비공개를 바꿀 때만 움직이고 점수에는 **로그로** 들어가며 R25가 요구하는 것은 요청자 무관이지 최신성이 아니므로, `popularity`와 같은 모양의 주기 작업이 `population_stats`에 한 줄 쓰고 요청 경로는 그 한 줄을 읽는다. **스냅샷이 없을 때는 지금 쿼리로 폴백하고 `degraded`는 붙이지 않는다.** 근거: (가) 행은 한 번 써지면 남으므로 진짜 공백은 **한 번도 돌지 않은 DB**뿐이고, 루프는 부팅 직후 한 사이클을 돌린다(작업 실패는 공백이 아니라 낡은 값이고, 그것이 이 값에 허용된 상태다). (나) IDF를 끄면 답의 **의미**가 바뀐다 — 모두가 가진 topic이 순위를 정하게 되므로, 느린 답이 아니라 다른 답이다. (다) `degraded`는 계약 §3-4의 어휘이고 호출자가 그것을 받아서 할 수 있는 일이 없다. **0도 폴백한다**: 빈 환경에서 부팅한 pod가 진짜 0을 적고 row는 그 뒤에 오는데, `ln(0 / holders)`는 수가 아니라 PostgreSQL 에러다(셀 사람이 없으면 `holders` row도 없으므로 그 상태에서 세는 값은 어차피 쓰이지 않는다). **나이 임계는 두지 않는다** — 임계를 넘은 요청은 요청마다 다시 세는 상태로 돌아가고, 그것도 DB가 멀쩡할 때 일어난다. 다만 모집단이 크게 어긋나면 `ln(N'/N)`만큼 **모든 항에 더해지는** 오차라 상대 순위가 움직인다는 것은 1-9에서 다시 본다. **주기 작업은 `popularity` 재계산과 같은 사이클이 아니라 별도 루프다**: 하나의 `try`가 사이클 전체를 삼키므로 인기도 집계가 실패하면 모집단까지 멈추고, 둘의 자연스러운 주기가 다르다(대화는 30분, 공개 설정은 훨씬 느리며 합성 스펙 §8은 이 표를 하루 한 번 쓴다고 말한다). 설정 행은 `content.population_refresh_minutes`(기본 60) — `popularity.refresh_minutes` 옆이 아니라 그것을 읽는 소스 옆에 둔다. **실측(2026-09-13, 19,811 agents / 65,229 row, 모집단 11,677)**: content 쿼리 전체가 13.7~24.7 ms → **2.8~3.2 ms**, 계획에서 `visible_topic_rows` 전체 스캔(65,229행)이 사라졌다. 스냅샷 읽기는 0.012~0.027 ms, 한 사이클은 69.7 ms(모두 같은 fixture 위 반복 실행의 최소~최대) | 오너 | 2026-09-13 | 계약 §7, 설정 레지스터 §8, 구현 계획 1-7, 진행표 13-3, 합성 스펙 §8-3, R25·R58 |
| R60 | **`GROUNDING_DISAMBIGUATION_ENABLED`를 prod에서도 켠다.** 지금까지 dev만 `true`이고 prod는 `false`였는데, **확장 프롬프트가 이 게이트가 켜져 있다는 전제로 쓰여 있다** — plain-name 규칙이 한 그룹에 부모와 자식을 같이 담게 하고, 그러면 exact label이 둘이 되며, 그것은 shortlist라 결정자를 요구한다(`stages/expansion_prompt.py`가 이것을 "deploy note"라고 적어 뒀다). **실측(2026-09-15~16, 2만 모집단 200 질문, `validation_results.md` §11-4)**: 꺼진 쪽에서 질문의 26%가 422 `ambiguous`로 끝나고 답이 나온 147건 중 32건도 그룹 하나를 버려 **200건 중 84건이 이 게이트에 닿는다**. 켜면 못 정한 그룹 85개 중 70개가 정해져 **422가 52 → 6(88% 감소)**, 완전일치 92 → 143, recall 0.588 → 0.849가 되고 **precision도 0.918 → 0.940으로 같이 오른다** — recall을 precision과 바꾼 것이 아니라, 결정자가 없을 때 버리던 후보들 중 옳은 것이 많았다는 뜻이다. 양쪽 다 같은 200건을 **세 번씩** 돌려 정확도 열이 매번 동일했다. **대가**: text→topic 단계가 p50 +42%·p95 +79%(요청 전체로는 모델이 98%를 차지하므로 p50 약 1.42 → 1.94초), 토큰 +24%, 요청 +42%. `RECOMMEND_DEADLINE_SECONDS` 20초에는 여유가 있다. **새 실패 모드 하나**: 결정자가 답을 못 하면 `disambiguation_failed` → 503이 되어, 지금 422(요청에 대한 판정)인 것이 우리 장애가 된다 — 세 런에서 0건이었지만 건강한 프록시에서 잰 값이다. **코드 기본값도 같이 `True`로 옮긴다**(처음에는 미뤘다가 같은 날 결정). 두 오버레이가 모두 `true`인데 기본값만 `false`면 **로컬과 테스트만 이 프롬프트를 자기가 전제하지 않은 게이트와 함께 읽는다** — §11-4의 첫 측정이 실수로 prod 동작을 잰 것이 그 함정의 실례다. 대신 포스처가 fail-closed에서 **fail-open으로 뒤집힌다**: 전에는 오버레이 키를 빠뜨리면 422가 늘었고, 이제는 게이트가 조용히 켜진 채로 남는다. 배선 테스트가 두 오버레이의 **값**을 정확히 단언하는 것이 그 유일한 그물이다. **켜기 전에 잰 것이 답하지 못하는 것 다섯**(2026-09-16 리뷰): ① **얼어 있는 `POST /recommend`도 같이 켜진다.** `composition.py`의 두 자리가 한 플래그를 읽는데 §11-4는 explicit 경로만 쟀다. 게다가 그 파이프라인은 retrieval이 404를 내면 캐시를 비우고 S2를 다시 도는 경로가 있어 **disambiguation을 두 번 낸다** — 요청당 completion 상한이 측정한 경로 4회, 안 측정한 경로 **7회**다. ② **꼬리가 데드라인을 넘을 수 있다.** 한 completion의 상한이 약 12.1초(6초 × 2시도 + 백오프)이므로 순차 4회는 약 48초, 7회는 약 85초이고 `RECOMMEND_DEADLINE_SECONDS`는 20초다. p50 논거(1.42 → 1.94초)는 꼬리에 대해 아무 말도 안 한다 — 확장에서 타임아웃 한 번(7.9초)에 pick 세 번(약 5.4초)이면 13.3초이고, **네 번 중 어디서든 한 번 더 타임아웃하면 20초를 넘는다.** 그때 로그에 남는 원인은 `disambiguation_failed`가 아니라 `deadline`이다. 그 위 절벽은 gunicorn 기본 30초이고(`Dockerfile`에 `--timeout`이 없다) 파드당 워커가 1개라 arbiter가 죽이면 그 파드의 **모든** 진행 중 요청이 같이 죽는다. ③ **422 → 503 전환이 이 행이 적은 것보다 넓다.** `stages/grounding.py`가 잡는 것은 `ProviderError`이고 `LlmMalformedOutput`도 그 하위다 — 콘텐츠 필터가 `choices: []`로 200을 답하거나 `finish_reason`이 `stop`이 아니면 **재시도 없이 한 방에** `disambiguation_failed`가 된다. 프록시 장애만이 아니다. ④ **부분 실패가 안 보인다.** 다른 그룹이 하나라도 grounded면 응답은 200이고, 흔적은 grounding 로그 줄의 `groups_disambiguation_failed`뿐이다 — 요청 로그의 `failure_causes`에도 없다. 조용히 답이 좁아지는 열화에는 **에러율 신호가 아예 없다.** **개정 2026-09-16**(코드 #45): 신호는 올렸다 — `recommend_explicit.completed`와 `recommend.completed`가 `groups_disambiguation_failed`를 싣고, 타입 ①의 줄은 분모로 쓸 `groups`도 싣는다(얼어붙은 줄은 원래 있었다). 스테이지 줄에 이미 있던 값을 요청 줄로 올린 이유는 **로그 메트릭 필터가 이벤트 하나만 매치하기 때문**이다 — 잃은 것은 스테이지 줄이 알고 요청이 어떻게 끝났는지는 완료 줄이 알아서, 둘을 동시에 요구하는 조건을 어느 한 줄로도 쓸 수 없었다. **다만 이 행이 "흔적 없는 유일한 열화"로 읽히면 그것은 과장이다**: `groups_ambiguous`와 `groups_empty`도 똑같이 200으로 나가고 타입 ①의 `degraded`에 남지 않는다. 이것만 다른 점은 **책임 소재**다 — 나머지는 요청에 대한 판정이고, 이것은 503급 업스트림 실패가 200을 타고 나간 것이다. **두 경로에 같은 필터를 쓸 수 없다**: 얼어붙은 줄의 `outcome`은 `empty_reason or "ok"`라 답이 비면 `no_public_holders`가 되는데, 토픽이 좁아진 요청일수록 비기 쉬워서 `outcome = "ok"`로 거르면 **가장 아픈 모집단을 빼고 센다**. 그리고 얼어붙은 경로는 **답을 만든 grounding만** 센다 — ①의 재grounding 경로에서 첫 패스에 잃은 그룹은 이 줄에 없다(스테이지 줄은 두 번 찍어 한 요청을 두 번 센다). **호출자는 여전히 모른다**: 타입 ①의 `degraded[]`는 계약 필드라 값을 더하는 것이 계약 변경이고(**O23**), 얼어붙은 응답에는 `degraded` 자체가 없어 더할 자리도 없다. ⑤ **e3llm의 용량이 어디에도 적혀 있지 않다.** 두 repo를 뒤져도 rate limit·쿼터·처리량 문서가 없다. 정상 상태에서 completion이 요청당 +42%이고, HPA는 CPU로만 스케일하는데 늘어난 일은 순수 I/O 대기라 **이 부하를 보지 못한다**. 429는 재시도 대상이고 서킷 브레이커가 없어서, 레이트 리밋이 걸리면 한 요청이 예전 2회 대신 **최대 8회**를 보낼 수 있다. **배포 전 권고**: `groups_disambiguation_failed > 0`과 `deadline` 원인에 알람을 걸 것(둘 다 다른 신호가 없다), 그리고 e3llm 쪽에 용량을 물을 것. **개정 2026-09-16**: 앞의 것은 신호를 올렸고(#45, ④ 참고), `deadline`은 네 파이프라인이 모두 이미 `failure_causes`에 싣고 있어 코드 쪽에 더할 것이 없다 — **다만 "신호가 준비됐다"까지는 아니다**. 데드라인은 진행 중인 호출을 취소하는데, 되감기는 라이브러리가 자기 예외를 올리면 그 요청은 `deadline` 원인 없이 다른 이름으로 끝난다(`explicit_recommend.py`가 SQLAlchemy의 `InterfaceError`를 예로 적어 뒀다). ②가 말하는 꼬리가 정확히 그 경로라서, 이 알람은 **하한을 센다**. **알람 자체는 둘 다 없다**: `iac`에 `aws_cloudwatch_log_metric_filter`가 한 건도 없다. 메트릭 알람과 Slack 전달은 이미 있으므로(`slack-alerts` 모듈, 여섯 모듈의 `aws_cloudwatch_metric_alarm`, Container Insights가 쓰는 `/aws/containerinsights/<cluster>/application`), 새로 놓는 것은 **로그 필드 → 메트릭 한 홉**이다 | 오너 | 2026-09-16 | 진행표 15·16-D, `validation_results.md` §11-4, `k8s/overlays/prod/configmap-patch.yaml`, `stages/expansion_prompt.py` |
| R61 | **API와 워커가 부팅 때 데이터베이스에 한 번 닿아 보고, 그 결과를 서로 반대로 해석한다.** `composition.py`가 "different decision from this one"이라며 열어 둔 것을 닫는다 — 엔진은 생성 시 아무것도 열지 않아서 **있지만 틀린 DSN이 깨끗하게 부팅하고 두 probe를 다 통과한 뒤 첫 요청에서 실패**하며, 그때 `deploy.sh`는 이미 성공을 보고한 뒤다. 게다가 liveness와 readiness가 같은 `/health`를 읽고 있었고 그 응답은 무조건 `ok`였다. **API**: 새 루트 route `/ready`가 연결 하나를 열어 `SELECT 1`을 하고 **성공하거나 transient로 끝나면 래치**해서 다시 묻지 않는다. `startupProbe`가 그것을 읽고 liveness·readiness는 `/health`에 그대로 둔다 — 이 데이터베이스는 모든 pod가 공유하므로 서빙 경로의 의존성 검사는 한 번의 흔들림으로 **전 pod를 동시에 빼거나(readiness) 재시작한다(liveness)**. **거부하는 것은 "데이터베이스가 답하지 않았다"보다 좁다**: `is_transient`(워커의 재시도 사다리가 `UpstreamUnavailable`과 함께 쓰는 그 술어)로 갈라 **다음 시도에도 같을 것만** 거부한다. 틀린 비밀번호(`28`)·없는 데이터베이스(`3D`)는 이 pod가 서빙할 수 없는 설정이고, 연결 거부는 장애다. 장애까지 거부하면 **장애 중에는 어떤 pod도 뜨지 못한다** — 스케일업·spot 선점·노드 드레인·OOM이 전부 CrashLoopBackOff가 되고 백오프가 복구를 데이터베이스 자신의 복귀 뒤로 더 늦춘다(dev는 PDB `minAvailable: 0`에 spot 선호라 최악이다). **남는 구멍은 적어 둔다**: 포트가 안 열린 것은 보안 그룹 미개방인지 데이터베이스 다운인지 한 번의 시도로 구분되지 않고, 이 규칙은 **서빙 쪽으로 넘어뜨린다** — 첫 배포는 `deploy.sh`를 보는 사람이 있고 새벽 4시 장애는 없다. **워커는 정반대로, 모든 실패를 거부한다.** deferq는 raise한 리스너의 메시지를 **ack**하고 재전달도 DLQ도 없으므로 데이터베이스 없이 뜬 워커는 소비하고 잃는다 — 쓰기를 하는 리스너 넷은 `worker/retry.py` 사다리를 태운 뒤 버리고, refresh만 arm하는 둘은 성공한 뒤 스케줄된 태스크에서 잃는다. **안 소비하는 것이 안전한 상태**다. API가 거부하는 요청은 대개 호출자가 아직 들고 있고, 워커가 소비해서 버린 이벤트는 없어진다. **그 규칙이 무료가 아닌 지점 둘**: ① **첫 기동에서는 아무것도 보존하지 않는다** — 큐와 바인딩은 컨슈머가 선언하고 퍼블리셔는 exchange만 선언하므로, 이 워커가 한 번도 성공적으로 뜬 적이 없으면 그 사이 발행된 이벤트는 라우팅되지 않아 브로커가 버리고 **퍼블리셔에게는 성공했다고 답한다**. 거부가 기동보다 나쁘지는 않지만(양쪽 다 그 창을 잃는다) 낫지도 않다 — 이 규칙이 실제로 버는 것은 **두 번째 기동부터**이고, go-live 이후에는 그것이 전부다. ② **보존하는 적체에 상한이 없다** — 큐에 `x-max-length`도 TTL도 없고, 브로커는 다른 bourbon 서비스들과 공유하는 단일 인스턴스이고(`bourbon-rabbitmq-tokyo-*`, `iac`의 `amazon-mq-rabbitmq` 모듈이 `SINGLE_INSTANCE`로 세운다), **큐 깊이 알람이 없다.** 장기 장애의 첫 신호는 RabbitMQ 자신의 메모리·디스크 알람이고 **그때는 이미 남의 퍼블리셔가 막힌 뒤**다. 깊이 제한과 `MessageCount` 알람은 요청서 §4에 더했다. **부수 효과 하나가 두 규칙을 잇는데, 위의 두 갈래에서 서로 다르게 작동한다**: 포트가 **거부(RST)**로 답하면 API의 게이트는 transient로 읽고 래치를 열어 **롤아웃이 성공하며**, 그 배포를 실패시키는 것은 워커다. 반대로 **패킷이 드롭되면**(보안 그룹 미개방) API 쪽에서도 멈춘다 — `confirm_reachable`에 연결 타임아웃이 없어(`make_postgres_engine`이 `connect_args`를 주지 않으므로 asyncpg 기본 60초) `/ready`가 답하기 전에 probe의 `timeoutSeconds: 3`이 먼저 걸리고, 여섯 번 실패하면 컨테이너가 죽는다. 즉 **API가 조용히 통과하는 경우는 거부되는 포트 하나뿐**이고, 첫 배포에서 더 흔한 드롭 쪽은 두 프로세스가 같이 잡는다. **거부가 실제로 하는 일**은 과장하기 쉬워서 적어 둔다 — **멈추는 것은 없다.** kubelet이 컨테이너를 재시작해 CrashLoopBackOff가 되고, 새 ReplicaSet은 Available pod를 얻지 못하고, 옛 ReplicaSet이 계속 서빙하며, `progressDeadlineSeconds`는 어디에도 설정돼 있지 않다. 실제로 멈추는 것은 `deploy.sh`의 `rollout status --timeout=120s`이고, 이유는 그 줄이 아니라 pod 이벤트와 로그에 있다 | 오너 | 2026-09-16 | 진행표 15, 코드 #46, `agent_discovery/composition.py`, `api/routers/health/checker.py`, `worker/app.py`, 요청서 §4, `iac`의 `amazon-mq-rabbitmq` |
| R62 | **클라이언트 API에서 `x-user-id`가 없거나 UUID가 아니면 401 `authorization_error`다**(지금까지 둘 다 403). bourbon-api가 edge-auth를 바꾼다 — 토큰 없는 요청을 **막지 않고 통과시키되 `x-user-id`를 제거**한다(`x-envoy-auth-headers-to-remove`에 더한다). 동기는 로그인하지 않고도 보여야 하는 공유 프로필 페이지(`/@robert`)다. 그러면 헤더의 부재가 뜻하는 것이 바뀐다 — 지금까지는 "이 요청은 검사를 건너뛰었다"였고 앞으로는 **"로그인하지 않았다"**이며, 그 답은 401이다. **값이 있는데 UUID가 아닌 경우도 같은 401이다.** 처음에는 그쪽을 403으로 남기려 했다 — 사이드카는 검증된 UUID를 쓰거나 헤더를 지우거나 둘 중 하나이므로 그런 값은 메시 밖에서 온 호출이고, 403이 추상적으로는 더 정직하다. 접은 이유는 그 경우를 만들 수 있는 것이 컨테이너를 직접 부르는 개발자뿐이고, 헤더 하나짜리 계약에 서비스마다 다른 코드가 있는 것이 그 구분보다 비싸기 때문이다. 두 경우는 **메시지로** 갈린다. 참고로 **bourbon-topic-api는 예전부터 둘 다 401이었다** — 다만 근거가 정반대다("검사를 우회했다"). 그쪽 `feature/edge-auth-optional-identity`(`fb54884`)가 더한 것은 답이 아니라 **익명 opt-in(`OptionalViewerId`)과 route-table 테스트**다. 즉 이 결정은 그쪽을 따라간 것이 아니라 **원래 갈라져 있던 것이 수렴한 것**이다. `error_code`는 플랫폼 철자 `authorization_error` — bourbon-api 봉투가 `{request_id, error_code, message}`로 우리와 같다. **따라오는 것 셋.** ① `ForbiddenError`를 지운다 — 403을 raise하는 경로가 남지 않으므로, 남겨 두면 R51이 `room_created` 미러를 지운 것과 같은 이유로 "합의된 것처럼 읽히는" 상태 코드가 된다. ② **route-table 테스트를 새로 넣는다** — 기존 테스트는 route가 `x-user-id` 파라미터를 *선언*했는지만 봐서, 헤더를 optional로 읽고 익명을 그냥 서빙하는 route를 통과시킨다(적대적 리뷰가 실제로 그런 route를 넣어 전체 스위트를 통과시켰다). edge가 익명을 들여보내는 날부터 그 의존성이 **유일한 관문**이므로, `get_requester_id`가 의존성 트리에 있는지를 route마다 확인하고 익명 허용 목록을 빈 집합으로 고정한다. bourbon-api 가이드도 서비스마다 이 테스트를 요구한다. ③ **401과 프레임워크의 404·405를 info로 로그한다**(`AppError.quiet`, bourbon-api와 같은 철자) — structlog-sentry가 warning을 Sentry 이벤트로 올리므로, 로그아웃 상태 페이지의 트래픽이 요청당 Sentry 이벤트 하나가 된다. 404·405까지 내리는 이유는 **경로 스캔**이다: 우리 prefix의 없는 경로는 예전에는 토큰이 없으면 사이드카에서 끝났고 이제는 핸들러까지 온다. 4xx는 traceback도 렌더하지 않는다. **bourbon-topic-api는 4xx 전부를 info로 내린다** — 우리는 그렇게 하지 않는다. 우리 4xx에는 `grounding_failed`·`grounding_ambiguous`처럼 **우리가 내린 판정**이 섞여 있어서, 그것까지 Sentry에서 빼는 것은 로깅 정리가 아니라 제품 결정이다. 그래서 내린 것은 판정이 없는 둘(라우팅 실패)과 401뿐이다. **열어 두는 것**: bourbon-api 가이드는 웹 클라이언트에 "401이면 세션 갱신 후 재시도"를 지시하고, 같은 가이드가 authorizer 장애를 401이 아니라 503으로 답하는 이유로 "갱신으로 풀 수 없는 것에 401을 주면 루프"를 든다. "처음부터 로그인 안 함"도 갱신으로 풀리지 않는다 — 인터셉터가 어떻게 도는지는 그쪽 것이라 요청서로 묻는다. `WWW-Authenticate`는 RFC 9110상 MUST지만 보내지 않는다(우리는 credential을 읽지 않고, `Basic`을 보내면 브라우저가 비밀번호를 묻는다) — 알고 하는 일탈로 적어 둔다 | 오너 | 2026-09-17 | 계약 §1-2·§3-3, 진행표 15, 요청서 bourbon-api §3, bourbon-api `feature/edge-auth-anonymous`, bourbon-topic-api `feature/edge-auth-optional-identity` |
| R63 | **`RecommendedAgent`가 표시 필드를 싣고, 그 값은 요청 시 bourbon-api 내부 route에서 읽는다.** 지금까지 세 응답은 `agent_id`와 `owner_user_id`만 주고 이름·사진은 클라이언트가 해결하기로 돼 있었다 — 방 안의 `agent_profiles_v1` 카드가 그렇게 하고 있고(bourbon-api `messages/schemas.py`의 `AgentProfileItem`이 "display fields are viewer-dependent or mutable, so the client resolves them via `GET /users/{user_id}`"라고 적는다), 계약 §3-1도 같은 문장을 갖고 있었다. **그 규칙은 카드의 모양에 맞춰 내려진 것이다**: 뷰어가 이미 들어와 있는 방에서 **한 사람**을 보여주고, 카드가 DM 진입을 제안하니 친구 관계가 실제로 필요하다. **discover는 모양이 다르다** — 방 밖의 목록이고 한 페이지가 `for_you.limit_default` 20건, 상한 50건이다. 같은 규칙을 옮기면 클라이언트가 공개 `GET /users/{user_id}`를 스크롤 한 페이지마다 20~50번 쳐야 하고, 그 route는 bourbon-api에서 **유저당 가장 비싼 경로**다 — personal_agent를 bootstrap·realign 하고 `commit()`까지 하는, **읽기인데 쓰는** 경로다. **그래서 목록을 그리는 데 필요한 것은 우리가 채운다.** 모양은 bourbon-api의 `GET /api/users` 응답 원소(`UserOut`)를 그대로 따른다 — **그 endpoint를 부른다는 뜻이 아니라 클라이언트의 기존 유저 카드 렌더러가 블록을 그대로 받게 하려는 것이고, 디자인이 나오기 전의 기준값이지 확정이 아니다**(오너, 2026-09-22). 즉 `id`·`name`·`picture`·`handle`과 중첩된 `personal_agent`(`agent_id`·`owner_user_id`·`enabled`·`public`·`name`·`picture`)다. 필드를 더하거나 빼는 것은 계약 §3-1과 hydration 한 곳만 고치는 싼 변경이다. **그 모양이 이 결정의 선과 맞아떨어진다**: `UserOut`에는 `relationship`이 없고 그것은 `GET /users/{user_id}`의 `UserProfileOut`에만 있다 — bourbon-api가 뷰어 무관/뷰어 의존을 이미 그 자리에서 갈라 놨다. **뷰어에 따라 달라지는 것은 채우지 않는다** — 친구 관계와 열려 있는 DM 방 id는 기준이 bourbon-api이고, 우리가 친구 미러로 답하면 미러가 밀릴 때 틀린 값을 주며, browse 화면에서 모르는 사람 50명의 관계를 한꺼번에 내리는 것은 불변식 5가 막는 방향이다. 그것은 목록이 아니라 **누름**의 정보라 클라이언트가 그때 한 번 읽는다. **경계는 "그리는 데 필요한가" 대 "누르는 데 필요한가"다.** **읽는 방법**: 요청 시 `GET /api/internal/users?ids=`와 `GET /api/internal/agents?ids=` 둘을 병렬 배치로 읽는다 (그쪽 배치 상한 100개 > 우리 페이지 상한 50). **미러하지 않는다** — 우리가 받는 이벤트에 프로필 변경이 하나도 없어 미러를 유지할 방법이 주기 스윕뿐이고, 이름과 사진은 그 주기만큼 눈에 띄게 낡는다. 요청 시에 읽으면 낡음 자체가 없다. **실패는 이미 있는 어휘로 답한다**: `hydration_partial` + 200이고, 카드는 id만 가진 채 그려진다. **503이 아니다** — 이 단계는 랭킹이 끝난 뒤라 어떤 후보가 답에 드는지를 바꾸지 않는다. **`storage/friends.py`가 적어 둔 반례("요청 시 bourbon-api 조회는 그들의 가용성을 모든 추천 앞에 세운다")는 여기에 걸리지 않는다**: 친구 집합은 후보 쿼리 **안**의 필터라 못 읽으면 답이 줄어들고(불변식 3), 표시 필드는 답이 정해진 뒤의 마지막 단계라 필드만 null이 된다. 두 선택이 한 repo에 공존하는 이유가 그것이고, **bourbon-api가 죽어도 discover는 200이어야 한다**는 것이 이 결정이 지켜야 할 조건이다 — 구현에서 테스트로 고정한다. **새 런타임 의존이 하나 생긴다**: 지금 이 서비스는 bourbon-api를 HTTP로 부르는 곳이 없고(이벤트 단방향과 uuid5 재계산뿐), `providers/`에 `topic_api`·`llm`뿐이다. 접근 방법은 이미 플랫폼에 있다 — bourbon-topic-api가 같은 자리에서 같은 일을 하고 있고(`topic/visibility/friends.py`, `BOURBON_API_URL: "http://bourbon-api"`를 **base ConfigMap**에 두고 URL은 origin만, 전체 경로는 클라이언트 모듈 한 곳에, 타임아웃 2초, URL이 비면 클라이언트를 만들지 않는 것이 로컬 기본값), 우리 `providers/topic_api/transport.py`가 같은 규칙(내부 prefix, 게이트웨이 아닌 Service 직접, 자격증명 없음, `outbound_headers()`의 request id 하나)을 이미 적어 뒀다. `x-user-id`는 전달하지 않는다 — 그쪽 내부 route가 읽지 않고, 요청자 신원은 이 호출에 실리지 않는다(보내는 id는 추천된 소유자들뿐). **덤으로 앞당겨지는 것**: go-live 백필이 쓰려던 bourbon-api 클라이언트가 이 결정으로 먼저 생긴다(진행표 15의 여섯째 주기 잡). **버린 대안 둘**: (가) bourbon-api에 공개 배치 route(`GET /users?ids=`)를 요청한다 — 뷰어 의존 필드까지 한 번에 얻지만 타팀 작업이고, 이미 있는 내부 배치 route를 공개면에 복제하는 일이다. (나) 클라이언트가 N번 친다 — 위의 비용 그대로. **열어 두는 것**: 목록 카드가 실제로 무엇을 그리는지는 클라이언트 화면 기획에 달렸고, 요청서 `requests/client.md` §3의 4번으로 묻는다. 필드 목록은 싸게 바뀌지만 **뷰어 의존 값이 목록에 필요해지면 전제가 깨진다** — 그때 다시 본다. **개정 2026-09-22 (구현 중, 코드 #58)**: 네 가지가 바뀌었다. ① **요소가 둘이 됐다** — 타입 ①은 `RecommendedAgent`, ②③은 표시 블록 둘을 더한 `DiscoverAgent`. ② **타입 ①은 채우지 않고, 키 자체가 없다.** 그 답을 받는 bourbon-agent는 카드를 그리지 않는다 — owner id로 사람을 자기가 조회해 모델이 쓸 문장을 만들고, 방에 올리는 카드에는 그 id 하나만 싣는다(`recommendation/tools.py`). 우리가 채우면 그쪽이 곧 하는 호출을 우리 데드라인 안에서 한 번 더 하고 값은 버려진다. **null인 키가 아니라 없는 키인 이유**는 null이 "채우려다 실패했다"로 읽히는데 타입 ①의 답은 완전하기 때문이다. ③ **모양이 `UserOut` 흉내에서 이 답에 맞는 것으로 바뀌었다.** 처음에는 클라이언트의 기존 유저 카드 렌더러가 블록을 그대로 받게 하려고 그쪽 모양을 베꼈는데, 그 대가가 한 카드에 `owner_user_id` 세 번·`agent_id` 두 번이었다(오너 지적). 이제 id는 요소 최상위에만 있고 — §2-5 보고와 §8 로그가 이미 읽는 자리다 — 블록은 표시 필드만 갖는다: `owner`(`name`·`picture`·`handle`), `agent`(`name`·`picture`·`public`). `agent`는 혼자서도 null이 될 수 있다(사람 row만 오는 경우). ④ **`enabled`는 싣지 않는다** — bourbon-api의 답(2026-09-22). 처음에 적은 근거("discoverable하니 항상 true")는 **틀렸고 그 사실을 같이 남겼다**: `discoverable`은 "public topic이 있나"(R57)일 뿐이고 그쪽 DM 게이트는 `public`·`enabled`·소유자 활성 셋을 본다(`bourbon_api/rooms/service.py`). 빼는 근거는 추론이 아니라 그쪽의 답이다. **그리고 "답을 실패시키지 않는다"가 말이 아니라 동작이 되려면 셋이 필요하다**(리뷰 두 번에서 나왔다): 예산은 이 업스트림의 것과 **요청에 남은 시간 중 작은 쪽**(안 그러면 완성된 페이지가 503이 된다), 한 chunk의 실패가 답한 chunk를 버리지 않을 것, 실패는 감싸인 모양이 아니라 **실패 자체로** 가려낼 것(그러지 않으면 진짜 버그가 `hydration_partial`로 삼켜진다) | 오너 | 2026-09-22 | 계약 §3-1·§3-4·§4-5·§6·§11, 설정 레지스터 §9, 요청서 client·bourbon-api, 진행표 18 |
| R64 | **페이지가 있는 답은 `has_next`와 `next_cursor`를 같이 준다**(R63 후속). 지금까지는 `next_cursor` 하나였고 마지막 페이지에서 null이었다 — 그것만으로 충분하지만, R63이 카드 모양을 bourbon-api의 `UserOut`에 맞추기로 한 이상 **봉투도 같은 모양인 쪽이 클라이언트에 한 가지 페이지 로직만 남긴다**(`GET /api/users`의 `UserListOut`이 둘 다 준다). **둘은 언제나 일치한다** — `has_next`가 참인 것과 `next_cursor`가 있는 것은 같은 말이고, 봉투를 만드는 한 곳이 그 불변을 지킨다. 중복인 것을 알고 두는 것이고, 값이 갈리면 그것은 상태가 아니라 우리 결함이다. 붙는 곳은 타입 ③의 최상위와 타입 ②의 **섹션마다**이며, 커서가 없는 타입 ①에는 둘 다 없다. **섹션 목록 자체는 페이지하지 않는다**(같은 날 같이 정함): `sections`는 요청자 topic 중 preference 상위 N개로 고정이고, 더 보려면 `sections`를 올려 다시 요청한다(상한 `by_topic.sections_max` 10). 섹션마다 인덱스 쿼리가 하나씩이라 그 수를 올리는 비용이 선형이고, 화면을 계속 내리는 상호작용은 섹션 안의 "더 보기"다. **계약 변경이다** — `contract_version`은 움직이지 않는다(§10: 필드를 더하는 것은 버전을 올리지 않는다) | 오너 | 2026-09-22 | 계약 §2-4·§3-2·§11, R63 |
| R65 | **타입 ②의 섹션은 그 topic과 1 hop 자손을 묻는다.** 지금까지 후보 조회는 `topic_id.in_(topic_ids)` 정확 일치였고, **계약은 자손을 한 글자도 말하지 않았다** — 정한 것이 아니라 정하지 않은 채 구현이 그렇게 된 자리다. **motivation**: 얼어붙은 `POST /recommend`는 topic-api의 `/topics/{id}/users`를 직접 읽고 **그쪽이 자손을 펼쳐서** 답한다(dev 로그의 `descendants_dropped`가 그 플래그다). 새 route들은 우리 `visible_topic_rows`를 읽는데 그 표는 `(topic_id, tier, owner)` 키라 정확 일치만 안다. 그대로 두면 클라이언트가 구 route에서 옮겨올 때 **답이 조용히 좁아진다.** **범위**: 아래 방향만, 1 hop만, **타입 ②만**. 부모·형제로 넓히지 않는 이유는 요청자가 가진 것보다 *더 구체적인* 사람은 그 관심의 한 구석을 아는 사람이지만 더 일반적인 쪽("음료")은 그렇지 않아서다. **타입 ①을 제외하는 이유는 coverage다**: ①의 첫 정렬 키가 `(coverage desc, score desc)`이고 그 값은 매치된 topic 수(`ranking.py`의 `len(strongest)`)라, 자손을 세면 "커피" 하나를 물었는데 자손 셋을 공개한 사람이 coverage 3이 되어 정확히 맞춘 사람을 제친다 — 분모가 뜻을 잃는다. ②는 `score desc`만 쓰고 coverage를 보여주지도 쓰지도 않아 이 문제가 없다. 소스(`TopicIndexSource`)를 ①②가 공유하므로 **확장은 소스가 아니라 쿼리의 속성**이어야 한다. **1 hop인 이유는 실측이다**(2026-09-23, 커밋된 카탈로그 + dev 스냅샷): 사람들이 실제로 공개한 topic 377개 중 자식이 있는 것은 153개뿐이고, 섹션의 `IN` 목록은 1 hop에서 **중앙값 1개 그대로·p95 21·최악 69**, 3 hop에서 **p95 44·최악 463**이다. 재귀 걸음 자체는 싸다 — `catalog_edges`가 3,136행이고, 같은 걸음을 타입 ③이 3 hop 양방향으로 하면서 쿼리 전체가 2.8~3.2 ms다(R59). 늘어나는 쪽은 후보 스캔이고, 1 hop이면 감당한다. **응답에 새 필드를 만들지 않는다**: `requested`가 이미 그 자리이고(계약 §3-1이 "타입 ②: 섹션 topic이면 true"로 정의해 뒀다), 지금은 `assembly.py`가 상수 `True`로 고정해 둔 탓에 **그 필드가 실제로 말하는 것이 "내가 타입 ③인가"뿐**이었다. 1 hop만 펼치므로 `false`는 "직계 자손"으로 모호하지 않다. 섹션이 이미 기준 topic을 싣고 있어 파생 가능하지만, `empty`·`has_next`와 같은 이유로 **한 곳에서 유도해 응답에 담는다** — 클라이언트가 두 값을 조합하게 만들지 않는다. **점수**: 자손 hit의 `topic_score`에 `content.decay_per_hop`을 곱한다. 안 그러면 자손만 여럿 가진 사람이 `max(topic_score)`를 타고 정확히 맞춘 사람을 제친다. 상수는 타입 ③이 쓰는 것을 빌리고 새로 만들지 않는다. **`matched_topics`는 ②에서 여전히 비지 않는다** — 섹션 topic이든 자손이든 근거가 있고, 재확인에서 전부 사라진 소유자는 카드가 비는 것이 아니라 답에서 빠진다(불변식 4). **전제는 확인받았다**: "커피" 섹션에 핸드드립만 공개한 사람이 나오는 것이 기대 동작이 맞다(오너, 2026-09-23). 이 결정이 기대는 유일한 제품 판단이었다. **재는 것은 구현과 함께 간다**: 합성 모집단에서 확장 유무를 나란히 놓고 recall이 실제로 오르는지와 p95를 본다(`cli validate`). **지금까지 실측된 것은 지연이 문제가 아니라는 것까지이고, 품질이 오른다는 증거는 아직 없다** — 재서 안 오르면 그때 되돌린다 | 오너 | 2026-09-23 | 계약 §2-2·§3-1·§4-4·§11, 설정 레지스터 §8, 요청서 client, 진행표 19 |
| R66 | **노출도 보고받는다 — 카드를 실제로 화면에 그렸을 때.** 새 route `POST /api/svc/agent-discovery/attributions/impressions` `{recommendation_id, cards[{owner_user_id, position, section_topic_id}]}`, 204. **motivation**: 클라이언트가 목록을 **미리 fetch해 두고 나중에 그린다.** 그래서 §8의 `served_at`은 "답한 때"이지 "본 때"가 아니고, 받아만 놓고 스크롤하지 않은 목록까지 전부 분모에 들어간다 — R47이 재려던 비율이 **fetch 수로 나눈 값**이 되고, 왜곡의 크기조차 알 수 없다. 타입 ②에서 더 크다(선반이 세로로 쌓여 아래 선반은 대개 눈에 안 닿는다 — dev 데이터에서 자손 매칭 선반은 4~7번째에 있었다). **적히는 곳은 결정 로그**(`REC#{id}/LOG`의 `impressions[]`)**이지 PostgreSQL이 아니다**: 읽는 쪽이 평가 배치 하나뿐이고 조인 상대가 없다 — §2-5가 SQL에 있는 이유(워커가 `message_created`와 조인)가 여기엔 없다. 그래서 새 테이블·새 스윕·새 보존 규칙이 없고 그 항목의 TTL을 그대로 탄다. append는 `attribute_exists(PK) AND requester_user_id`(§8 `pages[]`와 같은 조건)에 **카드 수 상한 한 절을 더해** 일어나므로 **우리가 낸 적 없는 목록도, 남의 목록도 적히지 않는다** — §2-5와 달리 검증되는 보고다. 그 상한이 레지스터 행 `attribution.max_impression_cards`이고, **건수가 아니라 카드 수를 센다**(건수만 묶으면 항목이 DynamoDB의 400 KB를 넘고, 넘긴 항목은 같은 목록의 `pages[]`까지 못 받는다). 조건에 걸려도 204(404는 id 존재를 묻는 도구가 된다). **측정 전용이다**: 노출을 랭킹에 먹이는 것(피로도·중복 억제)은 이 데이터가 쌓인 뒤 실측으로 내릴 별개 결정이고, 지금 임계를 고르면 짐작이다. **그리고 시작 보고(§2-5)에 `section_topic_id`·`position`이 붙는다**(nullable 컬럼 둘, 마이그레이션 하나): R65 이후 한 소유자가 한 화면의 두 선반에 동시에 서므로 `(recommendation_id, owner_user_id)`가 카드를 가리키지 못하고, 그러면 분모는 선반별로 갈리는데 분자가 안 갈린다. 타입 ②는 **선반마다 position이 1부터 다시 시작하므로**(`by_topic.py` `start_position=1`) 카드를 가리키는 것은 `(section_topic_id, position)` 짝이다. 응답 스키마는 바뀌지 않는다 — 세 값이 이미 응답에 있다. **다만 셋이 같은 곳에 있지는 않다**: `owner_user_id`·`position`은 카드에, **선반 topic은 카드를 감싼 섹션**(`DiscoverSection.topic_id`)에 있다. 카드의 `matched_topics[].topic_id`는 R65 이후 대개 선반의 자손이므로 거기서 읽으면 안 된다 | 오너 | 2026-09-23 | 계약 §2-5·§2-6·§6·§6-1·§8, 설정 레지스터 §9, 요청서 client §2·§3·§4, walkthrough §2-2, 구현 진행 20 |

## 열린 항목 (결정 대기)

| # | 항목 | 누가 |
|---|---|---|
| ~~O1~~ | ~~agent 공개 여부의 필드: `enabled` 재사용인지 새 필드인지~~ → **R23으로 닫힘**(2026-09-09) | — |
| ~~O2~~ | ~~요청자 자신의 topic 목록을 **어디서** 읽나: (a) 요청 시 topic-api 내부 route(권고) vs (b) 소유자 키 아래에만 미러. 읽는 tier 범위(public·friends·private, hidden 제외)는 R22로 닫혔다~~ → **R27로 닫힘**(2026-09-09) | — |
| ~~O3~~ | ~~`recommendation_id`를 클라이언트가 대화 시작 요청에 실어 주는 변경을 지금 요청 범위에 넣는가~~ → **R24로 닫힘**(2026-09-09) | — |
| ~~O4~~ | ~~인기도 정의(기간, 대화 시작만/지속 포함, 신규 부스트)~~ → **R29로 닫힘**(2026-09-09) | — |
| O5 | "맞음/안 맞음" 표시의 유무와 축. **2026-09-09**: "안 맞음"은 첫 버전에 넣지 않는다(R41). 기획 중인 방향은 "나와 비슷해요"(겹치는 관심사) / "다른 관점도 살펴봐요"(요청자에게 없는 관심사) 두 축 — 확정 전. 계약 `fit`은 자리만. **2026-09-10**: "다른 관점" 축의 재료 후보로 HEXACO 성향(R42·O20) — 겹치지 않는 topic은 관심이 없다는 뜻이지 다른 관점이라는 뜻이 아니다 | 오너 (기획 디자인 중) |
| O6 | 명시 피드백(좋아요/숨기기)의 유무. **2026-09-09**: 제품 논의 없음 — 미룸. 계약에 이벤트 자리도 두지 않고, 생기면 그때 정의한다 | 오너 (미룸) |
| ~~O7~~ | ~~재활성 이벤트가 없다 — **탈퇴 뒤** 돌아온 유저를 어떻게 아는가(`user_registered`가 다시 나오나). "오래 안 쓰다 돌아온" 유저는 R19의 `last_active_at`이 우리 신호로 잡으므로 이 항목은 탈퇴·재활성만 남는다~~ → **R38로 닫힘**(2026-09-09) | — |
| ~~O8~~ | ~~A안의 엔진: Gorse(서비스형) vs `implicit`(라이브러리형). recsys 라이브러리 사용은 A안으로 분류한다. **근거(2026-09-08, `library_verification.md`)**: LightFM은 Python 3.14에서 빌드 실패라 후보에서 뺐다. `implicit` ALS는 10만×10만에서 fit 1~11 s·RSS 300 MB. Gorse v0.5.11은 Redis cache store에 RediSearch를 요구해 R08 안에서는 MySQL/PostgreSQL cache store만 가능하고, 그때 10만 유저 상시 재계산 사이클(1.5~7분)에 cache 1,400만 행. 기본 설정에서는 CF가 서빙되지 않는 원인 둘(§4-3)이 있고, 고친 뒤 서빙 HR@10 0.0006은 `implicit` ALS(0.0054)의 1/9~~ → **R33으로 닫힘**(2026-09-09) | — |
| ~~O9~~ | ~~타입 ② 섹션 크기·순서·페이지(섹션당 몇 명, 한 화면에 몇 섹션, 섹션 순서가 요청자 preference 점수인지 — 지금은 preference)~~ → **R30으로 닫힘**(2026-09-09) | — |
| ~~O10~~ | ~~요청자의 친구 집합을 요청 시 bourbon-api에서 조회하나 `friendship_changed`로 미러하나~~ → **R17로 닫힘**(2026-09-08, 미러) | — |
| ~~O11~~ | ~~타입 ③ B안 CF의 첫 구현: 아이템 기반 이웃 vs 행렬 분해. **근거(2026-09-08, `library_verification.md` §3)**: 스펙 밀도(λ=3)에서 코사인 정규화 이웃은 무작위 수준, 정규화 없는 동시 출현은 ALS 16f의 절반, 어느 CF도 인기도를 넘지 못한다. λ=10에서 ALS 16f와 동시 출현+인기도가 인기도와 동률이 되며 군집 신호는 두 배~~ → **R34로 닫힘**(2026-09-09) | — |
| ~~O12~~ | ~~friends tier 근거의 후보를 랭커에서 올릴지(`tier_is_friends` 가중)~~ → **R32로 닫힘**(2026-09-09) | — |
| ~~O13~~ | ~~타입 ③ 갱신의 값들: TTL(예 24시간), 스윕 주기(예 30분), 사이클당 상한, 전역 학습 주기(예 하루 1회), `cf_candidates_stale` 임계(TTL의 몇 배, 예 7일). 모양은 R19~~ → **R36으로 닫힘**(2026-09-09) | — |
| ~~O14~~ | ~~**CF 가중을 켜는 기준을 자동 판정으로 두는가.** 제안: 배치가 주기적으로 ALS를 학습해 실제 로그 leave-one-out에서 인기도와 비교하고, HR@10과 군집 다양성에서 이길 때만 랭커의 `cf_score` 가중을 0에서 올린다. λ 추정으로 시점을 정하지 않는다. 근거 `library_verification.md` §3~~ → **R35로 닫힘**(2026-09-09) | — |
| O15 | **배움을 위한 탐색(exploration) 항을 랭커 목표에 넣는가.** 추천이 만든 대화를 CF가 다시 배우는 노출 편향이 있고, 목표가 "새 지식을 만나게 하기"면 인기도·유사도만으로는 같은 사람만 보인다. 결정 로그의 `entry`·`recommendation_id`로 보정 가능. **첫 답 R20**(점수 구간 안 섞기). 남은 것: 그 이상의 탐색 항(ε-random, 노출 보정 가중)을 넣는가 **2026-09-09**: 미룸 — 결정 로그가 쌓인 뒤 본다 | 오너 (미룸) |
| O20 | **persona(HEXACO)의 실제 도입.** R42가 방향·키 비공유·코드 자리를 정했다. 남은 것: (1) **경로** — 이벤트로 받는다는 방향만 있다(R42, 확정 아님; bourbon-agent에 API를 열 가능성은 없다). `persona_updated`의 traits 변경에 요약 벡터를 싣는가, 별도 이벤트인가, 어느 시점(추출 직후·revision마다)에 내는가. HEXACO 추정치는 sharable slot이 아니라 추출 노트(`PERSONA_EXTRACTION#note`)에 있어 visibility 구분이 없으므로 bourbon-agent가 무엇을 내보낼지 먼저 정해야 한다; (2) **동의 범위** — 후보(소유자) 쪽 벡터를 우리 저장소에 두는 것과 불변식 1("공개된 row만")의 관계, traits 공개 여부에 대한 기획; (3) **용도** — 랭커 feature(가중을 0에서 올리는 규칙 — R35와 같은 자동 게이트가 후보)와 O5의 "다른 관점" 축 재료 중 어디까지; (4) **검증** — 합성 군집에 facet 벡터를 더해 ablation 표에 "persona만" 행, confidence·관측 수 가중 | 오너(경로·동의 범위·기획) + bourbon-agent(무엇을 내보낼지) |
| O22 | **"추천 목록에 올릴지"를 따로 둘 것인가.** R57이 `discoverable`을 "public topic이 있나"의 파생값으로 만들면서, R23이 지키려던 구분이 스위치 하나로 합쳐졌다 — **누구나 말 걸 수 있다**(플랫폼의 `agents.public`, agent DM 게이트)와 **낯선 사람에게 추천된다**(우리)는 세기가 다른 노출인데, 지금은 public topic을 하나 공개하면 둘 다 켜지고 빼는 방법은 그 topic을 비공개로 돌리는 것뿐이다. 따로 두기로 하면 **bourbon-api에 다시 요청할 일은 아니다** — 우리 추천 목록에 대한 설정이므로 우리 route와 우리 컬럼으로 끝난다(파생값과 옵트인 둘을 나누고 후보 조회가 둘 다 본다). 자리는 이미 있다: R57이 `public` 갈래의 `discoverable` 조건을 중복인 채로 남겨둔 것이 그것이다 | 오너 (기획) |
| O23 | **부분 grounding 손실을 답이 말해야 하는가.** R60이 게이트를 켜면서 판별기가 답을 못 한 그룹이 생겼는데, 다른 그룹이 하나라도 grounded면 응답은 200이고 `resolved_topics`가 조용히 짧아진다 — 호출자는 원래 몇 개였는지 알 방법이 없다. 같은 성격인 `expansion_partial`은 `degraded[]`에 있는데 이것만 없다. 두 갈래: (1) 타입 ①의 `DegradedReason`에 값을 하나 더한다 — **계약 변경**이라 `api/structs/discovery.py`의 `Literal`과 호출자가 같이 움직인다; (2) 로그로만 두고 계약은 그대로 둔다. 얼어붙은 `POST /recommend`는 갈래가 아니다 — 그 응답에는 `degraded` 필드 자체가 없고, bourbon-agent가 옮겨가면 사라진다. **먼저 빈도를 잰다**: #45가 두 완료 로그 줄에 `groups_disambiguation_failed`를 올렸으므로, 실제로 얼마나 자주 일어나는지 보고 나서 계약을 움직일지 정한다 | 오너 (기획) |
| O24 | **DynamoDB가 답하지 않는 것도 §3-3의 "저장소 무응답"인가.** `store_unavailable`(503)은 지금 SQLAlchemy의 `OperationalError`·`InterfaceError` 둘만을 뜻한다. 타입 ③은 세 저장소를 읽는데 그중 CF 풀만 DynamoDB이고, `storage/items.py`가 botocore 예외를 번역하지 않아 `to_app_error`가 모른다 — **그래서 DynamoDB 장애 하나는 500 `internal_server_error`로, 우리 결함으로 접수된다**. Postgres가 같은 이유로 죽으면 503인데. 두 갈래: (1) botocore의 전송 계열(`EndpointConnectionError`·`ConnectTimeoutError`·5xx `ClientError`)을 `store_unavailable`로 매핑한다 — 알람과 페이징 대상이 바뀐다; (2) 그대로 둔다 — 요청 경로에 있는 DynamoDB 읽기가 CF 풀 하나뿐이라, 그것이 죽었을 때 답을 못 주는 것을 남의 장애로 적는 것이 맞는지가 불분명하다. **`cf.pool()`이 일부러 잡지 않는 것과는 다른 질문이다** — 그쪽은 "풀 없이 답할지"이고(계약의 `degraded`는 "낡은 pool"만 정의한다), 이쪽은 "못 답한 것을 누구 탓으로 적을지"다. **dev 배포 뒤에 빈도를 보고 정한다** | 오너 |
| O25 | **이벤트 로그 row가 "어떤 이벤트인가"만 적고 "어떤 모양인가"는 적지 않는다 — 버전 필드를 넣을 것인가.** 계약 §6-1의 `EVT#` item은 이벤트 이름·시각·payload를 담고 스키마 버전이 없다. 퍼블리셔가 payload 모양을 바꾸면 보존 기간(`events.ttl_days` = 180일) 동안 **한 이름 아래 두 모양이 같이 남고**, 읽는 쪽은 어떤 키가 있는지로만 구분한다. `bourbon.topics_updated`가 첫 사례다(topic-api #76): #76 이전 row에는 `topic_id`가, 이후 row에는 `topics`가 있고, 새 미러는 옛 row를 **아무 말 없이 읽고 그 필드를 버린다** — 선언하지 않은 것을 무시하는 것이 이벤트를 잃지 않게 하는 그 규칙이기 때문이다. **replay는 무사하다**(어느 쪽이든 유저를 재조회한다). 흔들리는 것은 집계다 — 이벤트당 topic 수, 유저·일당 이벤트 수는 배포 시점에 계단이 생기고, 합성 스트림(`synthetic/events.py`)은 아직 옛 알갱이를 낸다. 넣는다면 item에 `schema` 한 칸이고 keyspace 결정이다. 안 넣는다면 캘리브레이션이 생길 때 "이 날짜 이전/이후"를 읽는 쪽이 알아야 한다. 캘리브레이션 job이 아직 없으므로 **그때 결정한다** | — | 2026-09-17 | 계약 §6-1, `worker/journal.py`, `agent_discovery/storage/items.py`, 진행표 15 |
| ~~O21~~ | ~~**PostgreSQL 신규 도입 확인.** R18은 조인·집계 집합을 PostgreSQL에 두는데, 인프라 방침(2026-09-10 공유)은 "마이크로서비스별 주 DB로 DynamoDB 권장"이고 플랫폼 어느 서비스도 PostgreSQL을 쓰지 않는다. `requests/infra.md` §1에 근거와 MySQL 대안을 적었다~~ → **R45로 닫힘**(2026-09-10, 인프라 확답) | — |
| ~~O16~~ | ~~**파라미터 자동 보정의 주기·샘플 임계값·보존 기간**(합성 스펙 §8). 제안: 하루 1회, 90일 창, 활성 유저 1,000명·대화 시작 5,000건 미만이면 기본값 유지, `event_log` 보존 180일~~ → **R37로 닫힘**(2026-09-09) | — |
| ~~O17~~ | ~~**오래 활동하지 않은 소유자의 agent를 후보에서 하드 제외하는가.** R19는 제외하지 않고 `recency`·`popularity`로 순위만 내린다. agent는 소유자 없이도 대화하므로 "오래 안 들어온 사람의 agent는 추천하지 않는다"가 제품 의도인지는 오너 판단. 하기로 하면 후보 조회 조건에 `last_active_at` 조건 하나~~ → **R31로 닫힘**(2026-09-09) | — |
| ~~O18~~ | ~~**content 유사도의 정의.** 지금은 topic id 정확 일치의 가중 합이다. 제안: (a) IDF 가중 `log(전체 소유자 수 / 그 topic을 가진 소유자 수)` — 흔한 topic 겹침의 과대평가를 막는다; (b) 계층 감쇠 — topic-api 검색 설정(`SEARCH_DEFAULT_DECAY` 0.6, `SEARCH_MAX_DEPTH` 3)의 상수 **0.6/hop, 최대 3 hop**을 빌린다. 빌리는 것은 상수다: topic-api는 `/search/users`에서 쿼리 topic의 자손(하향) 방향에 `decay^distance`를 쓰고 단일 topic route(`/topics/{id}/users`)는 감쇠가 없는데, 우리는 요청자 topic과 소유자 topic 사이의 양방향 최단 hop에 쓴다(요청자 `hand_drip` ↔ 소유자 `coffee`처럼 부모만 가진 소유자도 0.6). 카탈로그는 DAG라 최단 hop, 경로당 가장 구체적인 일치 하나만 세고(하위 정확 일치 1.0 > 부모만 일치 0.6), drawer(`selectable: false`)는 일치 노드가 아니라 통과 노드; (c) 임베딩 kNN(pgvector/OpenSearch)은 넣지 않는다 — 카탈로그 자체가 의미 계층이고 grounding이 `related`까지 표시한다. 제안이 정하는 다시 볼 조건: topic-api의 "grounding 실패 preference 비율"이 나쁠 때. **제안에서 제외한 것**: "요청자가 대화한 agent들의 topic을 요청자 프로필에 더한다" — bourbon-agent가 요청자의 발화에서 이미 persona를 추출해 topic으로 흘러오고, agent의 발화는 evidence가 될 수 없다는 추출기 규칙과 정면으로 어긋난다(행동 신호는 CF의 몫). 필요한 것: 카탈로그 그래프 내부 route(이벤트 정의서 §2-8). 측정: 비교표에 "content만" 행~~ → **R25로 닫힘(카탈로그 소스는 R26)**(2026-09-09) | — |
| ~~O19~~ | ~~**요청자가 이미 대화를 시작한 agent를 for-you(타입 ③)에서 제외하는가.** 계약 §2-3과 합성 정답(스펙 §4 ③ CF)은 제외를 가정하고 있고, walkthrough §5-3·§5-4가 A·H를 그 가정으로 뺀다. 제품이 다르게 정하면 셋을 같이 바꾼다~~ → **R28로 닫힘**(2026-09-09) | — |
