# 검증 결과 — 10만 합성 유저 (2026-09-14)

> `implementation_plan.md` §1-9의 산출물. 재설계 §6의 프로토콜대로 10만 합성 모집단에서 품질·지연 시간·게이트를 재고, 그 위에서 설정 초기값을 판단한 기록이다.
>
> 값은 코드 repo의 `agent_discovery/settings.py`에 있고(R54), 뜻은 `agent_discovery_settings.md`에, 결정은 `decisions.md`에 있다. **이 문서는 그 값들의 근거다.**
>
> 측정 도구는 코드 repo의 `evaluation/` 패키지이고 `python -m cli validate {population,quality,invariants,latency,gate}`로 다시 돌릴 수 있다. `invariants`는 §11-1에서 추가됐다.

---

## 0. 이 문서를 읽는 법

### 0-1. 무엇을 한 실험인가

실제 유저 데이터가 없으므로 **정답을 아는 모집단을 만들어서** 잰다. 생성기(`synthetic_population_spec.md`)가 10만 명의 유저·군집·친구 관계·대화 로그를 만드는데, 이때 **군집 사이의 잠재 친화도(`affinity`)** 를 심어 두고 그걸로 대화 상대를 고르게 한다. 서비스는 그 친화도를 볼 수 없다. 그래서 "서비스가 만든 추천"과 "생성기만 아는 정답"을 비교하면 추천이 얼마나 맞았는지를 잴 수 있다.

측정은 세 종류이고, **읽는 법이 서로 다르다.**

- **ablation**(§3·§4) — 랭커의 가중치를 하나만 켜고 나머지를 0으로 두어 **소스 하나가 혼자 답하게** 한 뒤 정답과 대조한다. "어느 소스가 무엇을 아는가"를 보는 것이지 "무엇을 배포할까"를 보는 것이 아니다. 결과는 **평균**이고, 높을수록 좋지만 어디까지가 좋은지는 아래 0-2의 기준선으로 읽는다.
- **게이트(R35)**(§5) — 유저마다 실제로 대화한 상대 하나를 숨기고, 나머지로 학습한 뒤 ALS와 인기도에게 각각 10명을 추천하게 해서 **숨긴 상대를 맞히는지** 본다. 이것이 실서비스에서 `w_cf`를 자동으로 올리고 내리는 그 판정이다. 결과는 **비율**이고, 임계와 비교해서 읽는다.
- **불변식 카운트**(§11-1) — 위 둘이 "답이 얼마나 맞았나"라면 이건 **"답이 담아서는 안 되는 것을 담았나"** 다. 결과는 **개수**이고 **0이 아니면 결함**이다. 평균도 비율도 아니므로 잘/못을 논할 여지가 없는 대신, **0이 저절로 증거가 되지는 않는다** — 셀 것이 없는 모집단에서는 전부 새는 서비스도 0을 낸다. 그래서 이 종류만 "무엇을 거절할 기회가 있었나"라는 분모를 함께 싣는다.

지연 시간(§6)은 셋 중 어디에도 속하지 않는다. 정답이 없는 측정이고, **이 머신에서는 절대값을 낼 수 없다**(§0-3).

### 0-2. 지표의 뜻

| 지표 | 답하는 질문 | 읽는 법 |
|---|---|---|
| **recall@10** | 생성기가 아는 정답 상위 10명과 추천 10명이 얼마나 겹치나 | 0~1. 0.313이면 정답 10명 중 3.1명을 맞혔다 |
| **NDCG@10** | recall과 같은 질문인데 **순위**를 본다 | 정답 1위를 1위에 놓으면 만점. 같은 사람을 찾아도 아래쪽에 놓으면 낮다 |
| **HR@10** (`hr_cf`, `hr_popularity`) | 숨긴 상대가 추천 10명 안에 들어온 유저의 비율 | 후보가 5만 명대라 절대값이 작다. **인기도 대비**로 읽는다 |
| **`hr_ratio`** | ALS의 HR ÷ 인기도의 HR | 1.0이면 비긴 것. `gate.min_hr_ratio`가 이 값을 읽는다 |
| **`cluster_gain`** | ALS의 "같은 군집 비율" ÷ 인기도의 같은 군집 비율 | "개인화가 되고 있나"를 본다. `gate.min_cluster_gain`이 읽는다 |
| **`chance`** | 그 눈금에서 **두 사람을 아무렇게나 뽑았을 때** 겹칠 확률 | 없으면 0.13을 0.05와 비교할 수 없다. 이번에 처음 넣었다 |
| **`cluster`** (§3) | 추천 10명 중 요청자와 **같은 군집**인 사람의 비율 | 생성기의 군집 라벨로 잰다. "개인화가 되고 있나"의 가장 직접적인 형태 |
| **`proxy`** (§3) | 추천 10명 중 **요청자가 공개한 topic을 같이 공개한** 사람의 비율 | 구현이 실제로 쓸 수 있는 눈금(계약 §11). content 소스에 대해서는 거의 동어반복이라 근거로 안 쓴다 |
| **`empty`** (§3) | 아무도 못 내놓은 답의 수 | 정확도 지표는 이걸 **놓친 것으로 채점**하고, 비율 지표(`cluster`·`proxy`)는 **건너뛴다** — 아무도 없는 목록에는 비율이 없다 |

**recall·NDCG의 바닥과 천장.** 정답 파일은 "정답을 다 아는 오라클이 냈을 상위 10명"이므로 **천장은 1.0**이다. 바닥은 0이 아니라 무작위 기대값인데, 후보가 5만 명대이므로 10명을 아무렇게나 골라 맞힐 기대값은 **0.0002 수준**이다(후보를 10만으로 잡아도 자릿수는 같다). 그래서 §3의 0.31은 "무작위보다 세 자릿수 위이고 천장의 3분의 1"로 읽는다 — 좋다/나쁘다가 아니라 **소스끼리 비교하라고 있는 숫자**다.

### 0-3. 절대값과 비율 중 무엇을 믿나

**이 문서에서 믿을 수 있는 것은 대부분 비율이고, 절대값에는 조건이 붙는다.**

- **지연 시간의 절대값은 못 믿는다.** 같은 조건 다섯 번에서 1.6~2.1배로 흔들렸다(§6). 한 런 **안에서의** 비교만 결과다.
- **HR의 절대값은 작다.** 후보 5만 명 중 10명을 고르는 일이라 그렇다. 인기도 대비로 읽는다.
- **`personal`(같은 군집 비율)의 절대값은 눈금마다 4배씩 다르다.** 비율(`cluster_gain`)은 눈금이 달라도 거의 같다(§5-1). 그래서 비율로 읽는다.
- **불변식 카운트(§11-1)만은 절대값이 전부다.** 0이거나 결함이거나이고 비율로 읽을 것이 없다. 대신 **그 0을 믿어도 되는지**가 따로 문제이고, §11-1이 분모와 변이 검증으로 그것을 다룬다.

---

## 1. 한눈에

1. **게이트는 잰 모든 조건에서 닫힌다.** ALS는 인기도 적중률의 **0.39~0.61**이고 `gate.min_hr_ratio` 1.0에 닿지 않는다. `w_cf`는 0으로 남는다 — R35가 의도한 상태다.
2. **`w_cf`를 올려야 한다던 1-8의 완료 조건은 충족되지 않는다.** 그건 이미 알려져 있었다(§1-8의 주석, 2026-09-12). **새로 알게 된 것은 "충족 여부를 이 샘플로는 판별할 수 없다"** 는 것이다 — `hr_ratio`의 95% 신뢰구간이 ±0.2라 λ 사이 차이가 노이즈에 묻힌다(§5-3).
3. **계약 §11이 걱정한 "눈금이 다르다"는 비율에서는 문제가 아니다.** 10만에서 세 눈금이 전부 `cluster_gain` 0.86~0.92를 준다. **문제는 모집단 크기다** — 2만에서는 같은 눈금이 0.93과 1.28로 갈린다(§5-2).
4. **1-8이 쓰던 눈금은 포화해서 못 쓴다.** 그 눈금에서는 ALS도 인기도도 **자기 무작위 기저보다 낮다**(§5-1).
5. **스펙 §4의 타입 ③ CF 정답은 4분의 3이 인기도로 설명된다.** 그래서 인기도만 켠 랭커가 CF 정답을 CF 자신보다 잘 맞힌다(0.807 대 0.313). **지금 지표로는 두 소스를 가를 수 없다**(§4).
6. **1~5는 전부 생성기 파라미터에 대한 사실이지 구현에 대한 사실이 아니다.** 그래서 이 문서는 게이트 임계를 **옮기지 않는다** — 옮길 근거도, 그대로 둘 근거도 측정이 주지 않았다.
7. **IDF 분모를 요청마다 세면 타입 ③ 요청이 거의 두 배가 된다**(§6-1). R59의 값이 처음으로 숫자가 됐다.
8. 콜드스타트 유저는 10만·λ=10에서 **5명**이고, 그 다섯에게 **CF는 구조적으로 아무것도 주지 않는다**(§3).
9. **서비스 응답의 필터 불변식은 10만에서 0이다**(§11-1). 4,525개 답, 54,925장 카드, 68,151개 row 지목에서 위반 0건. **그리고 그 0이 카운터가 죽어서 나온 값이 아니라는 것을 변이로 확인했다** — 가시성 조건을 한 줄 고장 내면 102~197건이 샌다. 다만 **여덟 열이 다 같은 무게는 아니다**: friends 유출·자기 추천·커버리지 역전·소유자 단위 유출 넷은 변이로 발생시켜 봤고, 나머지 넷은 읽기 결함 하나로는 발생할 수 없는 적재 경로·구조 가드다(§11-1). 비공개 전환은 정적 적재에 시간 창이 없어 **직접 만들어서** 쟀다(50명 전환 → 재조회가 704건 거절 → 노출 0).

---

## 2. 측정 조건

| | |
|---|---|
| 코드 | `bourbon-agent-discovery-api` `052a64d` (브랜치 `recsys/the-lever-that-turns-the-conversation-rate`) |
| 설정 해시 | `v1@452a57163831` — 레지스터의 코드 기본값 그대로, 오버라이드 없음 |
| 모집단 | `run_id` `7f468673de7f-20260908`, `--users 100000 --conversation-rate 10 --now 2026-09-14T00:00:00Z` |
| 런타임 | Python 3.14.2 · implicit 0.7.3 · numpy 2.5.3 · SQLAlchemy 2.0.52 |
| 머신 | macOS(Darwin 25.6.0), 12 코어 / 24 GB. PostgreSQL 17-alpine과 dynamodb-local은 Docker Desktop |
| 적재 경로 | 직접 적재(스펙 §0의 경로 나). 이벤트 리플레이 경로는 쓰지 않았다 |

**대체한 것 하나를 먼저 밝힌다.** 요청자 본인의 topic 재조회(R27)는 실서비스에서 topic-api HTTP 호출이지만, 측정에서는 생성기 배열을 읽는 인메모리 stand-in이다(`evaluation/stack.py::Held`). 그래서 §6 표의 `query` 단계가 타입 ②에서 0에 가깝다 — topic-api 왕복이 거기 빠져 있다. 이것이 `topic_api.timeout_ms`에 대해 무엇을 말할 수 있고 없는지는 §8-1에 적었다.

**10만 모집단의 크기**(실측):

| 테이블 | 행 | 용량 |
|---|---|---|
| `friends` | 3,679,354 | 615 MB |
| `interactions` | 728,292 | 210 MB |
| `room_turns` | 728,292 | 92 MB |
| `visible_topic_rows` | 323,068 | 78 MB |
| `agents` | 98,943 | 18 MB |
| `popularity` | 54,779 | 8.5 MB |
| **합계** | | **약 1,022 MB** |

스펙 §5의 추정과 대조하면 `friends` 약 366만 ✓, `agents` = 10만 − 탈퇴 ✓. `interactions`는 대화 **997,868건**(= U × λ ✓)을 방 단위로 접은 수다 — **대화의 27%가 이미 있는 방을 다시 연다.** §5의 "U × λ ≈ 30만"은 λ=3 기준이다.

생성·적재·fit·스윕·측정 전체가 **약 7분**이다(단일 프로세스). 스윕은 **98,938명**의 pool을 만들었다 — `agents` 98,943명 중 다섯이 빠지는데, **그 다섯이 정확히 콜드 코호트다**(§3).

---

## 3. 품질 — 스펙 §4 타입 ③

### 무엇을 물었나

타입 ③(메인 화면)은 소스 셋을 합쳐 답한다: 인기도, content(카탈로그 겹침), CF. **각 소스가 혼자서는 무엇을 아는가?**

### 어떻게 쟀나

랭커의 가중치를 하나만 켜고 나머지를 0으로 두어 네 가지 랭커를 만들고, **같은 사람들**에게 물어 **두 정답 파일 각각**에 채점했다.

- 정답 파일 둘: **CF 정답**(`affinity × pop`, 생성기만 아는 잠재 구조)과 **content 정답**(R25의 겹침)
- 샘플 505명 — `synthetic/truth.py`의 상수 `FOR_YOU_ASKERS` 500명에 콜드 5명
- R20 섞기는 끄고 결정 로그의 `ranked`(섞기 전 순서)를 채점했다 — 스펙 §4의 지시다

### 숫자

```
ranker      cohort askers empty  recall cf  content   ndcg cf  content   cluster    proxy
popularity  all       505     0      0.807    0.000     0.918    0.000     0.122    0.051
popularity  cold        5     0      0.840    0.000     0.942    0.000     0.102    0.028
content     all       505     1      0.000    0.319     0.000    0.769     0.578    0.468
content     cold        5     0      0.000    0.260     0.000    0.757     0.463    0.357
cf          all       505     5      0.313    0.000     0.533    0.000     0.205    0.062
cf          cold        5     5      0.000    0.000     0.000    0.000      --       --
combined    all       505     0      0.334    0.249     0.449    0.419     0.338    0.227
combined    cold        5     0      0.100    0.260     0.145    0.623     0.282    0.193
```

### 읽는 법

**행으로 읽으면 대각선이 선다.** 단독 소스 셋은 각각 자기 정답 파일을 상대 파일보다 잘 회수한다(인기도 0.807 대 0.000, content 0.319 대 0.000, CF 0.313 대 0.000). 파이프라인이 각 소스의 정답을 실제로 복원한다는 뜻이고, **표가 성립한다는 증거는 여기까지다.**

**열로 읽으면 CF 열에서 무너진다.** CF 정답을 가장 잘 회수하는 것은 CF(0.313)가 아니라 **인기도(0.807)** 다. 이건 파이프라인 결함이 아니라 **정답 파일의 성질**이고 §4가 그걸 따로 잰다. 코드의 라이브 테스트도 행 방향 둘만 단언하고 이 열은 단언하지 않는다 — 테스트가 잡을 수 있는 종류의 사실이 아니기 때문이다.

**`combined` 행은 대각선의 일부가 아니다.** 합친 랭커에는 "자기 정답 파일"이 없다. 두 정답 모두에서 단독 소스보다 낮은 것(0.334 < 0.807, 0.249 < 0.319)은 당연하다 — 여러 신호를 섞으면 어느 한 정답에 대한 순도가 떨어진다. **이 표는 무엇을 배포할지 고르는 표가 아니다.**

**`empty`** 는 아무도 못 내놓은 답의 수다. CF 행의 5는 전부 콜드 코호트다.

**`--` 는 0이 아니라 "잴 것이 없었다"는 뜻이다.** CF 콜드 행의 `cluster`·`proxy`가 그렇다 — 다섯 명 전부 답이 비어서 비율을 낼 목록 자체가 없다. 0이라고 적었으면 "열 명을 냈는데 같은 군집이 하나도 없었다"로 읽혔을 것이고, 그건 전혀 다른 사실이다. 반대로 같은 행의 recall·NDCG는 0이 맞다 — 못 찾은 것은 못 찾은 것이다.

### 그래서

1. **콜드스타트에서 CF는 구조적으로 아무것도 기여하지 않는다.** 대화가 없으면 R19의 스윕 대상이 아니고(`storage/cf.py`의 `due` 조건이 "누군가와 대화한 적 있는 사람"이다) 따라서 pool이 없다. 결함이 아니라 계약 §2-3 그대로이고, 그 화면의 `basis`가 인기도·content로 답하는 근거다. **처음으로 측정됐다.**
2. **content가 가장 개인화돼 있다** — 군집 라벨 눈금에서 0.578로 인기도(0.122)의 **4.7배**다. `rank.for_you.w_content`가 큰 값이어야 한다는 **방향**을 지지한다. (`proxy` 열의 9.2배는 근거로 쓰지 않는다 — 그 눈금은 "요청자가 공개한 topic을 소유자도 공개했나"이고 content 소스는 바로 그 겹침으로 순위를 매기므로 거의 동어반복이다.)
3. **콜드 코호트는 5명이다.** 지어내지 않았고, 그래서 콜드 행의 수치는 **다섯 사람에 대한 것**이다. 제대로 재려면 `conversation_rate`가 0을 많이 내는 파라미터가 따로 필요하다(스펙 §4가 이미 그렇게 적어 두었다).

---

## 4. 정답이 무엇으로 만들어져 있나

### 무엇을 물었나

§3에서 인기도가 CF 정답을 CF보다 잘 맞혔다. **정답 파일 자체가 인기도로 되어 있는 것 아닌가?**

### 어떻게 쟀나

스펙 §4의 타입 ③ CF 정답은 `affinity[군집(u)][군집(소유자)] × pop[a]`다. `affinity` 행렬을 **전부 1로 바꾸고** 생성기의 같은 함수를 다시 불렀다. 그러면 동일한 가시성·R28 필터 아래 **인기도만으로** 정렬된 목록이 나온다. 두 목록의 겹침이 답이다.

### 숫자

| 측정 | 값 |
|---|---|
| 인기도만으로 회수하는 오라클 top-10 | **0.765** |
| 1위를 같게 맞히는 비율 | 0.233 |
| 인기도 분포 | p50 0.0082 · p99 0.0649 · max 1.0 (**p50→max 122배**) |
| `affinity` 분포 | min 0.270 · median 0.502 · max 1.251 (**전체 범위 4.6배**) |

### 읽는 법

`popularity_skew` 2.0이 만드는 인기도가 두 자릿수 배율로 흩어지는 동안 `affinity`는 5배 안에 있다. 곱에서 **어느 항이 순서를 정하는지**가 이 두 줄이다. 1위 일치율이 0.233이므로 순서에는 친화도 정보가 남아 있지만, **집합 겹침인 recall@10으로는 CF와 인기도를 가를 수 없다.**

### 그래서

스펙 §7의 열린 항목 "정답의 선명도"가 다른 얼굴로 돌아왔다. **고치는 방법 셋은 전부 스펙 §2·§4를 고치는 일이고 오너 판단이다**: `affinity` 대비를 키우거나, `popularity_skew`를 낮추거나, 타입 ③ CF의 지표를 집합 겹침이 아니라 순위 상관으로 바꾸거나.

---

## 5. 게이트 재측정 — 계약 §11

### 무엇을 물었나

`gate.min_cluster_gain` 1.5는 **합성 모집단의 군집 라벨**로 정해졌는데, 실서비스에는 그 라벨이 없어서 구현은 **대리 지표**를 쓴다(계약 §11). 눈금이 다른데 임계를 그대로 써도 되나?

### 어떻게 쟀나

**같은 게이트 코드를 눈금만 바꿔 여러 번 돌렸다.** `cf/gate.py`가 `topics[소유자] & topics[요청자]`를 묻기 때문에, 눈금이란 "사람 → 라벨 집합" 매핑 하나다. 사람마다 자기 군집 라벨 하나만 담은 집합을 주면 그 교집합이 곧 "같은 군집인가"가 된다. **프로덕션 코드는 바뀌지 않았고**, 같은 holdout·같은 추천 목록에 대해 질문만 달라진다.

| 눈금 | 정의 | 누가 쓰나 |
|---|---|---|
| **published** | `public` 또는 `friends`로 공개했고 소유자가 살아 있는 row | **구현이 실제로 쓰는 것** — `visible_topic_rows`에 있는 것 |
| **cluster** | 각자의 주 군집 라벨 하나 | 검증 §3-1이 쓴 것 |
| **1-8의 눈금** | `hidden`만 뺀 전 tier (`private` 포함) | 1-8의 스케일 테스트가 쓰던 것 |

### 5-1. 10만·λ=10에서 세 눈금

| 눈금 | ALS | 인기도 | **`cluster_gain`** | `chance` | 라벨 있는 사람 |
|---|---|---|---|---|---|
| 1-8의 눈금 | 0.2044 | 0.2224 | 0.919 | **0.2983** | 100% |
| published | 0.0528 | 0.0617 | 0.856 | 0.0328 | 58% |
| cluster | 0.1329 | 0.1449 | 0.918 | 0.0952 | 100% |

**레벨은 눈금마다 4배까지 차이 나는데 비율은 거의 같다**(0.919 / 0.856 / 0.918).

**1-8의 눈금은 못 쓴다.** `chance`가 0.2983인데 ALS도 인기도도 그보다 **낮다.** `private`까지 넣으면 거의 모두가 거의 모두와 topic을 공유하게 되어 지표가 **포화**한다 — `cf/gate.py`가 "이미 대화한 사람의 topic" 대리 지표를 기각했을 때와 같은 현상이다. 계약 §11과 레지스터 §4에 기록된 **10만 실측 "인기도 0.345 / ALS 0.249"** 는 자릿수로 보아 이 눈금의 값이다(여기서 재현하면 0.2224 / 0.2044로 정확히 일치하지는 않는다 — 나머지 차이는 은퇴한 생성기 몫으로 보이고, 확정하지 않는다).

### 5-2. 두 규모, 세 λ

| N | λ | 눈금 | ALS | 인기도 | **`cluster_gain`** | `hr_cf` | `hr_pop` | **`hr_ratio`** | holdout |
|---|---|---|---|---|---|---|---|---|---|
| 10만 | 3 | published | 0.0555 | 0.0618 | 0.898 | 0.0069 | 0.0115 | 0.600 | 7,066 |
| 10만 | 3 | cluster | 0.1312 | 0.1461 | 0.898 | 〃 | 〃 | 〃 | 〃 |
| 10만 | 10 | published | 0.0528 | 0.0617 | 0.856 | 0.0065 | 0.0117 | 0.556 | 9,964 |
| 10만 | 10 | cluster | 0.1329 | 0.1449 | 0.918 | 〃 | 〃 | 〃 | 〃 |
| 10만 | 30 | published | 0.0512 | 0.0606 | 0.845 | 0.0063 | 0.0122 | 0.516 | 9,999 |
| 10만 | 30 | cluster | 0.1307 | 0.1463 | 0.894 | 〃 | 〃 | 〃 | 〃 |
| 2만 | 3 | published | 0.0460 | 0.0467 | 0.985 | 0.0120 | 0.0276 | 0.436 | 1,415 |
| 2만 | 3 | cluster | 0.1100 | 0.0830 | **1.324** | 〃 | 〃 | 〃 | 〃 |
| 2만 | 10 | published | 0.0469 | 0.0503 | 0.934 | 0.0150 | 0.0361 | 0.417 | 1,994 |
| 2만 | 10 | cluster | 0.1133 | 0.0883 | **1.283** | 〃 | 〃 | 〃 | 〃 |
| 2만 | 30 | published | 0.0521 | 0.0507 | 1.029 | 0.0145 | 0.0375 | 0.387 | 2,000 |
| 2만 | 30 | cluster | 0.1229 | 0.0902 | **1.363** | 〃 | 〃 | 〃 | 〃 |

**눈금 사이 차이**(cluster − published): 10만에서 **0.000 / 0.062 / 0.049**, 2만에서 **0.339 / 0.349 / 0.334**. 범위는 **0.00~0.35**이고 **10만·λ=3은 정확히 동점**이다.

**규모가 원인이다.** 인기도의 군집 라벨 `personal`이 2만 0.083 → 10만 0.145로 거의 두 배가 되는데 `chance`는 0.095로 같다. 로그가 두꺼워질수록 **인기 순위가 군집 쪽으로 선명해진다** — 대화 상대 선택이 친화도로 이뤄지므로. ALS 쪽은 그만큼 움직이지 않는다.

> **계약 §11에 대한 답.** 걱정했던 "눈금이 다르다"는 **비율에서는 문제가 아니다** — 10만에서 세 눈금이 전부 0.86~0.92를 준다. 문제는 **모집단 크기**다. 2만에서는 같은 눈금이 0.93과 1.28로 갈리고, 임계가 읽는 것이 바로 그 비율이다. 그러니 `gate.min_cluster_gain`을 정할 때 확정해야 하는 것은 눈금이 아니라 **어느 규모의 모집단을 기준으로 삼는가**이다. 다만 1-8의 눈금은 포화하므로 어느 쪽이든 쓰면 안 된다.

### 5-3. λ 방향은 판별되지 않는다

`hr_ratio`가 10만에서 0.600 → 0.556 → 0.516으로 **내려간다.** 1-8이 라이브 테스트로 고정했던 방향(올라간다)의 반대다. 원인은 §7의 세 번째 결함이다.

**그런데 뒤집힌 것도 결과가 아니다.** `hr_cf`는 1만 번 물어 60번 안팎을 맞히는 수준이다.

| λ | `hr_cf` | 맞힌 수 | `hr_ratio` | **95% 신뢰구간** |
|---|---|---|---|---|
| 3 | 0.0069 | 49 | 0.600 | [0.39, 0.81] |
| 10 | 0.0065 | 65 | 0.556 | [0.39, 0.72] |
| 30 | 0.0063 | 63 | 0.516 | [0.36, 0.67] |

세 구간이 거의 완전히 겹친다. **어느 방향도 이 샘플로는 판별되지 않는다.** 코드의 라이브 테스트는 방향 단언을 버리고 샘플이 실제로 판별하는 것으로 다시 썼다 — 그 테스트가 도는 두 rate(λ=3·10)에서 `hr_ratio`가 1.0 아래이고 둘의 차이가 구간보다 작다는 것이다. (λ=30에는 라이브 테스트가 없다.)

이 신뢰구간은 **게이트 자체에 대한 사실**이기도 하다: 하루치 판정이 이 정도로 흔들린다. `gate.holdout_ratio` 0.1과 `gate.step` 0.05를 다시 볼 때 이 숫자가 입력이다.

### 5-4. §3-1과의 비교

검증 §3-1은 λ=10에서 ALS 0.272 대 인기도 0.125로 **같은 군집 비율이 두 배**라고 기록했다 — `cluster_gain`으로 **2.18**이다. 이번 측정의 같은 눈금(cluster)에서는 **0.918**이다.

**떨어진 쪽은 ALS만이다.** 인기도의 같은 군집 비율은 0.125 → 0.145로 **올랐다**. §5-2가 말한 메커니즘 그대로다 — 모집단이 커지면 인기 순위가 군집 쪽으로 선명해진다. 그래서 "생성기가 바뀌어 둘 다 나빠졌다"가 아니라 **"ALS가 잃은 것을 인기도가 얻었다"** 가 맞는 서술이다.

### 그래서

1. **게이트는 닫혀 있고, 닫혀 있는 것이 정상 동작이다.** `w_cf` 0은 R35가 의도한 출발점이고 이 측정이 그것을 지지한다. 이 절은 "CF가 쓸모없다"가 아니라 **"이 모집단에서는 게이트를 열 근거가 없다"** 는 말이다.
2. **임계 두 개는 이 측정으로 옮길 수 없다.** `min_hr_ratio`는 §5-3의 신뢰구간 때문에, `min_cluster_gain`은 §5-2의 규모 의존성 때문에 그렇다. 후자에서 먼저 정해야 하는 것은 **눈금이 아니라 기준 모집단 크기**다 — 눈금 셋이 10만에서 0.86~0.92로 모이는데 같은 눈금이 2만에서는 1.28을 준다.
3. **1-8의 눈금은 어느 쪽으로도 쓰면 안 된다.** 포화해서 자기 무작위 기저보다 낮은 값을 낸다(§5-1). 계약 §11과 레지스터에 남아 있는 그 눈금의 수치는 §10에서 정정 대상으로 올렸다.
4. **이 절 전체가 생성기 파라미터에 대한 사실이다.** ALS 구현이나 임계에 대한 사실이 아니다. 실측이 §2를 갈아 끼우기 전까지 여기서 나온 값으로 배포 판단을 하면 안 된다 — §8-1의 두 줄이 그 이유로 "정할 수 없다"에 남아 있다.

---

## 6. 지연 시간 — 재설계 §6-3

### 먼저: 이 머신은 절대값을 내지 못한다

같은 코드·같은 모집단·같은 요청으로 **다섯 번** 쟀고, 타입 ③ p50이 **126~197 ms**(스냅샷 없음)와 **69~144 ms**(있음) 사이에서 움직였다. 1.6배와 2.1배다. 중간에 컴포즈의 worker·api·rabbitmq를 내려 보기도 했는데 오히려 느려졌으므로, 원인은 그 컨테이너가 아니라 노트북 자체다(바닥 load 4~5). **그래서 아래 절대값은 "서비스의 지연 시간"이 아니다.**

### 6-1. IDF 분모를 요청마다 세는 비용 — 1-9가 요구한 항목

**무엇을 물었나.** R59는 IDF의 분모(모집단 크기)를 주기 작업의 스냅샷으로 옮겼다. 그 전에는 요청마다 셌다. **그 항이 요청의 얼마를 차지했나?**

**어떻게 쟀나.** `population_stats`에 스냅샷이 없으면 `storage/content.py`가 모집단을 그 자리에서 센다. 같은 요청을 **스냅샷 없이 한 번, 있고 한 번** 쟀다(타입 ③ p50, ms).

| 환경 | 스냅샷 없음 | 있음 | 차이 |
|---|---|---|---|
| 스택 그대로 | 139.4 | 77.4 | 62.0 |
| 〃 | 125.5 | 69.2 | 56.3 |
| worker·api·rabbitmq 내림 | 190.6 | 143.2 | 47.4 |
| 〃 | 196.6 | 140.6 | 56.0 |
| 〃 | 194.5 | 143.9 | 50.6 |

**읽는 법.** 느린 쪽을 분모로 잡으면 25~45%지만, **읽어야 할 기준은 배포된 서비스의 p50**이다. 스냅샷이 있는 쪽(69~144 ms)에 47~62 ms가 더 붙는 것이므로, 그 집계는 **타입 ③ 요청을 거의 두 배로 만든다.**

**대조군이 이 결론을 지탱한다.** `by_topic`은 content 소스를 쓰지 않으므로 스냅샷과 무관해야 하는데, 다섯 런 모두 8% 안에 있고 **스냅샷 덕에 빨라진 적이 한 번도 없다**(49.1→53.2, 42.6→43.8, 54.8→57.5, 51.6→55.1, 56.0→58.9). 변화가 정확히 그 소스에만 왔다는 뜻이다.

**그래서.** 레지스터의 `content.population_refresh_minutes` 설명은 "로그로 들어가는 항이라 관대하다"고 하는데, 그건 **정확도**에 대한 말이다. **비용은 관대하지 않다.** 이 잡이 멈추면 타입 ③이 두 배로 느려진다.

**주의 하나**: 스냅샷 없는 쪽이 항상 먼저 돌아 PostgreSQL 캐시를 데운다. 대조군이 완화하지만 제거하지는 않으므로, 이 차이는 **하한**으로 읽는 편이 안전하다.

### 6-2. 어디서 시간이 가나

한 런의 단계 분해(스택 그대로, 스냅샷 있음):

```
kind       conc    n failed  wall p50 wall p95   assemble      query       rank    sources      total
for_you       1  200      0      77.4    168.0       10.0        2.0        5.0       61.0       77.0
by_topic      1  200      0      53.2     66.2       12.0        0.0        1.0       41.0       53.0
for_you      10  200      0     166.5    297.2       27.0       14.0        2.0      115.0      166.0
by_topic     10  200      0      68.6     91.5       16.0        0.0        0.0       51.0       69.0
for_you      50  200      0     869.5   1274.0      142.0      128.0        2.0      526.0      869.0
by_topic     50  200      0     328.4    491.5      141.0        0.0        0.0      185.0      328.0
```

- **`sources`가 지배한다** — total의 **56~79%**. 시간은 인덱스와 저장소 읽기에서 간다.
- **`rank`는 0~5 ms**다. 랭커는 논점이 아니고, 가중치를 더하는 것의 비용도 아니다.
- **동시성에 거의 선형으로 는다.** 이 측정은 **한 프로세스·한 이벤트 루프·SQLAlchemy 기본 풀(5+10)**이다. 프로덕션은 파드당 gunicorn 1 프로세스에 HPA(dev 1~3 / prod 2~5)라 그림이 다르다. 여기서 읽을 것은 "한 프로세스가 커넥션 15개로 동시 50을 받으면 큐가 선다"이고, `requests/infra.md` §1-2의 커넥션 산술과 같은 이야기다.
- `by_topic`의 `query`가 0인 것은 §2에 적은 대체 때문이다.

**그래서.** 지연 시간을 줄일 자리는 **랭커가 아니라 읽기**다. 가중치를 하나 더 더하는 비용은 측정에 나타나지 않고(`rank` 0~5 ms), 시간의 56~79%가 인덱스와 저장소에서 간다. 그리고 **동시성 50의 숫자는 "서비스가 그렇다"가 아니라 "한 프로세스가 커넥션 15개로 그렇다"** 이다 — 배포 형태가 다르므로, 이 줄에서 이어질 이야기는 튜닝이 아니라 `requests/infra.md` §1-2의 커넥션·레플리카 산술이다.

### 6-3. 타입 ①은 재지 않았다

첫 단계가 실제 LLM 호출이고 스펙 §3-6이 그 측정을 따로 가른다(200건은 모델을 통과시키고 나머지 1,800건은 topic id 직투입). e3llm-api가 필요한 그 측정은 이 문서에 없다.

### 6-4. 절대값을 내려면

조용한 전용 머신이거나 클러스터 안에서 재야 한다. 지금 형태로 다시 돌린다면 **한 번의 값이 아니라 반복의 폭**으로 읽어야 하고, 위 범위 안이면 회귀가 아니다.

---

## 7. 측정 도구와 환경의 결함 — 숫자를 믿기 전에 고친 것

**다음 사람이 이 표를 다시 돌릴 때 무엇을 의심해야 하는지가 표만큼 중요하다.** 넷이 나왔고 셋은 측정 쪽, 하나는 프로덕션이다. 전부 "숫자가 나왔다"에서 멈췄으면 그대로 이 문서에 들어갔을 것들이다.

1. **요청자 topic stand-in이 요청마다 전체 배열을 선형 스캔했다.** 10만 명이면 백만 원소 파이썬 루프가 파이프라인이 재고 있는 단계 안에서 돈다. 첫 측정에서 타입 ② p50 90 ms 중 **48 ms가 서비스가 아니라 stand-in**이었다. 소유자별로 한 번 색인해 고쳤다(→ 15 ms). **네트워크 호출의 stand-in은 그 호출보다 빨라야 한다. 아니면 재는 것은 stand-in이다.**

2. **모집단 스냅샷 없이 쟀다.** 시드는 `population_stats`를 비우고 다시 만들지 않으므로, 측정이 R59 이전의 비싼 경로를 돌고 있었다 — 아무도 배포하지 않는 서비스를, 실제보다 느리게. 고치는 김에 두 번 재도록 만들었고 그것이 §6-1이다. **1-9가 요구한 항목이 결함을 고치다가 측정됐다.**

3. **게이트의 로그가 대화 단위였는데 저장소는 방 단위로 접는다.** `agent_dm_room_id`는 `(요청자, 소유자 agent)`로 정해지는 함수라 **한 쌍의 대화는 전부 같은 방**이고, `interactions`는 방마다 한 행이다(turn 수는 `room_turns`에 있다). 시드는 turn을 합쳐 그 한 행을 만든다. 저장소가 `1 + α·ln(1 + Σt)`를 갖는 동안 측정 도구는 `k + α·Σln(1+t)`를 만들고 있었다. **`hr_ratio`가 CF에 유리한 쪽으로 약 0.1 틀어졌고**, 같은 문서의 두 표(`quality`와 `gate`)가 서로 다른 행렬을 보고 있었다. docstring은 "라이브 테스트가 저장소에 대고 확인한다"고 적고 있었는데 그런 테스트가 없었다. 지금은 실제로 적재해 `CfRepository.training()`과 쌍-가중까지 대조한다.

4. **머신이 조용하지 않고, 조용하게 만들 수도 없다.** §6 첫머리 그대로다. 도구가 아니라 환경이지만 성격은 같다 — **숫자가 나왔다고 그것이 서비스에 대한 숫자인 것은 아니다.**

### 7-1. 프로덕션 결함 하나 (코드 repo `a229fc9`)

`storage/cf.py`의 `_training`에 `ORDER BY`가 없고 `index_of`가 처음 나온 순서로 번호를 매기는데, `implicit`은 초기 factor를 시드된 생성기에서 **행마다** 뽑는다. 그래서 **같은 로그가 읽히는 순서에 따라 다른 모델로 적합됐다** — 셔플 후 한 유저의 top-10 중 셋이 바뀌었다(실측).

닿는 범위가 검증 표만이 아니다. 두 파드가 같은 테이블에서 서로 다른 pool을 쓰고, 게이트의 `hr_ratio`·`cluster_gain`이 데이터 변화 없이 흔들리며, 게이트는 아무도 요청하지 않아도 `w_cf`를 움직인다(R35).

정렬을 `Index`가 아니라 `index_of`에 뒀다. `Index`는 준 순서를 지켜야 한다 — 스윕이 한 사이클의 유저를 그것으로 번호 매기고 pool을 **위치로** 읽어 내기 때문이다. `Index`에서 정렬하면 사이클의 모든 pool이 **남의 대화**로 만들어지고, 자기 제외는 id로 가니 불변식 6만 멀쩡해서 더 안 보인다. 그걸 잡으라고 있던 테스트는 유저 **한 명**만 스윕하고 있어서 못 잡았다.

남은 잔여 하나는 문서에만 적었다: `rows_of`가 중복 쌍을 로그 순서로 더하는데 float32 덧셈은 결합법칙이 없어 마지막 비트가 달라질 수 있다. 랭킹이 읽는 것보다 한참 아래고, 닫으려면 로그 전체를 정렬해야 한다.

---

## 8. 설정값 — 1-9가 정한 것과 정할 수 없는 것

### 8-1. 정할 수 없다고 판단한 것

| 값 | 왜 |
|---|---|
| `gate.min_hr_ratio` 1.0 | 잰 모든 조건에서 최대 0.605다. 낮춰서 열리게 하는 것은 R35가 막으려는 그 일이고, 그대로 두는 것도 §4의 이유로 근거가 약하다. **스펙 §2를 실측으로 갈아 끼우는 것이 먼저다.** |
| `gate.min_cluster_gain` 1.5 | §5-2 — 눈금이 아니라 **기준 모집단 크기**를 먼저 정해야 한다. 2만에서 1.28, 10만에서 0.92다. |
| `topic_api.timeout_ms` 3000 | 측정이 topic-api를 인메모리 stand-in으로 대체하므로 이 호출이 어느 런에도 없다. 말할 수 있는 것은 **나머지가 쓰는 예산**뿐이고 그것도 동시성에 따라 크게 다르다 — 타입 ③ 전체가 동시성 1에서 77 ms, 10에서 167 ms, 50에서 870 ms다. 800 ms로 조이는 판단은 **실서비스 topic-api의 지연 시간 분포**가 있어야 한다. **그대로 둔다.** |
| `rank.recency_half_life_days` 30 | 생성기는 holding의 `updated_at`을 **소유자의 생존 구간 안 임의 시각**으로 놓고 `close_rate`만큼만 뒤로 옮긴다. "사람들이 자기 topic을 얼마나 자주 다시 손보나"에 대한 모델이 아예 없다. 그래서 여기서 반감기를 재면 그 임의 선택을 되재는 일이 된다. |

### 8-2. 측정 근거가 생긴 것

| 값 | 근거 |
|---|---|
| `rank.for_you.w_content` 0.4 | §3 — content가 세 소스 중 가장 개인화됐다(군집 라벨 눈금에서 인기도의 4.7배). **크기가 커야 한다는 방향**을 지지하고, 0.4라는 값 자체를 지지하지는 않는다. |
| `rank.for_you.w_cf` 0.0 | §5 — 잰 모든 조건에서 게이트가 닫힌다. 0에서 시작한다는 R35의 판단이 측정으로 지지된다. |
| `cf.factors` 16 | §3-1의 격자에서 16f가 64f보다 전부 나았다. 이 측정이 다시 재지는 않았다. |
| `cf.regularization` 0.5 | **근거가 약하다.** §3-1의 격자에서 0.5는 16f와만, 0.05/5는 64f와만 붙어 있어 정규화 자체가 분리되지 않는다. 재측정 대상. |

---

## 9. 못 잰 것

| 항목 | 왜 |
|---|---|
| 100만 규모 | 이 노트북에서 친구 쌍 3,700만·목 서버 픽스처 9 GB다. 10만을 재고 외삽한다(오너 결정, 2026-09-14). 증가율의 두 점은 2만·10만이 있다 — §5-2가 그 두 점이다 |
| ~~서비스 응답의 필터 불변식~~ | **§11-1에서 쟀다** — 10만에서 위반 0건. 남는 것 셋: **재조회 지연 시간**(정적 적재에는 이벤트 도착부터 row 삭제까지의 창이 없어서, 아래 이벤트 리플레이 경로의 몫이다), **탈퇴 행을 읽기 필터로 증명하는 주입**(비공개 전환만 주입했다), **타입 ①의 텍스트→topic 단계**(grounding 뒤부터 쟀다) |
| 오라클의 HR 상한 | 이 생성기에서 재지 않았다. §3-1의 약 0.02는 **은퇴한 생성기**의 값이라 이 표의 `hr_cf` 0.0065를 그것에 대고 읽으면 안 된다 |
| 타입 ① 지연 시간·정확도 | 첫 단계가 LLM이고 스펙 §3-6이 측정을 가른다 |
| 타입 ② 순서 일치율 | 지표 함수(`order_agreement`)는 있고 채점기에 연결하지 않았다 |
| §6-4 민감도 표 | `opener_rate`·`affinity` 노이즈·`off_cluster_topic_rate` ±50%. `--params` 파일 변형으로 돌아간다 |
| 장애 주입 3종 | CF 워커 중단 / `cf_candidates` 비어 있음 / 이벤트 유입 중단. §6-1의 "스냅샷 없음"이 넷째 종류를 우연히 잰 셈이다 |
| 이벤트 리플레이 경로 | 직접 적재만 썼다. 두 경로 일치는 1-6이 확인했는데 **150명 규모**였고, 시각 컬럼은 빼고 `interactions`는 의도적으로 927 대 0이었다(스펙 §6-1) |
| 도쿄 월 단가 | 용량은 §2에 있다. 단가 환산은 인프라 답을 받은 뒤 |

---

## 10. 이 측정으로 정정되는 문서

1. **`agent_discovery_contract.md` §11 / `agent_discovery_settings.md` §4** — 기록된 "10만 실측 인기도 0.345 / ALS 0.249"는 **`private`까지 넣은 눈금**의 값으로 보이고, 그 눈금은 자기 무작위 기저(0.298)보다 낮아 **포화한다**(§5-1). 그리고 §11이 걱정한 "눈금이 다르다"는 비율에서는 문제가 아니다 — 문제는 모집단 크기다(§5-2).
2. **`library_verification.md` §3-1** — "이 스파이크의 생성기로 잰 값"이라는 주석은 이미 있다. 더할 것은 **`cluster_gain` 2.18 → 0.918**이고, **떨어진 쪽은 ALS뿐이며 인기도는 올랐다**는 것이다(§5-4).
3. **`implementation_plan.md` §1-8의 완료 조건** — 미충족이라는 것은 이미 적혀 있다. 더할 것은 **이 샘플로는 충족 여부를 판별할 수 없다**는 것이다(§5-3).
4. **`synthetic_population_spec.md` §7의 "정답의 선명도"** — §4의 숫자(0.765 / 122배 대 4.6배)로 항목을 갱신하고, 셋 중 어느 길로 갈지를 오너 결정으로 올린다.
5. **`agent_discovery_redesign.md` §6-2** — "agent visibility: 규칙 그대로 + 소량 예외"는 R57로 표현 불가능해졌다(스펙 §2는 이미 "예외 없음"으로 고쳐져 있다). 이 측정과 무관하게 낡은 줄이다.

---

## 11. 항목 16 — 1-9 잔여 측정

§9가 "못 잰 것"으로 남긴 항목들을 하나씩 지우는 절이다. 측정 조건(§2)과 모집단은 위와 같고, 날짜와 코드만 다르다.

### 11-1. 서빙 응답의 필터 불변식 (2026-09-14, 코드 `5510a56`)

#### 무엇을 물었나

스펙 §4가 **0건이라고 못 박은 것들**이다. 위의 §3~§6이 "답이 얼마나 맞았나"를 물었다면 이건 **"답이 담아서는 안 되는 것을 담았나"** 를 묻는다. 성격이 다르다 — recall 0.31은 판단의 근거지만, 친구 아닌 사람에게 나간 friends 전용 row는 **결함**이다. 그래서 평균이 아니라 **세고**, 하나라도 있으면 명령이 실패로 끝난다.

| 스펙 §4 | 이 표의 열 |
|---|---|
| **필터** 행 — friends tier row가 친구 아닌 요청자에게 나온 건수 | `friends` |
| **비공개 전환** 행 — `close_rate` 뒤 그 row가 결과에 나온 건수 | `private`, 그리고 아래 드리프트 |
| **탈퇴** 행 — 탈퇴 유저의 agent가 결과에 나온 건수 | `departed` |
| 행이 아니라 **타입 ①의 지표** — 상위 k에서 커버 수가 역전된 쌍 0건 | `invert` |

마지막 것은 필터가 아니라 **순서**에 대한 말이고, §4는 그걸 타입 ① 행의 지표 칸에 적어 뒀다. 성격이 다르지만 "0이어야 한다"는 같아서 같은 표에 뒀다.

여기에 스펙이 적지 않은 넷을 더 셌다.

- **`self`** — 불변식 6(자기 자신 추천). **다른 어떤 열도 알아채지 못하는 조건**이다: `storage/visibility._eligible`에서 `owner <> requester`를 떼도 나머지 열은 전부 0으로 남는다. 아무도 자기 친구가 아니고, 공개 row가 있는 사람은 자기 자신에게 보이기 때문이다.
- **`hidden`**(`nothing_visible`) — 가시성 규칙을 row가 아니라 **소유자 단위**로 본 것이고, 어겨진 규칙은 재조회가 강제하는 불변식 4다. 카드가 아무 row도 지목하지 않을 때 **`friends`와 짝을 이루는 둘 중 움직일 수 있는 쪽**이다 — 인기도만으로 뽑힌 카드는 row를 지목하지 않으면서도 "이 소유자는 이 요청자에게 보여도 된다"고 주장한다.
- **`repeat`** — 구조적 가드. 지금 코드 경로로는 발생할 수 없고, `evaluation/metrics.py`가 중복 없음을 전제하기 때문에 센다.
- **`unheld`** — 적재 경로 가드. 모집단에 기록이 없는 row를 카드가 지목했다는 뜻이다.

#### 어떻게 쟀나

**랭킹이 아니라 서빙된 카드를 센다.** §3의 품질 표는 결정 로그의 `ranked`를 채점한다 — 스펙 §4가 "섞기 전 순서"(R20)로 정확도를 재라고 하기 때문이고, 그 목록은 마지막 재조회 **전**에 만들어진다. 필터 불변식은 정확히 반대쪽 끝이다: 불변식 4는 어셈블리의 재조회 안에 있고, 그래서 세는 것은 `answer.agents` — **호출자가 실제로 건네받았을 카드**다.

**저장소가 아니라 생성기와 대조한다.** 나머지 측정은 서비스의 한 부분을 다른 부분과 비교하지만, 이건 서빙된 것을 모집단이 참이라고 말하는 것과 비교한다. 적재기와 읽기가 **같이** 틀린 경우가 드러나는 유일한 자리다.

**세 요청 타입 전부.** 타입 ③은 인기도 테이블·콘텐츠 쿼리·CF pool을 거치고, 타입 ②는 섹션마다 topic 인덱스를, 타입 ①은 같은 인덱스를 질문이 해석된 topic으로 거친다. 읽기가 셋이니 유출 자리도 셋이다. 다만 **타입 ①은 grounding 뒤부터 돈다** — 스펙 §3-6이 그 단계를 따로 떼라고 했고, 질문이 뜻하는 topic은 생성기 파일에 이미 있다. 그래서 텍스트→topic 단계의 유출 면은 여기에 없다.

**0이 증거가 되도록 분모를 붙였다.** friends 전용 row가 없는 모집단에서 세면 전부 새는 서비스도 0을 낸다. `withheld` 열은 **이 요청의 읽기가 거절해야 했던 friends 전용 row 수**이고, 요청 모양마다 거절하는 자리가 달라서 정의가 둘이다.

- **토픽 쿼리(①·②)** 는 후보 읽기에서 거절한다 → "그 토픽을 친구에게만 공개한 사람 중 요청자의 친구가 아닌 수". 서빙 여부와 무관하다.
- **타입 ③** 은 재조회가 제안된 소유자의 **모든** row를 읽는다 → "서빙된 소유자의 friends 전용 row 수", 토픽 제한 없이.

**토픽으로 제한한 정의를 타입 ②에 쓰면 읽기가 어떻게 동작하든 구조적으로 0이 된다.** 토픽 T로 서빙된 소유자는 T를 그 요청자에게 공개한 사람이고, 생성기는 `(소유자, 토픽)`마다 tier 하나만 쓰므로 T의 friends 전용 쌍둥이 row가 존재할 수 없다. 처음에 그 정의로 쟀다가 `by_topic`이 0으로 나와서 알았다.

**이 수는 저장소의 정확한 거절 수가 아니라 바닥값이다.** 생성기 배열에서 세므로 탈퇴자(이 런에서 1,057명)의 friends row를 포함하는데 적재기는 그걸 쓴 적이 없고, 반대로 요청자 자신의 row는 빼는데 읽기는 `owner <> requester`로 그것도 거절한다. 1,500명에서 저장소 대비 +0.6%다. "거절할 것이 있었나"의 분모로는 충분하고, 정확하다고 하면 산술이 뒷받침하지 않는 주장이 된다.

요청 하나로는 확인할 수 없는 사실 셋은 모집단 단위로 따로 확인하고, 없으면 스택을 열기 전에 exit 2로 거절한다. 이 런에서는 **탈퇴자 1,057명, friends 전용 공개자 3,110명, 다중 토픽 쿼리 966건**이다. 마지막 것이 없으면 모든 커버 수가 1이라 `invert`는 산술적으로 0이다.

#### 숫자

10만·λ=10, run `7f468673de7f-20260908`, 728,292 interactions 위에 98,938개 pool.

가운데 여덟 열(`friends`~`invert`)이 **0이어야 하는 것들**이고, 맨 오른쪽 `withheld`는 **0이면 안 되는 것**이다.

| kind | answers | cards | named | friends | self | hidden | departed | private | unheld | repeat | invert | withheld |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| `for_you:popularity` | 505 | 5,050 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | — | 6,010 |
| `for_you:content` | 505 | 5,040 | 15,654 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | — | 20,910 |
| `for_you:cf` | 505 | 5,000 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | — | 6,802 |
| `for_you:combined` | 505 | 5,050 | 9,991 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | — | 16,284 |
| `by_topic` | 505 | 14,785 | 14,785 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | — | 678,505 |
| `explicit` | 2,000 | 20,000 | 27,721 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 1,752,567 |
| **합계** | **4,525** | **54,925** | **68,151** | **0** | **0** | **0** | **0** | **0** | **0** | **0** | **0** | **2,481,078** |

`invert`의 `—` 는 그 행에 커버 수라는 것이 없다는 뜻이다. 타입 ③은 토픽을 요청받은 적이 없고, 타입 ②는 섹션마다 토픽 **하나**만 물으므로 모두의 커버 수가 1이라 역전될 쌍이 성립하지 않는다. 스펙 §4가 이 지표를 타입 ①에만 붙인 이유이기도 하다.

**위반 0건.** 분모는 **248만 건**이고, 이것이 "셀 것이 없어서 0"이 아니라는 근거다 — 읽기가 그만큼의 friends 전용 row를 실제로 마주쳤고 하나도 내주지 않았다.

#### 비공개 전환은 따로 만들어서 쟀다

`private` 열이 0인 것은 **읽기에 대한 사실이 아니다.** 갓 시드된 저장소는 소유자가 공개한 것만 담으므로, 비공개 row는 애초에 읽힐 자리에 없다. 그 열이 잡을 수 있는 건 적재기뿐이다.

스펙 §4가 실제로 묻는 것은 **"후보가 뽑힌 뒤 비공개가 된 row"** 이고, 그건 정적 적재에 존재하지 않는 시간 창이다. 그래서 만들었다: 서빙된 소유자 50명을 저장소에서 실제로 비공개 전환하고(row 삭제 + `discoverable=false`, R57이 허용하는 유일한 상태) 그들을 본 요청자들에게 다시 묻는다.

```
50명 비공개 전환 → 전환 전 그들이 있던 카드 2,341장 / 재조회가 떨어뜨린 후보 704건 / 전환 뒤 나온 카드 0장
```

**704가 판정의 분모다.** row가 사라지면 인기도 읽기와 콘텐츠 읽기는 그 소유자를 **아예 지목하지 않는다** — 여전히 지목하는 건 미리 계산된 CF pool뿐이고, 그게 정확히 불변식 4가 존재하는 이유다("스냅샷은 노출을 승인하지 않는다"). 2,341은 "전환 전 그들이 얼마나 자주 나왔나"일 뿐 재조회가 한 일이 아니다.

이 숫자는 결정 로그의 `recheck_dropped`에서 읽는데, **그 값은 재조회가 떨어뜨린 후보 전부이지 전환된 50명만이 아니다.** 이 런에서 둘이 같다고 보는 근거는 "그 사이에 저장소에서 바뀐 것이 그 50명뿐"이라는 것이고, 전환 전 기준선을 따로 찍지는 않았다. 변이 검증의 22 ↔ 22 대칭(아래)이 간접 근거다.

**재조회 지연 시간은 이 하네스로 못 잰다.** 스펙 §4는 "재조회 지연 안은 허용, 지연 시간 기록"이라고 하는데, 여기서 재조회는 요청마다 저장소를 읽으므로 지연 창이 0이다. 실제 창은 이벤트가 도착해 row가 지워지기까지의 시간이고, 그건 이벤트 리플레이 경로(스펙 §6-1, §9에 여전히 남아 있다)의 몫이다.

#### 이 0들을 믿어도 되나 — 변이 검증

카운터가 죽어 있는 것과 서비스가 새지 않는 것은 **같은 표를 낸다.** 그래서 서비스를 한 줄씩 고장 내고 어느 열이 움직이는지 봤다(1,500명 규모. 숫자는 `content` / `combined` / `by_topic` / `explicit` **행** 순서다):

| 무엇을 고장 냈나 | 움직인 열 |
|---|---|
| `storage/visibility._eligible`의 친구 조건 → `true()` | `friends` **182 / 197 / 192 / 102**, `hidden` 1 / 0 / 5 / 3, `invert` **368**(`explicit`) |
| 같은 함수에서 `owner <> requester` 제거 | `self` **14 / 13 / 17 / 1** |
| `ForYouAssembler`의 재조회 제거 | 위 표는 **전혀 움직이지 않고**, 드리프트만 뒤집힌다 — 1,500명에서 정상은 "22건 떨어뜨림 / 0장 서빙", 변이는 **"0건 떨어뜨림 / 22장 서빙"**. 재조회가 떨어뜨리던 22건이 그대로 노출로 넘어간다 |

`popularity`와 `cf` 행은 어느 변이에도 움직이지 않는다. **그건 그 행의 모양이지 빈틈이 아니다.** 두 소스 모두 `topic_id` 없는 hit를 내므로 그 행의 카드는 row를 지목하지 않고(`named` 0), row 단위 열은 애초에 움직일 자리가 없다. 소유자 단위 열이 안 움직이는 이유는 행마다 다르다 — `popularity` 행에서는 새로 노출된 소유자가 인기도만으로 열 명을 제쳐야 페이지에 닿고, `cf` 행은 **살아 있는 소스가 아예 없다**(후보가 미리 계산된 pool뿐이라 가시성 조건을 고쳐도 pool이 달라지지 않는다).

세 번째 줄이 이 절에서 가장 중요하다. **정상 표는 재조회가 망가져도 아무 말을 하지 않는다.** 정적 저장소에서는 재조회가 떨어뜨릴 것이 없기 때문이다. 드리프트 패스가 없으면 불변식 4는 10만에서 여전히 측정되지 않은 채다.

#### 그래서

1. **가시성 설계가 10만에서 성립한다.** 이 시스템의 필터는 지금까지 단위 테스트와 1,500명짜리 라이브 테스트에서만 확인됐고, 그 라이브 테스트가 보던 것은 불변식 6과 R28로 여기서 세는 넷과 **다른 것**이었다. 규모에서 유출이 0이라는 진술은 이번이 처음이다.
2. **이 문서의 설정값은 하나도 바뀌지 않는다.** 불변식은 조절할 값이 아니라 참이거나 거짓인 성질이다. §8의 두 표는 그대로다.
3. **얻은 것은 숫자가 아니라 도구다.** `cli validate invariants`는 임계를 판정하는 게이트가 아니라 **검사**이고, 위반이 있으면 1, 셀 것이 없으면 2로 끝난다. 가시성 조건·재조회·적재기를 건드리는 변경 앞에서 다시 돌릴 수 있다.
4. **다만 여덟 열이 같은 강도로 검증되지는 않았다.** 변이로 발생시켜 본 것은 넷이고, 나머지 넷이 0인 것은 "서비스가 막았다"보다 **"그 상태가 저장소에 존재할 수 없다"** 에 가깝다. 아래에 그대로 적는다.

#### 못 잰 것

- **`private`·`unheld`·`departed` 세 열은 읽기 결함 하나로는 발생할 수 없다.** 탈퇴자는 row도 `agents` row도 없어서, 그 테이블을 조인하는 읽기들이 애초에 이름 댈 수 없다 — `storage/popularity.popular_owners`에서 `has_visible_row`를 떼도 셋 중 아무것도 움직이지 않았다. (같은 변이에서 **안 보이는** 소유자를 막는 것은 그 조인이 아니라 `ForYouAssembler`의 재조회다. 방어가 두 계층이다.) 세 열은 여기서 **적재 경로 가드**이고, 그렇게 적혀 있다.
- **탈퇴 행은 세었지만 읽기 필터로 증명되지는 않았다.** 비공개 전환은 드리프트로 증명했고, 탈퇴는 그에 해당하는 주입을 하지 않았다.
- **타입 ①의 텍스트→topic 단계**는 위에 적은 대로 이 측정 밖이다.
- 100만 규모는 여전히 안 쟀다(§9).
