# 추천 시스템 서빙 — 피드백 루프와 오픈소스 recsys 도입 논의 기록 (rev 1, 2026-08-27)

> **문서 지위: 결정이 아니다.** 2026-08-27 대화에서 나온 구조 방향(topic-api를 토픽 조회로 좁히고,
> 대화 이벤트를 피드백으로 받아 추천 시스템 자체가 학습하는 그림)과, 그 판단에 필요한 **검증된 사실**,
> 그리고 **오픈소스 recsys 도입 평가**를 임시로 모아 둔 것이다. 어떤 항목도 확정이 아니고 어떤 코드도
> 이 문서를 근거로 바뀌지 않았다. **설계 정본은 `recommendation_pipeline_design.md`**이며, 이 문서가
> 정본과 어긋나면 정본이 맞다. 결정이 생기면 그때 정본 §S5·§S2·§9(열린 결정)로 올리고 이 문서는 그
> 결정을 가리키는 기록으로 남긴다.
>
> **표기**: `사실`(소스에서 `file:line`으로 확인) · `판단`(이 문서의 의견) · `열린 항목`(협의·측정 대기).
> 사실과 판단을 섞어 쓰지 않는다 — 이 문서의 유일한 지속 가치는 앞의 것이다.
>
> **자매 문서**
> - `facets_ownership_split_discussion.md`(rev 3) — holder 인덱스와 `score` 소유를 producer로 옮기는
>   구조. 이 문서 §1의 "topic-api는 토픽만 찾는다"가 성립하면 **그쪽 논의의 답이 선결 조건이 된다**(§6-2).
> - `service_boundary_discussion.md`(rev 2) — "자연어+맥락 → 토픽"이 어느 쪽 역할인가. 이 문서 §3-1의
>   토픽 축 학습이 성립하면 **그쪽 SB1의 답이 정해진다**(우리 쪽, §6-3).
>
> 기준 커밋: `bourbon-agent-discovery-api` `d82cd04` · `bourbon-topic-api` `9ee67f3`.

---

## §1 논의된 그림

2026-08-27 대화에서 제시된 방향. 네 조각이다.

1. **topic-api의 역할을 토픽 조회로 좁힌다.** 보유자 랭킹은 topic-api의 일이 아니게 된다.
2. **대화 이벤트를 피드백으로 받는다.** 추천된 에이전트와 사용자가 대화를 시작했는지, 얼마나 많이
   했는지, 맥락상 만족했을지.
3. **사용자 persona(md)를 외부에서 받는다.**
4. **협업 신호를 넣는다.** "이런 사용자가 이 토픽에 긍정적이었다면, 비슷한 사용자가 같은 토픽을
   요청했을 때 비슷하게 추천한다."

이 문서가 답하려는 질문은 둘이다. ⑴ 이 그림에서 무엇이 지금 정해져야 하고 무엇이 나중 항목인가(§3·§4).
⑵ 오랫동안 개발되어온 오픈소스 recsys를 도입하는 것이 여기에 맞는가(§5).

---

## §2 사실 — 소스에서 확인한 것

이 절이 이 문서의 본체다. 판단이 바뀌어도 여기는 남는다.

### 2-1 `사실` 학습된 순위가 들어갈 자리는 이미 비어 있다

`agent_discovery/stages/ordering.py`의 `OrderingStrategy`는 Protocol이고(`:21-29`), 기본 구현
`RrfOrdering`은 fused score만 읽는다(`:32-40`). `order()`가 전략을 인자로 받는다(`:44`).

모듈 docstring이 그 이유를 적어 뒀다(`:3-4`) — *"The primary key is a slot, because the key this
service actually wants -- an expertise ordering read out of topic-api's facets -- cannot be written
yet."* 그리고 tiebreak는 슬롯이 아니라 `order()`가 직접 붙인다(`:6-8`, `:54`): 전략이 잊을 수 없게.

→ **holder 축의 학습은 새 아키텍처가 아니다.** 비어 있는 슬롯에 공급처가 생기는 것이다.

### 2-2 `사실` "preference 신호는 tiebreak only"는 이미 등재된 선례다

정본 §1 규율 표(`recommendation_pipeline_design.md:64`):
`즐겨찾기 = tiebreak only, gate 우회 금지 | S5 — 단 dormant 슬롯: 현 파이프라인에 즐겨찾기 데이터
공급처가 없다. provider가 생길 때까지 이 키는 비활성`. §S5의 정렬 키 사다리(`:392-402`)에도 2키로
들어가 있고 `**dormant**`로 표시돼 있다.

→ engagement 피드백은 **새로운 종류의 신호가 아니다.** 즐겨찾기와 같은 부류(personalization,
expertise 아님)이고, 따라서 같은 자리에 같은 제약으로 들어가야 한다.

### 2-3 `사실` popularity prior 금지는 잠긴 규율이다

정본 `:66` — `popularity prior 금지 | S5 (RRF는 topic별 순위 융합 — 전역 인기 항 없음)`.
현재 RRF가 이 규율을 만족하는 근거가 "전역 인기 항이 없다"로 명시돼 있다.

### 2-4 `사실` 피드백/상호작용 로그는 어디에도 없고, 노출은 집계로만 남는다

`bourbon-agent-discovery-api` 전체에서 `rec_signal`·`rec-signal` grep 0건. 클릭·수락·거절·대화
어떤 이벤트도 기록되지 않는다.

노출 기록은 `pipelines/topic_recommend.py:68-72`의 `_RunSummary.to_log()`가 전부이고,
`ranked_holder_count`·`merged_user_count`·`excluded_user_count`·`ineligible_user_count`
— **전부 집계 수치다.** 어떤 후보가 몇 위였고 왜 잘렸는지는 남지 않는다.

→ 지금 서빙을 시작하면 **후보별 노출 기록 없이** 피드백이 쌓인다. §4-1이 이 사실 위에 있다.

### 2-5 `사실` 오프라인 점수 계산 / 온라인 서빙 분리는 이미 존재한다

`bourbon-topic-api`의 `score`·`score_detail`은 요청 경로에서 계산되지 않는다.
`topic/persona_topics/scoring.py:18`의 `topic_score(facets, confidence)`가 producing pipeline 쪽
함수이고, 저장 스키마 주석이 세 writer를 분리해 둔다(`topic/storage/user_topics.py:6-8`):
`user`(visibility·rank_key) / `extraction`(descriptions·blocks) / `injection`(score·score_detail).

→ recsys 패턴의 핵심 구조(오프라인 학습 → 온라인 서빙)는 **새로 도입할 것이 아니라 이미 있는 것**이다.
바뀌는 것은 그 오프라인 단계에 피드백이 들어가는지 여부다.

### 2-6 `사실` topic-api에 검색엔진도 벡터DB도 없다

`pyproject.toml` dependencies(`:5-26`)에 opensearch·elasticsearch·vespa·qdrant·milvus·faiss·
embedding 관련 항목이 하나도 없다. 저장은 DynamoDB(`aiobotocore`) 단독이고, top-k는
`topic/search/threshold.py`가 Fagin의 threshold rule을 직접 구현한 것이다(docstring이 *"the classic
threshold rule"*이라고 밝힌다).

### 2-7 `사실` 순위를 매길 후보 집합은 작다

`_MAX_GROUPS = 3`(`stages/expansion.py:36`) × `DEFAULT_USERS_LIMIT = 100`(= topic-api의 `_MAX_LIMIT`,
`providers/topic_api/client.py:45,55`) = **최대 300 후보**. `merge`가 중복을 접으므로 실제로는 더 적고,
`order()`가 `max_results`로 자른다.

→ **랭킹 쪽에는 규모 문제가 없다.** ANN도 feature store도 이 숫자에서는 해결할 대상이 없다.

### 2-8 `사실` 현재 rollup 랭킹은 유저 수가 늘면 결정론적으로 불완전해진다

`SEARCH_MAX_DESCENDANTS = 50` · `SEARCH_MAX_GSI_ITEMS = 20_000` · `SEARCH_MAX_USER_QUERIES = 2_000`
(`topic/config.py:139-142`), `page_size = limit × 4`(`_STREAM_PAGE_FACTOR` `topic/search/service.py:46`, 쓰이는 곳 `:233`).
읽지 않은 스트림의 bound는 ∞(`threshold.py` 설계), `_pick`이 ∞부터 고른다.

페이지 하나가 400명이고 전부 새 후보면 resolve 400건이므로 **5페이지에서 `max_resolves`가 소진되고,
50 스트림 중 45개는 bound가 ∞ → T=∞ → 증명 불가 → `exhaustive=False`**. 지금 동작하는 이유는 유저가
적어서 각 스트림이 첫 페이지에 드레인되기 때문(bound→0)이다. 우리 쪽은 이걸 fail-closed로 받는다
(`stages/retrieval.py:122`).

→ §1-1(topic-api를 조회로 좁힘)이 성립하면 **이 절벽은 고칠 대상이 아니라 없어질 대상이 된다.**
rollup·예산·resolve 단계가 전부 사라진다.

---

## §3 `판단` 학습 대상을 두 축으로 갈라야 한다

§1-2의 피드백은 `(요청자, 추천된 에이전트, 토픽, 결과)` 튜플이다. 여기서 학습 가능한 것이 둘인데
**안전성이 정반대다.** 하나로 묶어 "추천 품질 학습"이라고 부르면 위험한 쪽이 안전한 쪽의 정당성을
빌려 쓴다.

### 3-1 `판단` (A) 토픽 축 — 이견 없이 찬성

"이 사용자가 이렇게 물었을 때 어떤 토픽을 의도했나". 현재 S2(grounding·disambiguation)가 담당하고
LLM 콜을 쓰며 abstain도 나는 자리다. 피드백을 여기에 먹이면:

- 사람의 순위를 건드리지 않는다 → popularity prior가 들어올 통로가 **구조적으로** 없다.
- persona(§1-3)가 cold start를 푼다. 상호작용 0건이어도 persona 임베딩으로 user-user 유사도가 나오니
  §1-4의 "비슷한 사용자에게 비슷하게"가 첫날부터 동작한다. 축적되면 학습으로 전환한다.
- 실패 모드가 관측 가능하다: 잘못 ground하면 사용자가 같은 질문을 다시 한다(§4-2 표).

### 3-2 `판단` (B) 홀더 축 — 자기강화 루프가 사는 자리

"토픽 T에서 누구를 위로". 우리가 노출을 통제하므로 상위에 놓인 에이전트가 대화를 받고, 그래서 더
위로 가고, 더 받는다. pool이 유한하므로(§2-7: ≤300, 실제 반환은 `max_results`) **소수가 대화를 거의
전부 흡수하고, 실제로 더 잘 아는 사람과 무관하게 "그 분야 전문가"가 고정된다.**

전문성 추천에서 이것은 성능 저하가 아니라 **제품 주장이 거짓이 되는 것**이다. §2-3의 금지(정본
`:66`)가 막으려던 것이 정확히 이 상태다.

### 3-3 `판단` 불변식 — 학습은 순서만, 집합은 못 건드린다

> **학습된 요소는 이미 자격을 통과한 집합 안에서 순서만 바꾼다. 집합에 넣지는 못한다.**

§2-2의 즐겨찾기 선례와 같은 모양이고 같은 이유다. engagement는 expertise 신호가 아니라 preference
신호이므로, expertise gate를 통과한 **뒤에** 들어와야 한다. 이 불변식이 두 가지를 동시에 지킨다.

- **popularity prior 금지**(§2-3) — 순서만 바꾸면 인기 없는 자격자가 집합에서 빠지지 않는다.
- **귀속(attribution)** — 학습이 순서만 바꾸면 모든 결과가 여전히 보유 토픽으로 추적 가능하고
  `matched_topics[]`·`relation`·`owner_notes`가 그대로 성립한다. 학습이 집합을 고르면 "왜 이 사람인지"가
  사라지고, 그 순간 rev 5.8–5.12에서 wire에 올린 값들이 근거 없는 장식이 된다.

정본 §S5의 주 정렬 키(1키)는 facets 계약이 닫힐 때까지 계속 비어 있다. **학습이 그 자리를 채우려 하면
안 된다** — 그게 이 불변식의 실질이다.

---

## §4 `판단` 지금 정해야 하는 것 — 나중에 되돌릴 수 없는 것들

### 4-1 `열린 항목` propensity를 서빙 첫날부터 남긴다

추천한 것에 대해서만 피드백이 생기므로, 노출을 기록하지 않으면 **"보여주지 않았던 사람이 더 나았을까"를
영원히 알 수 없다.** 나중에 off-policy 보정(§5-1)을 하려면 그 시점에 데이터가 이미 있어야 한다.
§2-4가 이 항목의 근거다 — 현재 집계만 남는다.

필요한 것:

| 남길 것 | 이유 |
|---|---|
| 고려된 후보 **전체** (반환된 것만이 아니라) | 반환된 것만 남기면 분모가 없다 |
| 각 후보의 순위와 정렬 키 값 | 어떤 키가 그 순위를 만들었는지 재구성 가능해야 한다 |
| 잘린 이유 (gate / eligibility / stub / `max_results`) | 자격 미달과 순위 미달은 다른 사건이다 |
| ground된 토픽과 그 그룹 | (A) 축 라벨의 키 |

`판단` 이것은 **로그가 아니라 이벤트 스트림**이어야 한다. 후보별 user_id가 들어가므로 보존 정책이
붙는 데이터이고, 현재 structlog 규율(사용자 텍스트 금지·id는 남김)과는 다른 판단이 필요하다.

★ **이 그림에서 가장 싸고 가장 되돌릴 수 없는 항목이다.** 지금은 스키마 하나이고, 나중에는 못 만든다.

### 4-2 `판단` "대화를 얼마나 많이 했는지"를 positive label로 쓰면 안 된다

지식 제품에서는 **좋은 답이 대화를 끝낸다.** 정확한 답 한 번이면 짧게 끝나고, 못 알아들으면 길어진다.
대화량을 positive로 학습하면 **답을 효율적으로 하지 못하는 에이전트를 선호하도록** 학습된다. 콘텐츠
피드의 dwell time과 같은 함정이지만, 여기서는 부호가 더 선명하게 뒤집힌다 — 피드는 체류가 목적이고
Discovery는 해소가 목적이다.

대신 쓸 라벨:

| 신호 | 강도 | 근거 |
|---|---|---|
| 다른 토픽으로 **같은 에이전트에 재방문** | strong + | 슬롯 효과가 아니라 사용자 판단. 조작 어렵다 |
| 같은 토픽을 **다른 에이전트에게 다시 물어봄** | strong − | 첫 추천이 답을 못 했다는 뜻. 파생 비용 거의 0 |
| 명시적 dismiss | strong − | |
| 대화 시작 여부 | weak + | 슬롯 위치에 크게 오염됨 → §4-1 없이는 못 쓴다 |
| 대화량 | **label 아님, feature로만** | 위 이유 |

### 4-3 `판단` persona md는 비신뢰 입력이다

정본 §1 규율 표에 이미 `판정 입력 텍스트 전부 비신뢰 | S1·S2의 LLM 프롬프트`가 있다(`:67`).
persona md는 외부에서 받는 자유 텍스트이므로 **같은 부류**이고, 우리 프롬프트(expansion·
disambiguation)에 들어가는 순간 injection 책임이 우리로 온다.

`owner_notes`에서 합의한 "injection 완화는 bourbon-agent 책임"은 **우리가 내려주는 응답**에 대한 것이고,
**우리가 소비하는 입력**은 별건이다. rev 5.11 §9-⑨의 주의와 방향이 반대인 항목이다.

부수적으로 privacy: user-user 유사도를 persona로 계산하면 "비슷한 사용자가 좋아한 토픽"이 이웃에 대한
추론 채널이 된다. deferred 상태인 privacy gate가 여기서 두 번째 구체적 대상을 갖는다(첫 번째는
topic-api `friends` tier).

### 4-4 `판단` 부트스트랩 순서 — recsys가 첫 서빙 경로가 될 수 없다

학습 데이터를 얻으려면 먼저 서빙해야 한다. 따라서:

1. 현 파이프라인으로 서빙(real edge turn-on) + **§4-1을 첫날부터**
2. (A) 토픽 축 — persona로 cold start, 축적되면 학습으로 전환
3. (B) 홀더 축 — `OrderingStrategy` 슬롯에 공급처로 진입, 탐색 슬롯 1개 확보(§5-2)
4. expertise 주 정렬 키는 facets 계약이 닫힐 때까지 비어 있음 — §3-3

---

## §5 `판단` 오픈소스 recsys 도입 평가

### 5-0 두 개의 일반 원칙

먼저 이 평가를 만든 기준 두 개. 개별 도구 판정보다 이것이 오래 간다.

**⑴ 오픈소스 recsys의 가치는 대부분 *후보 생성*과 *대규모 학습*에 있다. 우리 문제는 그 둘이 아니다.**
후보는 300개(§2-7)이고 라벨은 내부 Alpha 규모에서 수천 건이다. 우리에게 어려운 것은 모델이 아니라
**편향된 로그로 정직하게 평가하기**다. → **도입할 것은 모델 스택이 아니라 평가·편향 도구다.**

**⑵ 도입 비용은 코드가 아니라 계약이다.** 프레임워크는 자기 가정을 갖고 온다 — 스칼라 점수가 랭킹의
표현이고, 노출은 긍정의 대리이고, popularity는 유용한 prior라는 가정. 이 코드베이스는 계약을 정본 +
뮤테이션 테스트 + import 경계 테스트로 잠갔는데, 프레임워크는 **그 잠금을 우회하는 통로**다.
`OrderingStrategy`가 15줄 Protocol인 것은 자산이다(§2-1): 공급처를 갈아끼울 수 있다. 프레임워크를 들이면
그 자산을 잃는다.

### 5-1 채택 후보

| 도구 | 무엇 | 왜 여기에 맞나 |
|---|---|---|
| **off-policy 평가 도구** (예: Open Bandit Pipeline 계열의 IPS/DR 추정기) | 편향된 노출 로그에서 "다른 순서였다면 더 나았을까"를 추정 | 원칙 ⑴이 지목하는 바로 그 어려움이다. 내부 Alpha 규모에서는 A/B를 돌릴 트래픽이 없으므로 **off-policy가 유일한 정직한 평가 수단**이다. 더 중요한 것은 이 도구들이 **로그 스키마를 강제한다**는 점 — §4-1의 되돌릴 수 없는 결정과 정확히 맞물린다. 스키마를 스스로 발명하면 나중에 보정 불가능한 형태가 되기 쉽다 |
| **popularity-bias 지표 정의** (노출 Gini/entropy, long-tail coverage) | 소수 항목이 노출을 흡수하는 정도의 측정 | **실제 구멍이다.** 정본이 popularity prior를 금지했는데(§2-3) **그것을 탐지할 지표가 없다.** 규율을 문서로만 갖고 있고 강제점이 없는 상태다(이 프로젝트에서 반복된 결함 유형). 정의를 차용하는 것은 수십 줄이고, 기존 eval ratchet gate에 키 하나로 붙는다 |

### 5-2 조건부 — 자체 구현이 낫다고 보는 것

**탐색(exploration) 슬롯.** VW contextual bandit 같은 성숙한 구현이 있지만, 후보 ≤300이고 역할이
tiebreak only(§3-3)이면 Thompson sampling 자체 구현이 100줄 수준이다. 하우스 스타일(frozen+slots
dataclass, 경계에만 pydantic, hand-written fake)에 프레임워크를 얹는 운영 비용이 이득보다 크다.
**평가는 §5-1의 도구로, 정책은 자체 구현으로** 나누는 것이 원칙 ⑵에 맞다.

탐색 슬롯은 두 역할을 겸한다: 루프를 깨는 장치이면서 §5-1 보정의 데이터원이다.

### 5-3 기각

| 도구군 | 기각 이유 |
|---|---|
| ANN·벡터 검색 (Milvus·Qdrant·Faiss), two-tower 검색 모델 | 후보 생성이 exact lookup 300개다(§2-7). 근사로 바꾸면 `exhaustive` fail-closed(§2-8)가 **깨진다** — 얻는 것 없이 계약을 잃는다 |
| feature store (Feast 등) | 온라인/오프라인 일관성 문제를 새로 사온다. 현재 feature는 같은 요청에서 이미 읽는 데이터다 |
| 대규모 학습 스택 (Merlin·TorchRec·HugeCTR) | 수십억 상호작용과 GB 임베딩 테이블 전제. 우리는 수천 이벤트다. 규모 불일치가 두 자리 이상 |
| MF/CF 라이브러리 (LightFM·implicit·Cornac) | **이것이 정확히 popularity prior 기계다**(§2-3 위반). 게다가 dense 상호작용 행렬을 전제하는데 Discovery는 sparse edge가 정상이다 |
| end-to-end 플랫폼 (Gorse류) | grounding·gate·attribution을 전부 자기가 소유한다. 지금까지 만든 계약을 버려야 한다 |
| 오픈소스 re-ranking 서비스 (Metarank류) | 개념상 가장 가깝다. 그러나 **스칼라 점수를 전제하고 ordering을 소유한다** — 정본 §2-1(`scalar 단일 점수 노출 금지`)과 정면 충돌이고, §2-1의 15줄 Protocol이라는 자산을 잃는다 |

### 5-4 `판단` recsys가 아니라 IR인 항목 — 혼동하지 않기

§2-8의 절벽은 recsys 문제가 아니라 **IR top-k 문제**이고, `threshold.py`는 검색엔진이 이미 갖고 있는
것(WAND / block-max 스킵)을 DynamoDB 위에 다시 구현한 것이다. 만약 holder 인덱스 소유가 우리로 오면
(§6-2) OpenSearch·Vespa 같은 **전치 인덱스 엔진이 진짜 후보가 된다**: `user`를 document, 보유 토픽을
term, 주입된 score를 impact, 후손 가중치를 term weight로 두면 rollup은 weighted OR top-k 한 방이고,
`resolve` 단계가 원리적으로 사라진다. 그리고 **WAND top-k는 근사가 아니라 정확하므로 fail-closed 계약을
고칠 필요가 없다.**

단 §1-1이 성립하면 이 항목의 우선순위는 내려간다 — rollup 자체가 없어지기 때문이다. 여기 적어두는
이유는 **"오픈소스 도입" 논의에서 IR 엔진과 recsys 프레임워크가 같은 항목으로 취급되면 안 된다**는
것이다. 앞은 계약을 보존하고 뒤는 계약을 가져간다.

---

## §6 열린 항목과 선결 조건

### 6-1 열린 항목 정리

| # | 항목 | 상태 |
|---|---|---|
| R1 | 후보별 노출·propensity 이벤트 스키마 (§4-1) | **미설계. 이 문서에서 유일하게 시급한 항목** — 서빙 시작 전에 정해야 한다 |
| R2 | 라벨 정의 (§4-2 표) | 초안만. "재방문"·"같은 토픽 재질의"의 관측 가능성이 bourbon-agent 쪽 이벤트에 달려 있다 — 미협의 |
| R3 | persona md 수신 계약 (출처·갱신·크기·언어) | 미협의. §4-3의 비신뢰 취급이 선결 |
| R4 | (A) 토픽 축 재순위의 진입 지점 | S2 어디인지 미설계. `grounding_select`의 rule pass와 LLM pick 사이인지, 그 뒤인지 |
| R5 | (B) 홀더 축 공급처가 `OrderingStrategy`에 들어오는 형태 | §3-3 불변식은 정해졌으나 인터페이스 미설계 |
| R6 | popularity-bias 지표를 eval ratchet에 추가 (§5-1) | 미착수. §5-1 두 항목 중 **이것이 먼저다** — 금지 규율에 강제점이 없다 |
| R7 | 탐색 슬롯 (§5-2) | 미설계. R1 없이는 데이터가 안 쌓이므로 R1 뒤 |
| R8 | off-policy 평가 도구 도입 (§5-1) | R1의 스키마가 이것을 결정하므로 **R1과 함께 판단**해야 한다. 도구를 나중에 고르면 스키마가 안 맞는다 |

### 6-2 `열린 항목` 선결 조건 — holder 인덱스는 누가 갖는가

§1-1이 성립하면 "토픽 T를 누가 보유하나"의 인덱스 소유가 우리(또는 추천 시스템)로 온다. 이것은
`facets_ownership_split_discussion.md`가 이미 다루는 질문과 **같은 질문**이고, 그 문서의 착수 자격은
측정값 셋이다(그쪽 §7-1).

→ **그 논의의 답이 이 그림에서 load-bearing이 된다.** 더 이상 독립적으로 미룰 수 있는 항목이 아니다.

### 6-3 `판단` 이 그림이 정하는 것 — `service_boundary_discussion.md` SB1

§3-1의 토픽 축 학습이 성립하면 "자연어+맥락 → 토픽"은 **우리 몫으로 정해진다.** 그 학습이 피드백
루프를 필요로 하고, 루프는 우리가 갖기 때문이다. 그쪽 문서의 경계 규칙(데이터 소유자는 사실과 검색
프리미티브, 호출하는 쪽은 해석·정책·실패 계약)과 같은 방향이다.

### 6-4 `판단` 답이 뒤집히는 조건

- **§5-1 첫 항목**: 라벨 규모가 예상보다 두 자리 크면(외부 공개·트래픽 증가) 원칙 ⑴의 전제가 깨지고
  학습 스택 도입이 다시 검토 대상이 된다. 기준은 라벨 수가 아니라 **A/B를 돌릴 트래픽이 있는가**다.
- **§3-2**: pool이 유한하다는 전제가 깨지면(홀더 수천 명) 자기강화 루프의 심각도가 달라진다. 다만
  방향은 나빠지는 쪽이다.
- **§5-3의 Metarank류**: 정본 §2-1의 "scalar 노출 금지"가 뒤집히면 이 판정이 바뀐다. 그 규율은
  설명 가능성 때문에 있으므로, 뒤집으려면 `owner_notes`까지 포함한 귀속 계약 전체를 다시 봐야 한다.

---

## §7 이 문서가 하지 않은 것

- 코드 변경 **없음**. `bourbon-agent-discovery-api`는 `d82cd04` 그대로다.
- 정본(`recommendation_pipeline_design.md`) 변경 **없음**. §3-3의 불변식을 §S5·§10으로 올리는 것은
  결정 후다.
- 오픈소스 도구를 **하나도 도입하지 않았다.** §5는 평가이고, 실제 선택은 R8이 R1과 함께 판단한다.
- 특정 제품·버전을 검증하지 않았다. §5의 도구 이름은 **부류의 예시**이고, 도입 시점에 실물 검증이
  따로 필요하다.
- bourbon-agent·producer·topic-api 어느 팀에도 **발신하지 않았다.**
- `topic_api_analysis.md` §11(열린 질문) 갱신 **없음**.

## 변경 이력

- **2026-08-27 rev 1** — 신설. 2026-08-27 대화(피드백 루프 기반 추천 + 오픈소스 recsys 도입)의
  사실·판단·열린 항목(R1–R8)을 임시 기록. §2는 `d82cd04`·`9ee67f3`에서 `file:line`으로 확인했다.
  ★규율 네 개:
  ⑴ §3 — **피드백을 "추천 품질 학습"으로 한 덩어리로 부르면 위험한 축이 안전한 축의 정당성을 빌려
  쓴다.** 학습 대상은 토픽 축과 홀더 축으로 갈라서 각각 판정한다.
  ⑵ §4-2 — **지식 제품에서 engagement 볼륨은 품질의 대리가 아니라 그 반대일 수 있다.** 좋은 답은
  대화를 끝낸다. 라벨을 고를 때 "이 신호가 많아지는 실패 모드"를 먼저 쓴다.
  ⑶ §5-0 ⑵ — **도입 비용은 코드가 아니라 계약이다.** 프레임워크는 자기 가정(스칼라 점수·노출=긍정·
  popularity 유용)을 갖고 오고, 그것이 정본과 테스트로 잠근 계약을 우회하는 통로가 된다.
  ⑷ §5-1 둘째 항목 — **금지 규율에 그것을 탐지할 지표가 없으면 그 규율은 강제점이 없다.** popularity
  prior 금지를 문서로 갖고 있으면서 노출 편중을 재는 지표가 없었다. 이 프로젝트에서 반복된 결함
  유형("문서로 쓴 불변식 ≠ 강제")의 새 사례다.
