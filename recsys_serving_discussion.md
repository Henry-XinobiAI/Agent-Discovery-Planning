# 추천 시스템 서빙 — 피드백 루프와 오픈소스 recsys 도입 논의 기록 (rev 3, 2026-08-27)

> **문서 지위: 결정이 아니다.** 2026-08-27 대화에서 나온 구조 방향(topic-api를 토픽 조회로 좁히고,
> 대화 이벤트를 피드백으로 받아 추천 시스템 자체가 학습하는 그림)과, 그 판단에 필요한 **검증된 사실**,
> 그리고 **오픈소스 recsys 도입 평가**를 임시로 모아 둔 것이다. 어떤 항목도 확정이 아니고 어떤 코드도
> 이 문서를 근거로 바뀌지 않았다. **설계 정본은 `recommendation_pipeline_design.md`**이며, 이 문서가
> 정본과 어긋나면 정본이 맞다. 결정이 생기면 그때 정본 §S5·§S2·§9(열린 결정)로 올리고 이 문서는 그
> 결정을 가리키는 기록으로 남긴다.
>
> **표기**: `사실`(소스에서 `file:line`으로 확인) · `판단`(이 문서의 의견) · `열린 항목`(협의·측정 대기).
> 사실과 판단을 섞어 쓰지 않는다 — 이 문서의 유일한 지속 가치는 앞의 것이다.
>
> **자매 문서**
> - `facets_ownership_split_discussion.md`(rev 4) — holder 인덱스와 `score` 소유를 producer로 옮기는
>   구조. 이 문서 §1의 "topic-api는 토픽만 찾는다"가 성립하면 **그쪽 논의의 답이 선결 조건이 된다**(§6-2).
> - `service_boundary_discussion.md`(rev 3) — "자연어+맥락 → 토픽"이 어느 쪽 역할인가. 이 문서 §3-1의
>   토픽 축 학습이 성립하면 **그쪽 SB1의 답이 정해진다**(우리 쪽, §6-3).
>
> **rev 2의 두 전제 (2026-08-27 지시)**
> 1. **기존 계약은 이 그림의 제약이 아니다.** 정본에 잠긴 규율은 *지금까지의* 가정 집합(topic-api가
>    보유자 랭킹을 소유·피드백 없음·facets 미정)에서 나온 것이므로, 가정이 바뀌면 재도출 대상이다.
>    rev 1의 논거 중 계약에서 나온 것은 이 문서에서 **효력을 잃는다**(§3-4에 무엇이 남고 무엇이
>    사라지는지 정리).
> 2. **네 입력 중 `topic`만 실재한다**(§2-9). 피드백 이벤트·persona·유사도는 전부 미래 값이다.
>    이것은 facets를 "나중에 결정된다"고 두고 슬롯만 만든 상황과 **같은 종류**이고, 그 선례의
>    방법을 이 문서의 방법으로 채택한다(§3-0). 단 한 지점에서 다르다 — §3-0-2.
>
> **rev 3이 더한 것** — 학습 행의 단위(라벨·feature가 무엇인지, §4-5), persona 처리(임베딩이
> 필요한 이유와 그 전에 시도할 것, §4-6), GPU가 필요한 지점(§5-6), 도입 형태와 재작성 트리거(§5-7),
> precompute의 경로별 속성(§5-8). **정정 두 건**: §5-5의 ⒝ 해석("topic 없이" ≠ "grounding 없이")과
> §4-1의 규모 근거(제약은 행 수가 아니라 positive 수).
>
> 기준 커밋: `bourbon-agent-discovery-api` `d82cd04` · `bourbon-topic-api` `9ee67f3` ·
> `e3llm-api` (embedding 차단 확인, §2-10).

---

## §1 논의된 그림

2026-08-27 대화에서 제시된 방향. 네 조각이다.

1. **topic-api의 역할을 토픽 조회로 좁힌다.** 보유자 랭킹은 topic-api의 일이 아니게 된다.
2. **대화 이벤트를 피드백으로 받는다.** 추천된 에이전트와 사용자가 대화를 시작했는지, 얼마나 많이
   했는지, 맥락상 만족했을지.
3. **사용자 persona(md)를 외부에서 받는다.**
4. **협업 신호를 넣는다.** "이런 사용자가 이 토픽에 긍정적이었다면, 비슷한 사용자가 같은 토픽을
   요청했을 때 비슷하게 추천한다."

5. **(rev 2) 위 네 조각 중 실재하는 입력은 `topic` 하나뿐이다.** 나머지는 전부 미래 값이고, 기존
   계약은 이 그림의 제약이 아니다.

이 문서가 답하려는 질문은 둘이다. ⑴ 입력이 아직 없는 상태에서 무엇을 지금 만들고 무엇을 만들지
않는가(§3·§4). ⑵ 오랫동안 개발되어온 오픈소스 recsys를 도입하는 것이 여기에 맞는가(§5).

---

## §2 사실 — 소스에서 확인한 것

이 절이 이 문서의 본체다. 판단이 바뀌어도 여기는 남는다.

### 2-1 `사실` 학습된 순위가 들어갈 자리는 이미 비어 있다

`agent_discovery/stages/ordering.py`의 `OrderingStrategy`는 Protocol이고(`:21-29`), 기본 구현
`RrfOrdering`은 fused score만 읽는다(`:32-40`). `order()`가 전략을 인자로 받는다(`:44`).

모듈 docstring이 그 이유를 적어 뒀다(`:3-4`) — *"The primary key is a slot, because the key this
service actually wants -- an expertise ordering read out of topic-api's facets -- cannot be written
yet."* 그리고 tiebreak는 슬롯이 아니라 `order()`가 직접 붙인다(`:6-8`, `:54`): 전략이 잊을 수 없게.

→ **holder 축의 학습은 새 아키텍처가 아니다.** 비어 있는 슬롯에 공급처가 생기는 것이다.

### 2-2 `사실` "preference 신호는 tiebreak only"는 이미 등재된 선례다

정본 §1 규율 표(`recommendation_pipeline_design.md:64`):
`즐겨찾기 = tiebreak only, gate 우회 금지 | S5 — 단 dormant 슬롯: 현 파이프라인에 즐겨찾기 데이터
공급처가 없다. provider가 생길 때까지 이 키는 비활성`. §S5의 정렬 키 사다리(`:392-402`)에도 2키로
들어가 있고 `**dormant**`로 표시돼 있다.

→ engagement 피드백은 **새로운 종류의 신호가 아니다.** 즐겨찾기와 같은 부류(personalization,
expertise 아님)이고, 따라서 같은 자리에 같은 제약으로 들어가야 한다.

### 2-3 `사실` popularity prior 금지는 잠긴 규율이다

정본 `:66` — `popularity prior 금지 | S5 (RRF는 topic별 순위 융합 — 전역 인기 항 없음)`.
현재 RRF가 이 규율을 만족하는 근거가 "전역 인기 항이 없다"로 명시돼 있다.

### 2-4 `사실` 피드백/상호작용 로그는 어디에도 없고, 노출은 집계로만 남는다

`bourbon-agent-discovery-api` 전체에서 `rec_signal`·`rec-signal` grep 0건. 클릭·수락·거절·대화
어떤 이벤트도 기록되지 않는다.

노출 기록은 `pipelines/topic_recommend.py:68-72`의 `_RunSummary.to_log()`가 전부이고,
`ranked_holder_count`·`merged_user_count`·`excluded_user_count`·`ineligible_user_count`
— **전부 집계 수치다.** 어떤 후보가 몇 위였고 왜 잘렸는지는 남지 않는다.

→ 지금 서빙을 시작하면 **후보별 노출 기록 없이** 피드백이 쌓인다. §4-1이 이 사실 위에 있다.

### 2-5 `사실` 오프라인 점수 계산 / 온라인 서빙 분리는 이미 존재한다

`bourbon-topic-api`의 `score`·`score_detail`은 요청 경로에서 계산되지 않는다.
`topic/persona_topics/scoring.py:18`의 `topic_score(facets, confidence)`가 producing pipeline 쪽
함수이고, 저장 스키마 주석이 세 writer를 분리해 둔다(`topic/storage/user_topics.py:6-8`):
`user`(visibility·rank_key) / `extraction`(descriptions·blocks) / `injection`(score·score_detail).

→ recsys 패턴의 핵심 구조(오프라인 학습 → 온라인 서빙)는 **새로 도입할 것이 아니라 이미 있는 것**이다.
바뀌는 것은 그 오프라인 단계에 피드백이 들어가는지 여부다.

### 2-6 `사실` topic-api에 검색엔진도 벡터DB도 없다

`pyproject.toml` dependencies(`:5-26`)에 opensearch·elasticsearch·vespa·qdrant·milvus·faiss·
embedding 관련 항목이 하나도 없다. 저장은 DynamoDB(`aiobotocore`) 단독이고, top-k는
`topic/search/threshold.py`가 Fagin의 threshold rule을 직접 구현한 것이다(docstring이 *"the classic
threshold rule"*이라고 밝힌다).

### 2-7 `사실` 순위를 매길 후보 집합은 작다

`_MAX_GROUPS = 3`(`stages/expansion.py:36`) × `DEFAULT_USERS_LIMIT = 100`(= topic-api의 `_MAX_LIMIT`,
`providers/topic_api/client.py:45,55`) = **최대 300 후보**. `merge`가 중복을 접으므로 실제로는 더 적고,
`order()`가 `max_results`로 자른다.

→ **랭킹 쪽에는 규모 문제가 없다.** ANN도 feature store도 이 숫자에서는 해결할 대상이 없다.

### 2-8 `사실` 현재 rollup 랭킹은 유저 수가 늘면 결정론적으로 불완전해진다

`SEARCH_MAX_DESCENDANTS = 50` · `SEARCH_MAX_GSI_ITEMS = 20_000` · `SEARCH_MAX_USER_QUERIES = 2_000`
(`topic/config.py:139-142`), `page_size = limit × 4`(`_STREAM_PAGE_FACTOR` `topic/search/service.py:46`, 쓰이는 곳 `:233`).
읽지 않은 스트림의 bound는 ∞(`threshold.py` 설계), `_pick`이 ∞부터 고른다.

페이지 하나가 400명이고 전부 새 후보면 resolve 400건이므로 **5페이지에서 `max_resolves`가 소진되고,
50 스트림 중 45개는 bound가 ∞ → T=∞ → 증명 불가 → `exhaustive=False`**. 지금 동작하는 이유는 유저가
적어서 각 스트림이 첫 페이지에 드레인되기 때문(bound→0)이다. 우리 쪽은 이걸 fail-closed로 받는다
(`stages/retrieval.py:122`).

→ §1-1(topic-api를 조회로 좁힘)이 성립하면 **이 절벽은 고칠 대상이 아니라 없어질 대상이 된다.**
rollup·예산·resolve 단계가 전부 사라진다.

### 2-9 `사실` (rev 2) 네 입력 중 실재하는 것은 `topic` 하나다

| §1의 조각 | 지금 있는가 | 확인 |
|---|---|---|
| topic (카탈로그 조회) | **있다** | `providers/topic_api/client.py` — `/search/topics`·`/topics/{id}/users` 둘 다 배선돼 있고 dev에서 동작한다 |
| 대화 이벤트 피드백 | 없다 | `rec_signal`·`rec-signal` grep 0건(§2-4). 수신 포트도 스키마도 없다 |
| persona md | 없다 | 이 repo에 persona를 받는 포트가 없다. `_MAX_GROUPS`·probe는 요청 body의 `topic`+`context`에서만 나온다 |
| user-user 유사도 | 없다 | 위 둘의 파생값이므로 둘이 없으면 없다 |

→ **facets와 같은 상황이다.** 정본 §S5의 주 정렬 키가 "나중에 결정된다"로 비어 있는 것과 같은
종류이고, 그때 취한 방법이 §3-0이다.

### 2-10 `사실` (rev 3) 인프라 — GPU도, 임베딩 경로도, 수치 스택도 없다

| 확인한 것 | 근거 |
|---|---|
| **클러스터에 GPU 노드가 없다** | `k8s/overlays/dev/deployment-patch.yaml:8-12`의 tolerations는 `karpenter.sh/capacity-type=spot`이고 GPU가 아니다. 우리 repo와 topic-api 양쪽에서 accelerator·nvidia 관련 키가 0건 |
| **e3llm-api는 embedding 모델을 명시적으로 차단한다** | `e3llm/models/google.py:78` — `exclude_keywords = ("audio", "live", "embedding", ...)`. 노출 라우트도 `/chat/completions` 하나(`api/main.py:19-20,167-172`). → **persona 임베딩 경로가 지금 없다**(§4-6) |
| **우리 런타임에 수치 스택이 없다** | `pyproject.toml:5-20` — numpy·scipy·sklearn 모두 없다. 첫 수치 의존성 도입이 이미지 크기 결정이 된다(§5-6) |
| **CLI가 서빙과 같은 파이프라인을 in-process로 부른다** | `cli/recommend.py:80` — `async with recommend_pipeline() as pipeline`. precompute의 이음매가 이미 존재한다는 증거(§5-8) |
| **pod은 spot을 tolerate한다** | 위와 같은 곳. 축출되므로 무상태여야 하고, 큰 모델을 서빙 이미지에 넣지 않을 이유가 된다 |

---

## §3 `판단` 학습 대상을 두 축으로 갈라야 한다

### 3-0 `판단` (rev 2) 방법 — facets 선례를 그대로 쓰고, 한 지점에서만 벗어난다

#### 3-0-1 선례가 규정하는 것: 이음매는 만들고 정책은 만들지 않는다

`stages/ordering.py:3-5`가 그 방법을 적어 뒀다 — *"the facet contract is open and the values upstream
are still dummy, so a formula locked in now would be fitted to a generator rather than to expertise."*
값이 없는 동안 공식을 잠그면 **스탠드인에 fit된다.** 그래서 슬롯(Protocol)만 만들고 기본 전략은
값 없이 도는 것으로 두었다.

§2-9의 세 입력에 그대로 적용하면 경계가 이렇게 갈린다.

| 지금 만든다 (이음매) | 지금 만들지 않는다 (정책) |
|---|---|
| 피드백 이벤트 스키마와 수신 포트 | 라벨 가중치 |
| persona 수신 포트 (+ 비신뢰 취급, §4-3) | 유사도 메트릭·임계값 |
| cohort(요청자) → 유사 집단 포트 | cohort 정의 자체 |
| 정렬 키 슬롯의 두 번째 공급처 자리 | 탐색률·감쇠 |
| 후보별 노출 이벤트 (§4-1) | 어떤 라벨이 positive인지의 확정 |

오른쪽 열을 지금 정하면 전부 스탠드인에 fit된다. **이 문서가 오른쪽 열에 답을 쓰지 않는 것은
미룬 것이 아니라 선례를 따른 것이다.**

#### 3-0-2 선례가 규정하지 못하는 것: 피드백은 기다리면 사라진다

facets와 피드백이 **한 지점에서 다르고, 그 차이가 §4-1의 근거를 바꾼다.**

- **facets는 기다려도 손실이 없다.** 계약이 닫히는 날 값은 *전체 이력과 함께* 도착한다 — topic-api가
  모든 유저의 점수를 소급 보유하고 있으므로, 그동안 우리가 아무것도 안 했어도 잃은 것이 없다.
- **피드백은 서빙이 만들어내는 값이다.** 노출을 기록하지 않은 채 서빙하면 그 기간의 피드백은
  **무엇이 왜 보여졌는지를 소급 재구성할 수 없어** 보정 불가능한 데이터가 된다.

→ 따라서 결론은 선례보다 좁다: **정책은 선례대로 dormant로 미루고, 노출 로깅은 미룰 수 없다.**
§4-1은 계약에서 나온 요구가 아니라 **이 비대칭에서 나온 요구다.**

§1-2의 피드백은 `(요청자, 추천된 에이전트, 토픽, 결과)` 튜플이다. 여기서 학습 가능한 것이 둘인데
**성질이 다르다.** 하나로 묶어 "추천 품질 학습"이라고 부르면 한쪽의 판단이 다른 쪽에 자동으로
적용된다.

### 3-1 `판단` (A) 토픽 축 — 이견 없이 찬성

"이 사용자가 이렇게 물었을 때 어떤 토픽을 의도했나". 현재 S2(grounding·disambiguation)가 담당하고
LLM 콜을 쓰며 abstain도 나는 자리다. 피드백을 여기에 먹이면:

- 사람의 순위를 건드리지 않는다 → popularity prior가 들어올 통로가 **구조적으로** 없다.
- persona(§1-3)가 cold start를 푼다. 상호작용 0건이어도 persona 임베딩으로 user-user 유사도가 나오니
  §1-4의 "비슷한 사용자에게 비슷하게"가 첫날부터 동작한다. 축적되면 학습으로 전환한다.
- 실패 모드가 관측 가능하다: 잘못 ground하면 사용자가 같은 질문을 다시 한다(§4-2 표).

### 3-2 `판단` (B) 홀더 축 — 자기강화 루프가 사는 자리

"토픽 T에서 누구를 위로". 우리가 노출을 통제하므로 상위에 놓인 에이전트가 대화를 받고, 그래서 더
위로 가고, 더 받는다. pool이 유한하므로(§2-7: ≤300, 실제 반환은 `max_results`) **소수가 대화를 거의
전부 흡수하고, 실제로 더 잘 아는 사람과 무관하게 "그 분야 전문가"가 고정된다.**

전문성 추천에서 이것은 성능 저하가 아니라 **제품 주장이 거짓이 되는 것**이다. §2-3의 금지(정본
`:66`)가 막으려던 것이 정확히 이 상태다.

### 3-3 `판단` (rev 2 재도출) 순서만 바꾸는가, 집합도 고르는가 — 금지가 아니라 명명된 선택

rev 1은 이것을 불변식으로 썼고 근거를 정본 규율(§2-2·§2-3)에 두었다. **계약이 종속이면 그 근거는
효력이 없다.** 그래서 금지에서 **두 선택지와 각각의 대가**로 내린다. 어느 쪽을 고를지는 제품 결정이다.

| | ⒜ 학습은 순서만 바꾼다 | ⒝ 학습이 집합도 고른다 |
|---|---|---|
| 순위가 되는 것 | 자격(expertise) → 그 안에서 선호 | 예측된 engagement |
| 얻는 것 | 자격 미달자가 인기로 올라오지 않는다. 결과마다 보유 토픽으로 근거가 남는다 | 표에 잡히지 않는 적합성까지 학습한다. 상한이 자격 판정의 품질에 묶이지 않는다 |
| 대가 | 학습이 개선할 수 있는 폭이 자격 통과 집합으로 제한된다 | **순위가 전문성의 함수가 아니라 과거 노출의 함수가 된다.** "이 사람이 이걸 안다"는 주장이 사실상 "이 사람이 많이 추천됐다"로 바뀐다 |
| 부수 효과 | `matched_topics[]`·`relation`·`owner_notes`의 귀속이 그대로 성립 | 귀속이 성립하지 않는다 — 어제 wire에 올린 값들과 그것을 렌더하는 bourbon-agent 쪽 배선이 근거를 잃는다 |

`판단` ⒝는 방어 가능한 제품 입장이다 — "사람들이 실제로 많이 대화한 에이전트를 위로"는 정당한 정의일
수 있다. 이 문서가 주장하는 것은 ⒝가 틀렸다는 것이 아니라, **⒝의 대가가 "이 사람이 이걸 안다"는
제품 주장의 의미 변경이고, 그것이 조용히 일어나면 안 된다는 것**이다.

`열린 항목` R9 — ⒜/⒝ 선택. §2-9 때문에 **지금 고를 필요가 없다**: 두 경우 모두 §3-0-1의 왼쪽 열
(이음매)은 동일하고, 갈라지는 것은 오른쪽 열(정책)이다. 데이터가 쌓인 뒤 측정으로 고를 수 있다.

### 3-4 `판단` (rev 2) 계약을 내렸을 때 남는 것과 사라지는 것

rev 1의 논거를 근거의 출처로 분류한다. 이 절이 rev 1 → rev 2의 실질적 변경이다.

**사라진다 (계약에서 나온 것)**

| rev 1의 논거 | 왜 사라지나 |
|---|---|
| ANN이 `exhaustive` fail-closed를 깬다 (rev 1 §5-3) | **스스로 소멸한다.** §1-1로 topic-api를 조회로 좁히면 완전성 플래그 자체가 없어진다. 옛 토폴로지의 산물이었다 |
| re-ranking 서비스는 scalar 노출 금지와 충돌 (rev 1 §5-3) | 그 규율이 종속이면 학습된 scalar가 자연스럽다. **기각 → 시기상조**로 내려간다(§5-3 갱신) |
| 귀속이 깨진다 (rev 1 §3-3) | 순수 계약이다. 다만 `owner_notes`는 이미 wire에 있고 bourbon-agent가 렌더한다 — 버리는 것은 되지만 **소비자가 이미 배선된 결정**이다(§3-3 표 마지막 행) |

**남는다 (계약과 무관한 것)**

| 논거 | 근거의 출처 |
|---|---|
| 대화량을 positive label로 쓰면 안 된다 (§4-2) | **측정 타당성.** 지식 제품에서 좋은 답은 대화를 끝내므로 이 라벨은 목표와 반대로 상관한다. 어떤 계약을 내려도 그대로 틀리다 |
| 남의 비공개 보유를 남에게 보이지 않는다 | **성질이지 설계 취향이 아니다.** 재설계와 무관하다 |
| persona md는 비신뢰 입력이다 (§4-3) | 같음. 우리 프롬프트에 들어가는 자유 텍스트라는 사실이 계약과 무관하다 |
| 후보 300개·라벨 수천 (§5-0 ⑴) | **산수다.** 단 그 300이 유지되는지는 §5-5에 달려 있다 |

---

## §4 `판단` 지금 정해야 하는 것 — 나중에 되돌릴 수 없는 것들

### 4-1 `열린 항목` propensity를 서빙 첫날부터 남긴다

추천한 것에 대해서만 피드백이 생기므로, 노출을 기록하지 않으면 **"보여주지 않았던 사람이 더 나았을까"를
영원히 알 수 없다.** 나중에 off-policy 보정(§5-1)을 하려면 그 시점에 데이터가 이미 있어야 한다.
§2-4가 현재 상태이고(집계만 남는다), **근거는 §3-0-2의 비대칭이다** — facets는 기다려도 전체 이력과
함께 오지만 피드백은 기다린 기간만큼 영구히 없다. 계약에서 나온 요구가 아니므로 rev 2에서도 그대로
남는다(§3-4).

필요한 것:

| 남길 것 | 이유 |
|---|---|
| 고려된 후보 **전체** (반환된 것만이 아니라) | 반환된 것만 남기면 분모가 없다 |
| 각 후보의 순위와 정렬 키 값 | 어떤 키가 그 순위를 만들었는지 재구성 가능해야 한다 |
| 잘린 이유 (gate / eligibility / stub / `max_results`) | 자격 미달과 순위 미달은 다른 사건이다 |
| ground된 토픽과 그 그룹 | (A) 축 라벨의 키 |

`판단` 이것은 **로그가 아니라 이벤트 스트림**이어야 한다. 후보별 user_id가 들어가므로 보존 정책이
붙는 데이터이고, 현재 structlog 규율(사용자 텍스트 금지·id는 남김)과는 다른 판단이 필요하다.

★ **이 그림에서 가장 싸고 가장 되돌릴 수 없는 항목이다.** 지금은 스키마 하나이고, 나중에는 못 만든다.

**(rev 3 정정) 규모의 제약은 행 수가 아니라 positive 수다.** rev 1·2가 "라벨 수천"이라고 쓴 것은
행(= (요청, 후보) 쌍)을 센 것인데, 모델 크기를 정하는 것은 결과가 긍정인 행의 수다. 자세한 것은
§4-5이며, 결론은 이 항목에도 걸린다 — **노출 이벤트를 남길 때 결과가 붙지 않은 행도 남겨야 한다**:
positive만 남기면 분모가 없고, 분모가 없으면 §5-1의 보정이 성립하지 않는다.

### 4-2 `판단` "대화를 얼마나 많이 했는지"를 positive label로 쓰면 안 된다

지식 제품에서는 **좋은 답이 대화를 끝낸다.** 정확한 답 한 번이면 짧게 끝나고, 못 알아들으면 길어진다.
대화량을 positive로 학습하면 **답을 효율적으로 하지 못하는 에이전트를 선호하도록** 학습된다. 콘텐츠
피드의 dwell time과 같은 함정이지만, 여기서는 부호가 더 선명하게 뒤집힌다 — 피드는 체류가 목적이고
Discovery는 해소가 목적이다.

대신 쓸 라벨:

| 신호 | 강도 | 근거 |
|---|---|---|
| 다른 토픽으로 **같은 에이전트에 재방문** | strong + | 슬롯 효과가 아니라 사용자 판단. 조작 어렵다 |
| 같은 토픽을 **다른 에이전트에게 다시 물어봄** | strong − | 첫 추천이 답을 못 했다는 뜻. 파생 비용 거의 0 |
| 명시적 dismiss | strong − | |
| 대화 시작 여부 | weak + | 슬롯 위치에 크게 오염됨 → §4-1 없이는 못 쓴다 |
| 대화량 | **label 아님, feature로만** | 위 이유 |

### 4-3 `판단` persona md는 비신뢰 입력이다

정본 §1 규율 표에 이미 `판정 입력 텍스트 전부 비신뢰 | S1·S2의 LLM 프롬프트`가 있다(`:67`).
persona md는 외부에서 받는 자유 텍스트이므로 **같은 부류**이고, 우리 프롬프트(expansion·
disambiguation)에 들어가는 순간 injection 책임이 우리로 온다.

`owner_notes`에서 합의한 "injection 완화는 bourbon-agent 책임"은 **우리가 내려주는 응답**에 대한 것이고,
**우리가 소비하는 입력**은 별건이다. rev 5.11 §9-⑨의 주의와 방향이 반대인 항목이다.

부수적으로 privacy: user-user 유사도를 persona로 계산하면 "비슷한 사용자가 좋아한 토픽"이 이웃에 대한
추론 채널이 된다. deferred 상태인 privacy gate가 여기서 두 번째 구체적 대상을 갖는다(첫 번째는
topic-api `friends` tier).

### 4-4 `판단` 부트스트랩 순서 — recsys가 첫 서빙 경로가 될 수 없다

학습 데이터를 얻으려면 먼저 서빙해야 한다. 따라서:

1. 현 파이프라인으로 서빙(real edge turn-on) + **§4-1을 첫날부터**
2. (A) 토픽 축 — persona로 cold start, 축적되면 학습으로 전환
3. (B) 홀더 축 — `OrderingStrategy` 슬롯에 공급처로 진입, 탐색 슬롯 1개 확보(§5-2)
4. expertise 주 정렬 키는 facets 계약이 닫힐 때까지 비어 있음 — §3-3

### 4-5 `판단` (rev 3) 학습 행의 단위 — 라벨과 feature가 무엇인가

이 절이 없으면 §5의 규모 논거("라벨 10³ + feature ~20")를 검증할 수 없다. 단위부터 고정한다.

**행 하나 = (요청 1건, 후보 에이전트 1명) 쌍.** 랭킹 학습에서 요청 1건이 하나의 query group이고,
그 안의 행들이 그 요청에서 고려된 후보들이다.

- **라벨** = 그 쌍의 결과. 등급형이며 등급의 근거는 §4-2의 표다:
  `0` 대화 안 함 / `1` 대화 시작 / `2` 다른 토픽으로 같은 에이전트에 재방문.
  같은 토픽을 다른 에이전트에게 다시 물어본 경우는 **첫 추천 행의 negative**로 들어간다.
- **feature** = 그 쌍에 대해 **서빙 시점에** 계산되는 수. 후보 하나의 속성이 아니라 "이 요청에서의
  이 후보"의 속성이다 — 같은 사람이 다른 요청에서는 다른 feature 값을 갖는다.

#### 4-5-1 `판단` 제약은 positive 수이고, 거기서 feature 상한이 나온다

```
요청 200건 × 반환 5명 = 1,000행
전환율 20%      → positive 200개
```

경험 규칙은 **feature 개수 ≤ positive / 10**이다. positive 200개면 **feature 5–8개가 상한**이고,
§5-1이 말한 "~20개"는 이미 과적합 영역이다. 이것이 §5-6에서 LR을 먼저 고르는 이유의 절반이다.

★ **이 계산은 R1(§4-1)에 의존한다.** 분모(결과가 붙지 않은 행)를 남기지 않으면 전환율을 알 수 없고,
전환율을 모르면 positive 수를 예측할 수 없어 모델 크기를 고를 근거가 없다.

#### 4-5-2 `사실` 오늘 배선 없이 쓸 수 있는 feature

`domain/candidates.py`의 `Candidate`·`GroupContribution`에 실제로 있는 값만 센다.

| feature | 출처 | 무엇을 묻는가 |
|---|---|---|
| `rrf_score` | `Candidate` | 현재 정렬 키 그 자체 — baseline |
| group 수 | `len(group_contributions)` | 여러 해석에서 겹쳐 나온 사람인가 |
| 최소 `distance` | `GroupContribution.distance` | 조회 토픽 자신을 보유했나(0) 후손만인가(≥1) |
| `ranking_contribution` (그룹 내) | 같음 | topic-api의 자기 점수. **토픽 간 비교 불가**라 그룹 안에서만 |
| 최상위 `rank` | 같음 | 융합 전 원래 순위 |
| `owner_notes` 개수 | 같음 | 자기 말로 쓴 것이 있나 |

여섯 개다 — 우연히 §4-5-1의 상한과 맞는다.

**배선이 필요한 것**: `HoldingEvidence`의 `deep_holdings_observed`·`holder_description_present`·
`timestamps_present`는 S4에서 `GroupContribution`으로 넘어올 때 떨어진다. `score_detail`(facets)은
값이 아직 dummy라 제외한다 — §3-0-1의 "스탠드인에 fit하지 않는다"가 그대로 적용된다.

### 4-6 `판단` (rev 3) persona 처리 — 임베딩보다 먼저 시도할 것이 있다

#### 4-6-1 임베딩이 필요한 이유는 정확히 하나다

**"비슷한 사용자"의 정의.** §1-4가 요구하는 것이 그것이고, 상호작용이 0이면 유사도의 근거가 persona
텍스트밖에 없다. 상호작용이 쌓이면 행동 기반 유사도(co-engagement)가 더 정확해지고 임베딩은
불필요해진다.

→ **임베딩은 cold start 장치이고 영구 부품이 아니다.** 이것이 도입 규모를 정한다: cold start 장치에
벡터DB 클러스터를 세울 이유가 없다(§5-3의 판정과 같은 방향).

#### 4-6-2 persona가 "나열"이면 벡터 없이 유사도가 나온다

persona가 "어떤 사람이다"와 "무엇을 선호한다"의 **나열**이라면:

```
persona 문서 → (기존 S1·S2 재사용) → 토픽 집합 → 집합 유사도(Jaccard / 가중 겹침)
```

우리는 이미 그 기계를 갖고 있다. 그리고 이쪽이 임베딩보다 세 가지가 낫다 — **해석 가능**("두 분 다
드립 커피와 등산"), **디버깅 가능**, **인프라 0**. §2-10의 임베딩 경로 부재도 이 경로에서는 문제가
되지 않는다.

임베딩이 필요해지는 조건은 둘이다: 나열이 아니라 자유 산문일 때, 또는 카탈로그에 없는 개념이 많을 때.
**둘 다 측정으로 확인할 수 있다** — persona 항목의 grounding 성공률이 그 지표다.

#### 4-6-3 임베딩을 쓰게 되면 한 벡터가 아니라 두 벡터다

말씀된 persona 구조는 한 문서에 성격이 다른 두 정보가 섞여 있고, 유사도에서 서로 다르게 작동한다.

| 부분 | 이것으로 유사도를 재면 |
|---|---|
| "어떤 사람이다" (정체성·배경·전문성) | 비슷한 **배경**의 사람이 이웃이 된다 |
| "무엇을 선호한다" (취향) | 비슷한 **취향**의 사람이 이웃이 된다 |

**§1-4가 원하는 것은 후자다** — "이 토픽에 긍정적이었다"를 예측하는 것은 취향 쪽이다. 문서를 통째로
한 벡터로 만들면 두 신호가 섞이고, **배경은 비슷한데 취향은 반대인 사람이 가까운 이웃**이 된다.

→ 섹션별로 나눠 **identity 벡터와 preference 벡터** 둘을 만든다. 유사도는 preference로, grounding
보조는 둘 다. 이것은 `embedding-disentanglement-ladder`(anchor 파티션 → stance-only embed →
projection layer)의 첫 칸과 같은 사다리다.

모델 요건: **다국어 · 대칭 유사도(문서-문서) · CPU급**. persona는 짧으므로 긴 컨텍스트는 불필요하다.
요청 경로가 아니라 **persona 갱신 시 배치**이고, §2-10 때문에 서빙 이미지에 넣지 않는다.

---

## §5 `판단` 오픈소스 recsys 도입 평가

### 5-0 두 개의 일반 원칙

먼저 이 평가를 만든 기준 두 개. 개별 도구 판정보다 이것이 오래 간다.

**⑴ 오픈소스 recsys의 가치는 대부분 *후보 생성*과 *대규모 학습*에 있다. 우리 문제는 그 둘이 아니다.**
후보는 300개(§2-7)이고 라벨은 내부 Alpha 규모에서 수천 건이다. 우리에게 어려운 것은 모델이 아니라
**편향된 로그로 정직하게 평가하기**다. → **도입할 것은 모델 스택이 아니라 평가·편향 도구다.**

**⑵ (rev 2 재도출) 프레임워크는 가정을 갖고 오고, 그 가정은 보이지 않는 곳에 있다.**
rev 1은 이 원칙을 "계약을 지킨다"로 썼는데, 계약이 종속이면 그 형태로는 성립하지 않는다(§3-4).
남는 실질은 다르다 — 스칼라 점수가 랭킹의 표현이고, 노출은 긍정의 대리이고, popularity는 유용한
prior라는 가정은 **프레임워크의 코드가 아니라 기본값과 손실함수에 들어 있어서** 도입한 뒤에는
찾아내기 어렵고 떼어내기 더 어렵다. §2-9에서 우리는 아직 어느 가정이 맞는지 **측정한 적이 없다.**
가정을 고르지 않은 상태에서 가정이 박힌 도구를 들이면 그 도구가 선택을 대신한다.

이것은 §3-0-1의 표와 같은 말이다: 이음매는 우리가 만들고, 정책은 데이터가 정한다. 프레임워크는
오른쪽 열을 미리 채워서 들어온다.

### 5-1 채택 후보

| 도구 | 무엇 | 왜 여기에 맞나 |
|---|---|---|
| **off-policy 평가 도구** (예: Open Bandit Pipeline 계열의 IPS/DR 추정기) | 편향된 노출 로그에서 "다른 순서였다면 더 나았을까"를 추정 | 원칙 ⑴이 지목하는 바로 그 어려움이다. 내부 Alpha 규모에서는 A/B를 돌릴 트래픽이 없으므로 **off-policy가 유일한 정직한 평가 수단**이다. 더 중요한 것은 이 도구들이 **로그 스키마를 강제한다**는 점 — §4-1의 되돌릴 수 없는 결정과 정확히 맞물린다. 스키마를 스스로 발명하면 나중에 보정 불가능한 형태가 되기 쉽다 |
| **popularity-bias 지표 정의** (노출 Gini/entropy, long-tail coverage) | 소수 항목이 노출을 흡수하는 정도의 측정 | **(rev 2 재도출) R9의 두 갈래 모두에서 필요하다** — 이것이 계약과 무관하게 남는 이유다. ⒜를 고르면 순서만 바꾼다는 것을 **검증**할 지표가 필요하고, ⒝를 고르면 받아들인 대가가 **얼마나 큰지 알** 지표가 필요하다. 지금은 어느 쪽도 관측 불가다. 정의 차용은 수십 줄이고 기존 eval ratchet gate에 키 하나로 붙는다 |

### 5-2 조건부 — 자체 구현이 낫다고 보는 것

**탐색(exploration) 슬롯.** VW contextual bandit 같은 성숙한 구현이 있지만, 후보 ≤300이고 역할이
tiebreak only(§3-3)이면 Thompson sampling 자체 구현이 100줄 수준이다. 하우스 스타일(frozen+slots
dataclass, 경계에만 pydantic, hand-written fake)에 프레임워크를 얹는 운영 비용이 이득보다 크다.
**평가는 §5-1의 도구로, 정책은 자체 구현으로** 나누는 것이 원칙 ⑵에 맞다.

탐색 슬롯은 두 역할을 겸한다: 루프를 깨는 장치이면서 §5-1 보정의 데이터원이다.

### 5-3 기각

| 도구군 | 기각 이유 |
|---|---|
| ANN·벡터 검색 (Milvus·Qdrant·Faiss), two-tower 검색 모델 | **(rev 2 재도출)** 계약 논거(`exhaustive` 파손)는 §3-4에서 소멸했다. 남는 근거는 산수 하나다 — 후보가 exact lookup 300개인 동안 근사 검색이 해결할 대상이 없다. **§5-5가 뒤집히면 이 판정도 뒤집힌다** |
| feature store (Feast 등) | 온라인/오프라인 일관성 문제를 새로 사온다. 현재 feature는 같은 요청에서 이미 읽는 데이터다 |
| 대규모 학습 스택 (Merlin·TorchRec·HugeCTR) | 수십억 상호작용과 GB 임베딩 테이블 전제. 우리는 수천 이벤트다. 규모 불일치가 두 자리 이상 |
| MF/CF 라이브러리 (LightFM·implicit·Cornac) | **(rev 2 재도출)** "§2-3 위반"은 §3-4에서 소멸했다 — §1-4가 요구하는 것이 바로 user-user CF이므로 위반이 아니라 **의도**다. 남는 근거는 데이터다: 이 라이브러리들은 dense 상호작용 행렬을 전제하는데 §2-9는 행렬이 비어 있다. **persona 기반 유사도는 CF가 아니라 content-based이고, 그쪽이 cold에서 도는 유일한 형태다** |
| end-to-end 플랫폼 (Gorse류) | grounding·gate·attribution을 전부 자기가 소유한다. 지금까지 만든 계약을 버려야 한다 |
| 오픈소스 re-ranking 서비스 (Metarank류) | **(rev 2) 기각 → 시기상조.** scalar 충돌 논거는 §3-4에서 소멸했다. 남는 이유는 §2-9다 — 학습할 라벨이 0인 상태에서 학습 랭킹 서비스를 붙이면 운영 대상만 늘고 동작하는 부분이 없다. 라벨이 쌓이면 재검토 대상이며, 그때 비교 기준은 "자체 구현 대비 무엇을 더 주는가"다 |

### 5-4 `판단` recsys가 아니라 IR인 항목 — 혼동하지 않기

§2-8의 절벽은 recsys 문제가 아니라 **IR top-k 문제**이고, `threshold.py`는 검색엔진이 이미 갖고 있는
것(WAND / block-max 스킵)을 DynamoDB 위에 다시 구현한 것이다. 만약 holder 인덱스 소유가 우리로 오면
(§6-2) OpenSearch·Vespa 같은 **전치 인덱스 엔진이 진짜 후보가 된다**: `user`를 document, 보유 토픽을
term, 주입된 score를 impact, 후손 가중치를 term weight로 두면 rollup은 weighted OR top-k 한 방이고,
`resolve` 단계가 원리적으로 사라진다. 그리고 **WAND top-k는 근사가 아니라 정확하므로 fail-closed 계약을
고칠 필요가 없다.**

단 §1-1이 성립하면 이 항목의 우선순위는 내려간다 — rollup 자체가 없어지기 때문이다. 여기 적어두는
이유는 **"오픈소스 도입" 논의에서 IR 엔진과 recsys 프레임워크가 같은 항목으로 취급되면 안 된다**는
것이다. 앞은 계약을 보존하고 뒤는 계약을 가져간다.

### 5-5 `열린 항목` (rev 2) 판정이 갈리는 단 하나의 질문 — 후보 생성이 유계인가

§5-0 ⑴은 산수이므로 계약을 내려도 남는다(§3-4). 그러나 **"후보 300개"라는 숫자가 유지되는지는
§1-4의 해석에 달려 있고, 여기서 §5-1·§5-3이 갈린다.**

| 해석 | 후보 생성 | 결과 |
|---|---|---|
| ⒜ 지금 모양 유지 — 토픽 하나로 ground한 뒤 그 보유자 중에서 순위 | 유계 (≤300) | §5-3의 기각이 전부 유지된다. ANN·feature store·대규모 학습 스택은 해결할 대상이 없다 |
| ⒝ topic 없이 요청 — "이 유저와 잘 맞을 에이전트"를 persona만으로 | **경우에 따라 다르다 (rev 3 정정)** | 아래 §5-5-1 |

#### 5-5-1 `판단` (rev 3 정정) "topic 없이"와 "grounding 없이"는 다른 것이다

rev 2의 표는 ⒝를 "grounding 없이"로 적고 후보가 무계라고 단정했다. **그것이 두 개를 하나로 묶은
것이다.** topic을 요청 인터페이스에서 없애는 것과, 내부에서 grounding을 우회하는 것은 별개다.

topic 없이 받은 요청의 후보 생성 방법은 셋이다.

| | 방법 | 판정 |
|---|---|---|
| ⑴ | persona → 토픽 집합(§4-6-2) → 각 토픽의 보유자 → 합집합 | **권고.** 지금 있는 기계로 가능 |
| ⑵ | 에이전트 임베딩에 ANN | 에이전트의 벡터가 결국 "보유 토픽 집합의 임베딩"이므로 ⑴과 같은 정보를 더 불투명하게 쓴다 |
| ⑶ | 협업 필터링 | 데이터 0 (§2-9) |

⑴을 고르면 **"topic 없이"는 인터페이스이고 내부적으로는 topic을 경유한다.** 결과:

- **후보가 유계로 남는다** — persona 상위 N 토픽 × 100. → **ANN도 벡터DB도 필요 없다**(§5-3 첫 행의
  기각이 ⒝에서도 유지된다).
- **여섯 단계가 거의 그대로 재사용된다.** S1의 입력이 "요청 topic 문자열"에서 "persona 문서"로 바뀌고
  **S2–S6은 동일**하다. 정본 §10 규칙 2("새 추천 형태 = `pipelines/`에 조립 함수 하나 추가")가 정확히
  이 모양이다.
- **귀속이 살아남는다** — "당신 persona의 X 때문에, X를 보유한 이 사람".

무계가 되는 것은 **grounding을 우회할 때만**이고, 그것은 ⑵·⑶을 고를 때다. 그리고 ⑵는 ⑴보다 나은
점이 없고 ⑶은 입력이 없으므로, **현재 근거로는 무계 경로를 고를 이유가 없다.**

★ R1 스키마에 대한 함의: ⑴을 고르면 "고려된 후보 전체"가 계속 유계이므로 rev 2의 걱정(상위 N +
노출 확률로 축약해야 한다)은 **지금은 불필요하다.** 단 ⑵·⑶을 열어 둘 것이면 그때 필요해진다.

### 5-6 `판단` (rev 3) GPU냐 통계냐 — 지점별로 갈린다

"recsys"를 한 덩어리로 보면 이 질문에 답할 수 없다. 네 지점이 각각 다르다.

| 지점 | GPU 필요? | 근거 |
|---|---|---|
| 랭커 **학습** | **아니다** | 라벨이 10³–10⁴ 행, positive 10²대(§4-5-1). 이 구간은 **GBDT가 신경망을 이긴다** — 알려진 데이터 규모 임계값이고, 신경 랭커가 GBDT를 넘으려면 10⁶+ 상호작용이 필요하다. CPU로 수초 |
| 랭커 **추론** | **아니다** | 후보 ≤300 × 트리 앙상블 = 마이크로초 |
| persona **임베딩** | 모델은 GPU급이지만 **요청 경로가 아니다** | 유저당 1회, persona는 거의 안 바뀐다 → 갱신 시 배치. CPU 수십 ms. 단 §2-10 때문에 **경로 신설이 필요하다** |
| ANN 인덱스 | §5-5-1 ⑵·⑶에서만, 그것도 10⁸ 벡터부터 | 10⁵–10⁶이면 단일 노드 HNSW로 sub-ms |

→ **통계 기반이 맞고 GPU는 필요 없다.** 그리고 이것은 비용 판단이 아니다 — 우리 데이터 규모에서 GPU
스택을 도입하면 인프라 값을 내고 **정확도가 더 나빠진다.**

#### 5-6-1 모델 선택 — LR로 시작해서 GBDT로 이행

정직한 순서다. positive 10²대 + feature 5–8개(§4-5-1)면 **logistic regression이 더 안전하다**(과적합).
positive가 10³을 넘어가면 **LightGBM `lambdarank`**가 이긴다.

LightGBM을 그 다음 단계로 고른 이유 네 개:

1. CPU 수초, GPU 불필요
2. **결측 feature를 네이티브로 다룬다** — 우리 feature는 구조적으로 부분 결측이다(persona가 없을 수
   있고, 신규 에이전트는 피드백이 없다). `stages/ordering.py:22-27`의 기존 규칙이 이미
   *"an unknown facet is ignored and an expected one that is missing is treated as absent, never as
   zero"*이므로 같은 태도다
3. **순수 함수로 서빙된다** — 모델 파일 + `predict()`. 서비스도 네트워크 홉도 상태도 없이
   `OrderingStrategy.key()`에 들어간다
4. **`monotone_constraints`** — feature가 한 방향으로만 작용하도록 **강제**할 수 있다. 즉 §3-3 ⒜를
   규율이 아니라 **기계적 강제점**으로 만들 수 있다. 이 프로젝트의 반복된 결함이 "문서로 쓴 불변식 ≠
   강제"인데, 여기서는 도구가 강제점을 준다

`판단` **LR을 먼저 고르는 이유가 하나 더 있다**: §2-10대로 런타임에 수치 스택이 없어서, LightGBM은
numpy까지 포함해 서빙 이미지를 ~150MB 늘린다. LR은 계수 배열 하나라 numpy조차 필요 없다.

### 5-7 `판단` (rev 3) 도입 형태 — 라이브러리이고, 재작성이 아니다

recsys가 대체하는 것은 **여섯 단계 중 정확히 하나**다.

| 단계 | recsys가 대체 | 비고 |
|---|---|---|
| S1 expansion | 아니다 | LLM 질의 확장 — recsys에 대응물이 없다 |
| S2 grounding | 아니다 | entity linking — recsys에 대응물이 없다 |
| S3 retrieval | §5-5-1 ⑴에서 **입력이 바뀔 뿐** | 후보 소스는 그대로 topic-api |
| S4 merge | 아니다 | 융합·제외·자격 |
| **S5 ordering** | **예 — 여기 하나** | `OrderingStrategy` 구현체 교체(§2-1) |
| S6 assembly | 아니다 | 귀속·wire |

S1·S2가 코드의 대부분이고 LLM 통합 전부인데 recsys는 그것을 건드리지 않는다. 그래서 도입은
**덧붙이는 변경**이다:

- `LearnedOrdering(OrderingStrategy)` — 1-메서드 Protocol 구현체 하나
- 피드백 수신 경로 — **요청 경로 밖**의 엔드포인트 또는 컨슈머
- 학습 잡 — 기존 `cli/` 서브커맨드 패턴 확장(`cli/__main__.py`의 `register` 구조)
- (§5-5-1 ⑴이면) `pipelines/`에 조립 함수 하나 — 정본 §10 규칙 2

#### 5-7-1 `판단` 가장 중요한 구체 권고 — feature 추출 함수는 하나여야 한다

학습 잡과 `LearnedOrdering`이 **같은 함수**를 쓴다. training/serving feature skew는 recsys 프로덕션
버그의 대표 유형이고, 코드 공유가 유일한 실질적 방어다.

→ **학습 잡을 별도 repo나 별도 서비스로 빼면 안 된다.** 같은 repo의 CLI 서브커맨드가 맞다. 이것은
편의가 아니라 방어다 — 두 구현이 생기는 순간 그 결함은 테스트로 잡히지 않는다(양쪽 테스트가 각자의
구현을 고정한다).

#### 5-7-2 `판단` 재작성을 정당화하는 것은 하나뿐이다 — precompute

추천을 요청 시점에 계산하지 않고 미리 계산해 KV로 읽는 구조라면 이 서비스의 대부분이 불필요해진다
(배치 잡 + KV 읽기가 전부). **그것이 유일한 재작성 트리거다.** 자세한 것은 §5-8.

### 5-8 `열린 항목` (rev 3) precompute — 단일 결정이 아니라 경로별 속성이다

`판단` precompute 여부는 하나로 정할 필요가 없다. 경로마다 답이 다르다.

| 경로 | precompute 가능? | 왜 |
|---|---|---|
| topic 요청 (자유 텍스트) | **불가** | 질문을 미리 볼 수 없으므로 grounding을 미리 할 수 없다 |
| persona 기반 (§5-5-1 ⑴) | **가능** | persona는 미리 있다. 하루 1회 배치가 자연스럽다 |

**미루는 대가가 거의 0이다.** precompute와 실시간은 **같은 파이프라인을 다른 트리거로 부르는 것**이고,
이음매가 이미 있다 — `cli/recommend.py:80`이 `async with recommend_pipeline()`으로 서빙과 같은
composition root를 in-process로 부른다(§2-10). CronJob이 이것을 유저 목록에 대해 루프하고 결과를 KV에
쓰면 그것이 precompute다. 조건은 하나 — 파이프라인이 요청 컨텍스트에 묶이지 않아야 하고,
`RecommendCommand`가 transport 무관 값이므로 이미 만족한다(정본 §10 규칙 3).

#### 5-8-1 `판단` precompute가 공짜가 아닌 유일한 지점 — visibility가 stale해진다

하루 전에 계산한 추천에, 그 사이 private으로 바뀐 보유가 들어 있을 수 있다. 이것은 §3-4에서 "계약과
무관하게 남는다"로 분류한 privacy 항목이므로 무를 수 없다.

→ **precompute 경로는 서빙 시점에 visibility를 재확인하는 단계가 필요하다.** 재확인은 id 목록에 대한
값싼 조회이므로 설계 가능하고, 그 단계를 넣지 않은 precompute는 **하루 동안 남의 비공개 보유를
노출한다.** 이것을 R11로 등재한다.

---

## §6 열린 항목과 선결 조건

### 6-1 열린 항목 정리

| # | 항목 | 상태 |
|---|---|---|
| R1 | 후보별 노출·propensity 이벤트 스키마 (§4-1) | **미설계. 이 문서에서 유일하게 시급한 항목** — 서빙 시작 전에 정해야 한다 |
| R2 | 라벨 정의 (§4-2 표) | 초안만. "재방문"·"같은 토픽 재질의"의 관측 가능성이 bourbon-agent 쪽 이벤트에 달려 있다 — 미협의 |
| R3 | persona md 수신 계약 (출처·갱신·크기·언어) | 미협의. §4-3의 비신뢰 취급이 선결 |
| R4 | (A) 토픽 축 재순위의 진입 지점 | S2 어디인지 미설계. `grounding_select`의 rule pass와 LLM pick 사이인지, 그 뒤인지 |
| R5 | (B) 홀더 축 공급처가 `OrderingStrategy`에 들어오는 형태 | §3-3 불변식은 정해졌으나 인터페이스 미설계 |
| R6 | popularity-bias 지표를 eval ratchet에 추가 (§5-1) | 미착수. §5-1 두 항목 중 **이것이 먼저다** — 금지 규율에 강제점이 없다 |
| R7 | 탐색 슬롯 (§5-2) | 미설계. R1 없이는 데이터가 안 쌓이므로 R1 뒤 |
| R8 | off-policy 평가 도구 도입 (§5-1) | R1의 스키마가 이것을 결정하므로 **R1과 함께 판단**해야 한다. 도구를 나중에 고르면 스키마가 안 맞는다 |
| R9 | 학습이 순서만 바꾸는가 집합도 고르는가 (§3-3 ⒜/⒝) | **지금 고르지 않는다.** 두 경우의 이음매가 같다(§3-0-1). 데이터 축적 후 측정으로 결정 |
| R10 | topic 없는 요청의 후보 생성 방법 (§5-5-1 ⑴/⑵/⑶) | **(rev 3 정정) R1보다 먼저 답할 필요가 없어졌다.** ⑴이면 후보가 계속 유계이므로 R1 스키마가 그대로다. ⑵·⑶을 열어 둘 때만 R1보다 앞선다 — 그리고 현재 근거로는 ⑵·⑶을 고를 이유가 없다 |
| R11 | precompute 경로의 visibility 재확인 단계 (§5-8-1) | **미설계. precompute를 하기로 하면 선결이다** — 없으면 하루 동안 남의 비공개 보유를 노출한다 |
| R12 | persona 임베딩 경로 (§2-10 · §4-6-3) | 미협의. e3llm의 embedding 차단을 풀 것인지 별도 배치로 갈 것인지. **단 §4-6-2가 먼저다** — grounding으로 되면 이 항목 자체가 불필요하다 |
| R13 | `HoldingEvidence`의 세 값을 `GroupContribution`까지 배선 (§4-5-2) | 미착수. feature 후보를 6개에서 9개로 늘리는 일이고, §4-5-1의 상한 때문에 **positive가 늘어난 뒤**에 의미가 있다 |

### 6-2 `열린 항목` 선결 조건 — holder 인덱스는 누가 갖는가

§1-1이 성립하면 "토픽 T를 누가 보유하나"의 인덱스 소유가 우리(또는 추천 시스템)로 온다. 이것은
`facets_ownership_split_discussion.md`가 이미 다루는 질문과 **같은 질문**이고, 그 문서의 착수 자격은
측정값 셋이다(그쪽 §7-1).

→ **그 논의의 답이 이 그림에서 load-bearing이 된다.** 더 이상 독립적으로 미룰 수 있는 항목이 아니다.

### 6-3 `판단` 이 그림이 정하는 것 — `service_boundary_discussion.md` SB1

§3-1의 토픽 축 학습이 성립하면 "자연어+맥락 → 토픽"은 **우리 몫으로 정해진다.** 그 학습이 피드백
루프를 필요로 하고, 루프는 우리가 갖기 때문이다. 그쪽 문서의 경계 규칙(데이터 소유자는 사실과 검색
프리미티브, 호출하는 쪽은 해석·정책·실패 계약)과 같은 방향이다.

### 6-4 `판단` 답이 뒤집히는 조건

- **R10이 ⑵·⑶으로 답하면** §5-3 첫 행(ANN 기각)이 **채택으로 뒤집힌다.** rev 2에서 계약 논거가
  사라진 뒤 남은 근거가 "후보가 유계다" 하나뿐이므로, 그 전제가 깨지면 판정도 함께 깨진다.
  **(rev 3)** 단 ⑴을 고르면 전제가 유지되므로 뒤집히지 않는다 — 뒤집히는 조건은 "topic 없는 요청"이
  아니라 "grounding 우회"다(§5-5-1).
- **(rev 3) positive 수가 10³을 넘으면** §5-6-1의 모델 선택이 LR에서 GBDT로 넘어간다. 이것은 판단이
  아니라 관측으로 결정되는 지점이며, §4-1이 남기는 분모가 그 관측을 가능하게 한다.
- **(rev 3) persona 항목의 grounding 성공률이 낮으면** §4-6-2가 무너지고 R12(임베딩 경로)가 필요해진다.
  이 역시 측정으로 답이 나오는 항목이다.
- **라벨 규모가 두 자리 커지면**(외부 공개·트래픽 증가) 원칙 ⑴의 전제가 깨지고 학습 스택 도입이
  다시 검토 대상이 된다. 기준은 라벨 수가 아니라 **A/B를 돌릴 트래픽이 있는가**다.
- **§3-2**: pool이 유한하다는 전제가 깨지면(홀더 수천 명) 자기강화 루프의 심각도가 커진다. R9 ⒝를
  고른 경우 이 방향은 더 빠르게 나빠진다.
- **§4-2(라벨)와 privacy 두 항목은 뒤집히는 조건을 적을 수 없다.** 계약이 아니라 측정 타당성과
  성질에서 나오기 때문이다(§3-4). 뒤집으려면 "지식 제품에서 좋은 답이 대화를 늘린다"가 관측되어야
  하고, 그것은 측정으로 확인할 수 있는 주장이다 — 반증 가능한 형태로 남겨 둔다.

---

## §7 이 문서가 하지 않은 것

- 코드 변경 **없음**. `bourbon-agent-discovery-api`는 `d82cd04` 그대로다.
- 정본(`recommendation_pipeline_design.md`) 변경 **없음**. §3-3의 불변식을 §S5·§10으로 올리는 것은
  결정 후다.
- 오픈소스 도구를 **하나도 도입하지 않았다.** §5는 평가이고, 실제 선택은 R8이 R1과 함께 판단한다.
- 특정 제품·버전을 검증하지 않았다. §5의 도구 이름은 **부류의 예시**이고, 도입 시점에 실물 검증이
  따로 필요하다.
- bourbon-agent·producer·topic-api 어느 팀에도 **발신하지 않았다.**
- `topic_api_analysis.md` §11(열린 질문) 갱신 **없음**.
- **(rev 2) 정본의 규율을 삭제하거나 수정하지 않았다.** "계약은 이 그림의 제약이 아니다"는 이 문서의
  전제이지 정본의 변경이 아니다 — 정본은 아직 현행 시스템(topic-api가 보유자 랭킹을 소유)을 서술하고
  있고, 그것이 바뀔 때 규율을 정리하는 것은 별건이다. 지금 정본에서 규율을 지우면 **아직 서빙 중인
  시스템의 근거를 지우는 것**이 된다.
- **(rev 2) R9(⒜/⒝)와 R10에 답하지 않았다.** rev 3에서 R10의 선후 관계가 정정되었다(§5-5-1).
- **(rev 3) 모델도 라이브러리도 도입하지 않았다.** §5-6-1의 LR/LightGBM은 **판정이고 선택이 아니다** —
  실제 도입은 positive 수가 관측된 뒤다(§6-4).
- **(rev 3) persona 임베딩 모델을 고르지 않았다.** §4-6-3의 요건(다국어·대칭·CPU급)만 적었고, §4-6-2가
  먼저 확인되어야 이 선택 자체가 필요한지 알 수 있다.
- **(rev 3) precompute를 결정하지 않았다.** 경로별 속성임을 밝히고 대가(R11)를 등재했을 뿐이다.

## 변경 이력

- **2026-08-27 rev 1** — 신설. 2026-08-27 대화(피드백 루프 기반 추천 + 오픈소스 recsys 도입)의
  사실·판단·열린 항목(R1–R8)을 임시 기록. §2는 `d82cd04`·`9ee67f3`에서 `file:line`으로 확인했다.
  ★규율 네 개:
  ⑴ §3 — **피드백을 "추천 품질 학습"으로 한 덩어리로 부르면 위험한 축이 안전한 축의 정당성을 빌려
  쓴다.** 학습 대상은 토픽 축과 홀더 축으로 갈라서 각각 판정한다.
  ⑵ §4-2 — **지식 제품에서 engagement 볼륨은 품질의 대리가 아니라 그 반대일 수 있다.** 좋은 답은
  대화를 끝낸다. 라벨을 고를 때 "이 신호가 많아지는 실패 모드"를 먼저 쓴다.
  ⑶ §5-0 ⑵ — **도입 비용은 코드가 아니라 계약이다.** 프레임워크는 자기 가정(스칼라 점수·노출=긍정·
  popularity 유용)을 갖고 오고, 그것이 정본과 테스트로 잠근 계약을 우회하는 통로가 된다.
  ⑷ §5-1 둘째 항목 — **금지 규율에 그것을 탐지할 지표가 없으면 그 규율은 강제점이 없다.** popularity
  prior 금지를 문서로 갖고 있으면서 노출 편중을 재는 지표가 없었다. 이 프로젝트에서 반복된 결함
  유형("문서로 쓴 불변식 ≠ 강제")의 새 사례다.
- **2026-08-27 rev 2** — 두 전제를 받아 재도출. ⑴ 기존 계약은 이 그림의 제약이 아니다 ⑵ 네 입력 중
  `topic`만 실재한다. 추가: §2-9(입력 재고), §3-0(facets 선례를 방법으로 채택 + 그 선례가 규정하지
  못하는 비대칭), §3-4(계약을 내렸을 때 남는 것과 사라지는 것), §5-5(판정이 갈리는 단 하나의 질문),
  R9·R10. 재도출: §3-3(불변식 → 두 선택지와 대가), §5-0 ⑵, §5-1 popularity 지표, §5-3 세 행, §4-1의 근거.
  ★규율 세 개:
  ⑸ **rev 1의 논거 다수가 계약에서 나왔고, 계약이 종속이 되자 함께 무너졌다.** 근거의 출처를 적어두지
  않으면 전제가 바뀔 때 무엇이 남는지 알 수 없다 → §3-4를 표로 만든 이유다. 앞으로 판단을 쓸 때
  **"이것은 계약에서 나왔는가, 측정·산수·성질에서 나왔는가"를 함께 적는다.**
  ⑹ **"나중에 결정된다"가 같아 보여도 기다림의 대가가 다를 수 있다**(§3-0-2). facets는 소급 이력과
  함께 도착하고 피드백은 기다린 기간만큼 영구히 없다. 선례를 방법으로 빌릴 때 **선례가 성립한
  조건까지 같은지** 확인한다.
  ⑺ **금지를 선택지로 내리면 대가를 명명해야 한다**(§3-3). "하지 마라"를 "이쪽을 고르면 이것을 잃는다"로
  바꾸지 않으면, 금지를 푼 것이 아니라 대가를 숨긴 것이 된다.
- **2026-08-27 rev 3** — 네 질문에 답하며 확장 + **정정 두 건**. 추가: §2-10(인프라 사실 — GPU 없음·
  e3llm embedding 차단·수치 스택 없음·CLI가 파이프라인을 in-process로 부름), §4-5(학습 행의 단위 —
  라벨·feature 정의와 오늘 쓸 수 있는 feature 6개), §4-6(persona 처리 — 임베딩은 cold start 장치이고,
  나열이면 grounding으로 대체 가능하며, 쓸 때는 identity/preference 두 벡터), §5-6(GPU는 어느 지점에도
  필요 없다 + LR→GBDT 순서), §5-7(도입은 라이브러리 — 여섯 단계 중 S5 하나 + feature 함수는 하나),
  §5-8(precompute는 경로별 속성), R11·R12·R13.
  **정정 ⑴ §5-5-1** — rev 2가 "topic 없이 = grounding 없이 = 후보 무계"로 묶었다. 셋은 다르다:
  persona를 grounding으로 경유하면 topic 없는 인터페이스에서도 후보가 유계이고 ANN이 불필요하다.
  R10의 "R1보다 먼저"도 이 정정으로 해제됐다.
  **정정 ⑵ §4-1 · §4-5-1** — rev 1·2의 "라벨 수천"은 행을 센 것이고, 모델 크기를 정하는 것은
  positive 수다. feature 상한(≤ positive/10)이 거기서 나오며, rev 1이 쓴 "~20개"는 과적합 영역이었다.
  ★규율 세 개:
  ⑻ **규모를 근거로 쓸 때는 세는 단위를 함께 적는다**(정정 ⑵). "라벨 수천"은 행·positive·요청 중
  무엇인지에 따라 결론이 달라지는데, 단위를 안 적으면 그 차이가 보이지 않는다.
  ⑼ **인터페이스에서 무엇을 없애는 것과 내부에서 그 단계를 우회하는 것은 다른 결정이다**(정정 ⑴).
  하나로 묶으면 필요 없는 인프라가 필요해 보인다.
  ⑽ **같은 값을 두 곳에서 계산하면 그 결함은 테스트로 잡히지 않는다**(§5-7-1). 양쪽 테스트가 각자의
  구현을 고정하기 때문이다 — training/serving feature skew가 그 사례다.
  ⑾ **문서 상호 참조에 rev를 고정하면 cascade가 생긴다.** 한쪽을 올리면 상대의 참조가 stale해지고,
  그것을 고치려 상대를 올리면 이쪽이 다시 stale해진다. 이번에 세 문서가 한 커밋에서 발행되므로 그 안에서
  fixpoint를 잡았다 — **규칙: 같은 커밋에서 함께 발행되는 문서들끼리는 서로의 최종 rev를 적고, 그
  갱신을 별도 rev로 세지 않는다.** 다른 커밋의 문서를 가리킬 때만 rev를 고정한다.
